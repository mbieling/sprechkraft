---
phase: 04-text-output
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - SPRECHKRAFT/Extensions/Defaults+Keys.swift
  - SPRECHKRAFT/Extensions/KeyboardShortcuts+Names.swift
  - SPRECHKRAFT/AppState.swift
  - SPRECHKRAFT/Info.plist
autonomous: true
requirements:
  - OUT-02
  - OUT-03

must_haves:
  truths:
    - "OutputMode-Enum existiert mit .field und .clipboard"
    - "Defaults.Keys.outputMode hat Standardwert .field (per D-06)"
    - "KeyboardShortcuts.Name.toggleOutputMode ist mit ⇧⌘V vorbelegt (per D-09)"
    - "AppState.axPermissionDenied: Bool ist vorhanden (per D-10)"
    - "Info.plist enthält NSAccessibilityUsageDescription"
  artifacts:
    - path: "SPRECHKRAFT/Extensions/Defaults+Keys.swift"
      provides: "OutputMode enum + Defaults.Keys.outputMode"
      contains: "enum OutputMode"
    - path: "SPRECHKRAFT/Extensions/KeyboardShortcuts+Names.swift"
      provides: "toggleOutputMode Name"
      contains: "toggleOutputMode"
    - path: "SPRECHKRAFT/AppState.swift"
      provides: "axPermissionDenied: Bool Property"
      contains: "axPermissionDenied"
    - path: "SPRECHKRAFT/Info.plist"
      provides: "AX-Usage-Beschreibung für macOS Privacy"
      contains: "NSAccessibilityUsageDescription"
  key_links:
    - from: "Defaults.Keys.outputMode"
      to: "AppDelegate.showMenu()"
      via: "Defaults[.outputMode]"
      pattern: "Defaults\\[.outputMode\\]"
    - from: "AppState.axPermissionDenied"
      to: "SettingsView"
      via: "appState?.axPermissionDenied"
      pattern: "axPermissionDenied"
---

<objective>
Legt alle Datentypen, Defaults-Keys und AppState-Properties an, die Phase 4 braucht —
bevor TextOutputService und AppDelegate-Wiring sie konsumieren.

Purpose: Foundation für parallele Plan-02-Implementierung. Kein neuer Code kann ohne
OutputMode-Enum und axPermissionDenied-Property korrekt kompilieren.
Output: OutputMode-Enum in Defaults+Keys, toggleOutputMode-Name, axPermissionDenied in AppState,
NSAccessibilityUsageDescription in Info.plist.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/PROJECT.md
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/STATE.md

<interfaces>
<!-- Bestehende Patterns die EXAKT repliziert werden. Executor liest diese Dateien zuerst. -->

Aus SPRECHKRAFT/Extensions/Defaults+Keys.swift (aktueller Stand):
```swift
import Defaults

extension Defaults.Keys {
    static let silenceDuration = Key<Double>("silenceDuration", default: 1.5)
    static let selectedMicUID = Key<String?>("selectedMicUID", default: nil)
}
```

Aus SPRECHKRAFT/Extensions/KeyboardShortcuts+Names.swift (aktueller Stand):
```swift
import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    static let toggleRecording = Self(
        "toggleRecording",
        default: .init(.r, modifiers: [.option, .command])  // ⌥⌘R
    )
}
```

Aus SPRECHKRAFT/AppState.swift (relevante Properties):
```swift
@MainActor
@Observable
final class AppState {
    var recordingState: RecordingState = .idle
    var audioLevel: CGFloat = 0.0
    var micPermissionDenied: Bool = false  // <-- Pattern für axPermissionDenied
    var isModelReady: Bool = false
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OutputMode-Enum + Defaults-Key + toggleOutputMode-Hotkey-Name</name>
  <files>SPRECHKRAFT/Extensions/Defaults+Keys.swift, SPRECHKRAFT/Extensions/KeyboardShortcuts+Names.swift</files>

  <read_first>
    - SPRECHKRAFT/Extensions/Defaults+Keys.swift — bestehende Key-Definitionen, Import-Zeilen; Enum wird in dieser Datei ergänzt
    - SPRECHKRAFT/Extensions/KeyboardShortcuts+Names.swift — bestehende Name-Definition als Vorlage
  </read_first>

  <behavior>
    - OutputMode.field ist der Standardwert (D-06)
    - OutputMode ist via Defaults.Serializable serialisierbar (RawRepresentable reicht bei String RawValue)
    - Defaults.Keys.outputMode liefert .field wenn der Key noch nie gesetzt wurde
    - KeyboardShortcuts.Name.toggleOutputMode hat ⇧⌘V als Default-Shortcut (D-09)
  </behavior>

  <action>
In SPRECHKRAFT/Extensions/Defaults+Keys.swift:

1. Vor der `extension Defaults.Keys {`-Block, nach den Imports, füge den OutputMode-Enum ein:

```swift
/// Ausgabemodus: Textfeld-Injektion via AX (Standard) oder Clipboard.
/// D-06: .field ist Standard beim ersten App-Start.
/// D-07: Persistiert via Defaults unter dem Key "outputMode".
enum OutputMode: String, Defaults.Serializable {
    case field      // Textfeld-Injektion via AXUIElement (Standard)
    case clipboard  // Clipboard-Kopie via NSPasteboard
}
```

2. Innerhalb `extension Defaults.Keys { }` nach den bestehenden Keys ergänzen:

```swift
    /// Ausgabemodus: Textfeld-Injektion (.field) oder Clipboard (.clipboard).
    /// Standard .field gemaess D-06 (Core Value: Tippen-Ersatz).
    static let outputMode = Key<OutputMode>("outputMode", default: .field)
```

In SPRECHKRAFT/Extensions/KeyboardShortcuts+Names.swift:

3. Innerhalb `extension KeyboardShortcuts.Name { }` nach toggleRecording ergänzen:

```swift
    /// Wechsel-Hotkey fuer Ausgabemodus (D-09): ⇧⌘V — Mnemonik: V fuer Voice-Paste.
    static let toggleOutputMode = Self(
        "toggleOutputMode",
        default: .init(.v, modifiers: [.shift, .command])
    )
```
  </action>

  <verify>
    <automated>grep -n "enum OutputMode" /Users/mbieling/claude/voice/SPRECHKRAFT/Extensions/Defaults+Keys.swift && grep -n "outputMode" /Users/mbieling/claude/voice/SPRECHKRAFT/Extensions/Defaults+Keys.swift && grep -n "toggleOutputMode" /Users/mbieling/claude/voice/SPRECHKRAFT/Extensions/KeyboardShortcuts+Names.swift && xcodebuild build -scheme SPRECHKRAFT -destination "platform=macOS" 2>&1 | tail -5</automated>
  </verify>

  <done>
    - Defaults+Keys.swift enthält enum OutputMode: String, Defaults.Serializable mit .field und .clipboard
    - Defaults+Keys.swift enthält static let outputMode = Key&lt;OutputMode&gt;("outputMode", default: .field)
    - KeyboardShortcuts+Names.swift enthält static let toggleOutputMode mit .v + [.shift, .command]
    - xcodebuild build exitiert mit 0
  </done>
</task>

<task type="auto">
  <name>Task 2: AppState.axPermissionDenied + Info.plist NSAccessibilityUsageDescription</name>
  <files>SPRECHKRAFT/AppState.swift, SPRECHKRAFT/Info.plist</files>

  <read_first>
    - SPRECHKRAFT/AppState.swift — vollständige Datei; axPermissionDenied wird analog zu micPermissionDenied ergänzt
    - SPRECHKRAFT/Info.plist — vollständige Datei; NSAccessibilityUsageDescription wird nach NSMicrophoneUsageDescription eingefügt
  </read_first>

  <action>
In SPRECHKRAFT/AppState.swift:

Ergänze nach `var isModelReady: Bool = false` die neue Property:

```swift
    /// true wenn AXIsProcessTrusted() false zurückgibt (kein AX-Permission).
    /// Wird in applicationDidFinishLaunching gesetzt (D-10).
    /// Konsumiert von SettingsView für den roten AX-Permission-Banner (D-11).
    /// Bei true fällt TextOutputService automatisch auf Clipboard zurück (D-12).
    var axPermissionDenied: Bool = false
```

In SPRECHKRAFT/Info.plist:

Füge innerhalb des `<dict>` nach dem NSMicrophoneUsageDescription-Eintrag (oder an anderer passender Stelle) ein:

```xml
<key>NSAccessibilityUsageDescription</key>
<string>SPRECHKRAFT benötigt Bedienungshilfen-Zugriff, um Diktate direkt in Textfelder einzufügen.</string>
```

HINWEIS: Kein `com.apple.security.accessibility` Entitlement schreiben — dieses Entitlement existiert nicht (Pitfall 6, RESEARCH.md). Nur Info.plist-Key, kein Entitlements-File ändern.
  </action>

  <verify>
    <automated>grep -n "axPermissionDenied" /Users/mbieling/claude/voice/SPRECHKRAFT/AppState.swift && grep -n "NSAccessibilityUsageDescription" /Users/mbieling/claude/voice/SPRECHKRAFT/Info.plist && xcodebuild build -scheme SPRECHKRAFT -destination "platform=macOS" 2>&1 | tail -5</automated>
  </verify>

  <done>
    - AppState.swift enthält var axPermissionDenied: Bool = false
    - Info.plist enthält NSAccessibilityUsageDescription mit deutschem Erklärungstext
    - Keine Entitlements-Datei wurde geändert
    - xcodebuild build exitiert mit 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppState → SettingsView | axPermissionDenied fließt als read-only Bool in UI; kein Input von außen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | OutputMode in UserDefaults | accept | Kein sensitiver Inhalt; nur enum-String ".field"/".clipboard" |
| T-04-02 | Tampering | Defaults.Keys.outputMode | accept | Lokale UserDefaults; kein Netzwerk-Exposure; User ist Eigentümer |
</threat_model>

<verification>
Nach Plan 01:
- `grep "enum OutputMode" SPRECHKRAFT/Extensions/Defaults+Keys.swift` → Treffer
- `grep "outputMode" SPRECHKRAFT/Extensions/Defaults+Keys.swift` → Treffer für Key-Definition
- `grep "toggleOutputMode" SPRECHKRAFT/Extensions/KeyboardShortcuts+Names.swift` → Treffer
- `grep "axPermissionDenied" SPRECHKRAFT/AppState.swift` → Treffer
- `grep "NSAccessibilityUsageDescription" SPRECHKRAFT/Info.plist` → Treffer
- `xcodebuild build -scheme SPRECHKRAFT` → exit 0
</verification>

<success_criteria>
- OutputMode-Enum ist Swift-6-konform kompilierbar und Defaults.Serializable
- Defaults.Keys.outputMode default ist .field (nicht .clipboard)
- toggleOutputMode default-Shortcut ist ⇧⌘V
- axPermissionDenied: Bool existiert in AppState mit Default false
- NSAccessibilityUsageDescription in Info.plist (kein AX-Entitlement)
- Build grün
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04-text-output/04-01-SUMMARY.md` erstellen.
</output>
