---
phase: 04-text-output
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - VoiceScribe/AppDelegate.swift
  - VoiceScribe/SettingsView.swift
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-03

must_haves:
  truths:
    - "print()-Stub in AppDelegate:75 ist entfernt und durch TextOutputService.shared.output() ersetzt"
    - "applicationDidFinishLaunching prüft AXIsProcessTrusted() und setzt appState.axPermissionDenied"
    - "toggleOutputMode-Hotkey (⇧⌘V) schaltet Modus um und aktualisiert das Menü"
    - "showMenu() zeigt Menü-Häkchen für aktiven Ausgabemodus (per D-08)"
    - "SettingsView zeigt roten AX-Permission-Banner wenn axPermissionDenied == true (per D-11)"
    - "SettingsView hat KeyboardShortcuts.Recorder für toggleOutputMode (per D-09)"
  artifacts:
    - path: "VoiceScribe/AppDelegate.swift"
      provides: "TextOutputService-Wiring + AX-Check + Hotkey + Menü-Häkchen"
      contains: "TextOutputService.shared.output"
    - path: "VoiceScribe/SettingsView.swift"
      provides: "AX-Permission-Banner + OutputMode-Recorder"
      contains: "axPermissionDenied"
  key_links:
    - from: "AppDelegate.onRecordingComplete"
      to: "TextOutputService.shared.output()"
      via: "await MainActor.run"
      pattern: "TextOutputService.shared.output"
    - from: "AXIsProcessTrusted()"
      to: "appState.axPermissionDenied"
      via: "applicationDidFinishLaunching"
      pattern: "AXIsProcessTrusted"
    - from: "KeyboardShortcuts.onKeyUp(.toggleOutputMode)"
      to: "Defaults[.outputMode].toggle()"
      via: "setupOutputModeHotkey()"
      pattern: "toggleOutputMode"
---

<objective>
Verdrahtet TextOutputService mit der bestehenden Transkriptions-Pipeline, fügt den
AX-Permission-Check beim App-Start ein, richtet den toggleOutputMode-Hotkey ein,
ergänzt Menü-Häkchen für den Ausgabemodus und fügt den AX-Permission-Banner in SettingsView ein.

Purpose: Verbindet alle Phase-4-Teile zu einem funktionierenden System.
Nach diesem Plan ist die Phase 4 Code-Implementierung abgeschlossen.
Output: AppDelegate.swift mit TextOutputService-Wiring; SettingsView.swift mit AX-Banner + OutputMode-Section.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/PROJECT.md
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/STATE.md

<interfaces>
<!-- Aus Plan 01 und Plan 02 — diese Typen/Signaturen existieren nach Wave 1 -->

Aus VoiceScribe/TextOutputService.swift (Plan 02):
```swift
@MainActor
final class TextOutputService: TextOutputServiceProtocol {
    static let shared = TextOutputService()
    func output(_ text: String, mode: OutputMode, axPermitted: Bool)
    func writeToClipboard(_ text: String)
}
```

Aus VoiceScribe/Extensions/Defaults+Keys.swift (Plan 01):
```swift
enum OutputMode: String, Defaults.Serializable {
    case field
    case clipboard
}
extension Defaults.Keys {
    static let outputMode = Key<OutputMode>("outputMode", default: .field)
}
```

Aus VoiceScribe/Extensions/KeyboardShortcuts+Names.swift (Plan 01):
```swift
extension KeyboardShortcuts.Name {
    static let toggleOutputMode = Self("toggleOutputMode",
        default: .init(.v, modifiers: [.shift, .command]))
}
```

Aus VoiceScribe/AppState.swift (Plan 01):
```swift
@MainActor @Observable final class AppState {
    var axPermissionDenied: Bool = false
    // + bestehende Properties: recordingState, audioLevel, micPermissionDenied, isModelReady
}
```

Bestehender AppDelegate-Code (WICHTIG — Integrationspunkte):
```swift
// Zeile 72-80: onRecordingComplete-Callback (HIER wird print() ersetzt)
audioController?.onRecordingComplete = { [weak self] samples, sampleRate in
    guard let self else { return }
    Task {
        let text = await self.transcriptionService.transcribeWithResampling(samples, sampleRate: sampleRate)
        await MainActor.run {
            if let text {
                print("Transkription: \(text)")  // <-- ERSETZEN
            }
            self.appState?.resetToIdle()
            self.updateIcon()
        }
    }
}

// applicationDidFinishLaunching (HIER AX-Check und setupOutputModeHotkey() hinzufügen)
func applicationDidFinishLaunching(_ notification: Notification) {
    NSApp.setActivationPolicy(.accessory)
    // ...
    setupHotkey()
    setupTranscription()
    // <-- setupOutputModeHotkey() und AX-Check hier ergänzen
}

// showMenu() (HIER Menü-Häkchen ergänzen nach "Beim Login starten")
```

SettingsView Pattern (micPermissionDenied-Banner als Vorlage für axPermissionDenied-Banner):
```swift
if appState?.micPermissionDenied == true {
    HStack(spacing: DesignTokens.Spacing.sm) {
        Image(systemName: "mic.slash.fill").foregroundStyle(.white)
        VStack(alignment: .leading, spacing: 2) {
            Text("Mikrofonzugriff verweigert")...
            Text("Öffne die Datenschutz-Einstellungen...")...
        }
        Spacer()
        Button("Einstellungen öffnen") {
            NSWorkspace.shared.open(URL(string: "x-apple.systempreferences:...Microphone")!)
        }
    }
    .padding(...).background(Color(.systemRed)).cornerRadius(8)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppDelegate — TextOutputService-Wiring, AX-Check, toggleOutputMode-Hotkey, Menü-Häkchen</name>
  <files>VoiceScribe/AppDelegate.swift</files>

  <read_first>
    - VoiceScribe/AppDelegate.swift — vollständige Datei; alle 4 Änderungspunkte genau lokalisieren:
      (1) Zeile 75 print()-Stub, (2) applicationDidFinishLaunching, (3) setupHotkey() als Vorlage, (4) showMenu()
    - VoiceScribe/TextOutputService.swift — output()-Signatur (aus Plan 02)
    - VoiceScribe/Extensions/Defaults+Keys.swift — OutputMode-Enum + outputMode-Key (aus Plan 01)
  </read_first>

  <action>
Vier Änderungen in VoiceScribe/AppDelegate.swift:

### Änderung 1: Import ergänzen (oben, nach bestehenden Imports)
```swift
import ApplicationServices  // Für AXIsProcessTrusted()
import Defaults             // Für Defaults[.outputMode]
```
(Falls Defaults bereits importiert ist, nur ApplicationServices ergänzen)

### Änderung 2: print()-Stub ersetzen (ca. Zeile 75)
Ersetze:
```swift
if let text {
    print("Transkription: \(text)")  // D-07: Pipeline-Stub, Phase 4 ersetzt dies
}
```
Durch:
```swift
if let text {
    // OUT-01/OUT-02: Text ausgeben via TextOutputService (ersetzt Phase-3-Pipeline-Stub)
    // axPermissionDenied von AppState — gesetzt in applicationDidFinishLaunching (D-10)
    let mode = Defaults[.outputMode]
    let axPermitted = !(self.appState?.axPermissionDenied ?? true)
    TextOutputService.shared.output(text, mode: mode, axPermitted: axPermitted)
}
```

### Änderung 3: applicationDidFinishLaunching — AX-Check + setupOutputModeHotkey()
Am Ende von applicationDidFinishLaunching, NACH `setupTranscription()`, ergänzen:

```swift
        // D-10: AX-Permission beim App-Start prüfen (ohne OS-Dialog — kein kAXTrustedCheckOptionPrompt)
        // Ergebnis in AppState.axPermissionDenied für SettingsView-Banner (D-11)
        // und TextOutputService-Routing (D-04/D-12)
        // HINWEIS: appState wird nach applicationDidFinishLaunching via setupAudioController() injiziert.
        // AX-Check daher AUCH in setupAudioController() wiederholen, damit appState nicht nil ist.
        let axGranted = AXIsProcessTrusted()
        appState?.axPermissionDenied = !axGranted

        setupOutputModeHotkey()
```

Außerdem am Ende von setupAudioController() (wo appState sicher nicht nil ist) ergänzen:
```swift
        // D-10: AX-Permission jetzt sicher setzen (appState ist hier garantiert nicht nil)
        let axGranted = AXIsProcessTrusted()
        appState?.axPermissionDenied = !axGranted
```

### Änderung 4: Neue Methode setupOutputModeHotkey() hinzufügen (nach setupHotkey())

```swift
    // MARK: - Output-Mode Hotkey (OUT-03, D-09)

    /// Registriert den toggleOutputMode-Hotkey (⇧⌘V, konfigurierbar).
    /// Wechselt Defaults[.outputMode] zwischen .field und .clipboard (D-07).
    /// Aktualisiert das Menü via updateMenu-Mechanismus (Observation-B Pattern).
    private func setupOutputModeHotkey() {
        KeyboardShortcuts.onKeyUp(for: .toggleOutputMode) { [weak self] in
            Task { @MainActor [weak self] in
                guard let self else { return }
                // D-07: Toggle zwischen .field und .clipboard
                Defaults[.outputMode] = Defaults[.outputMode] == .field ? .clipboard : .field
                // Menü spiegelt neuen Zustand beim nächsten Öffnen (showMenu() baut Menü neu)
                // Kein explizites updateIcon() nötig — Modus beeinflusst kein Icon
            }
        }
    }
```

### Änderung 5: showMenu() — Menü-Häkchen für Ausgabemodus (D-08)

Im showMenu()-Block, nach dem "Beim Login starten"-Eintrag und VOR dem Separator vor "Beenden",
folgendes einfügen:

```swift
        menu.addItem(.separator())

        // OUT-03/D-08: Ausgabemodus-Häkchen — zeigt aktiven Modus und erlaubt Umschalten
        let currentMode = Defaults[.outputMode]

        let fieldItem = NSMenuItem(
            title: "Textfeld-Injektion",
            action: #selector(setOutputModeField),
            keyEquivalent: ""
        )
        fieldItem.target = self
        fieldItem.state = currentMode == .field ? .on : .off
        menu.addItem(fieldItem)

        let clipItem = NSMenuItem(
            title: "Clipboard",
            action: #selector(setOutputModeClipboard),
            keyEquivalent: ""
        )
        clipItem.target = self
        clipItem.state = currentMode == .clipboard ? .on : .off
        menu.addItem(clipItem)
```

Und die beiden @objc-Actions als neue Methoden hinzufügen (nach toggleLoginItem):

```swift
    @objc private func setOutputModeField() {
        Defaults[.outputMode] = .field
    }

    @objc private func setOutputModeClipboard() {
        Defaults[.outputMode] = .clipboard
    }
```
  </action>

  <verify>
    <automated>grep -n "TextOutputService.shared.output" /Users/mbieling/claude/voice/VoiceScribe/AppDelegate.swift && grep -n "AXIsProcessTrusted" /Users/mbieling/claude/voice/VoiceScribe/AppDelegate.swift && grep -n "toggleOutputMode" /Users/mbieling/claude/voice/VoiceScribe/AppDelegate.swift && grep -n "setOutputModeField\|setOutputModeClipboard" /Users/mbieling/claude/voice/VoiceScribe/AppDelegate.swift && grep -v "print(\"Transkription" /Users/mbieling/claude/voice/VoiceScribe/AppDelegate.swift | grep -c "Transkription" || echo "print-Stub entfernt" && xcodebuild build -scheme VoiceScribe -destination "platform=macOS" 2>&1 | tail -5</automated>
  </verify>

  <done>
    - AppDelegate.swift enthält TextOutputService.shared.output(text, mode: mode, axPermitted: axPermitted)
    - AppDelegate.swift enthält AXIsProcessTrusted() (mindestens 1x, bevorzugt in setupAudioController)
    - AppDelegate.swift enthält setupOutputModeHotkey() mit KeyboardShortcuts.onKeyUp(for: .toggleOutputMode)
    - AppDelegate.swift enthält setOutputModeField() und setOutputModeClipboard() @objc-Actions
    - AppDelegate.swift enthält Menü-Häkchen-Logik mit currentMode == .field ? .on : .off
    - Der print("Transkription: \(text)")-Stub ist entfernt
    - xcodebuild build exitiert mit 0
  </done>
</task>

<task type="auto">
  <name>Task 2: SettingsView — AX-Permission-Banner + OutputMode-Section</name>
  <files>VoiceScribe/SettingsView.swift</files>

  <read_first>
    - VoiceScribe/SettingsView.swift — vollständige Datei; micPermissionDenied-Banner als exaktes Pattern für axPermissionDenied-Banner
    - VoiceScribe/AppState.swift — axPermissionDenied Property (aus Plan 01)
    - VoiceScribe/Extensions/KeyboardShortcuts+Names.swift — toggleOutputMode Name (aus Plan 01)
  </read_first>

  <action>
Zwei Ergänzungen in VoiceScribe/SettingsView.swift:

### Ergänzung 1: @Default(.outputMode) Property

Nach den bestehenden @Default-Properties ergänzen:
```swift
    @Default(.outputMode) private var outputMode
```

### Ergänzung 2: Neue Section "Textausgabe" (nach der Stille-Erkennungs-Section)

Füge innerhalb des Form { } nach der "Stille-Erkennung"-Section eine neue Section ein:

```swift
            // --- Textausgabe-Sektion (OUT-01, OUT-02, OUT-03, D-08 bis D-12) ---
            Section("Textausgabe") {
                // D-11: Roter AX-Permission-Banner — nur sichtbar wenn axPermissionDenied == true
                // Analoges Pattern zum micPermissionDenied-Banner (Phase 2)
                if appState?.axPermissionDenied == true {
                    HStack(spacing: DesignTokens.Spacing.sm) {
                        Image(systemName: "hand.raised.slash.fill")
                            .foregroundStyle(.white)
                        VStack(alignment: .leading, spacing: 2) {
                            Text("Bedienungshilfen-Zugriff erforderlich")
                                .font(.system(size: 13, weight: .medium))
                                .foregroundStyle(.white)
                            Text("VoiceScribe benötigt Zugriff, um Text direkt einzufügen. Stattdessen wird Clipboard verwendet.")
                                .font(.system(size: 11))
                                .foregroundStyle(.white.opacity(0.9))
                        }
                        Spacer()
                        Button("Einstellungen öffnen") {
                            // D-11/RESEARCH.md Pattern 4: URL für Privacy → Accessibility
                            if let url = URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility") {
                                NSWorkspace.shared.open(url)
                            }
                        }
                        .buttonStyle(.bordered)
                    }
                    .padding(DesignTokens.Spacing.sm)
                    .background(Color(.systemRed))
                    .cornerRadius(8)
                    .accessibilityLabel("Bedienungshilfen-Zugriff verweigert. Öffne Einstellungen.")
                }

                // D-08: Ausgabemodus-Picker — Häkchen-analog zum Menü
                // D-07: Persistiert via Defaults.Key<OutputMode>
                Picker("Ausgabemodus", selection: $outputMode) {
                    Text("Textfeld-Injektion").tag(OutputMode.field)
                    Text("Clipboard").tag(OutputMode.clipboard)
                }
                .pickerStyle(.segmented)
                .accessibilityLabel("Ausgabemodus")

                Text("Textfeld: Text wird direkt an der Cursorposition eingefügt. Clipboard: Text wird kopiert, zum Einfügen ⌘V drücken.")
                    .font(.system(size: 11))
                    .foregroundStyle(.secondary)

                // D-09: toggleOutputMode-Hotkey konfigurierbar (⇧⌘V Standard)
                KeyboardShortcuts.Recorder("Modus-Wechsel-Hotkey", name: .toggleOutputMode)
                    .accessibilityLabel("Modus-Wechsel-Hotkey konfigurieren")
            }
```

### Ergänzung 3: Preview mit axPermissionDenied ergänzen

Preview "Settings mit Permission-Fehler" erweitern:
```swift
#Preview("Settings mit AX-Permission-Fehler") {
    let state = AppState()
    Task { @MainActor in
        state.axPermissionDenied = true
    }
    return SettingsView(appState: state)
        .frame(width: 450, height: 500)
}
```
  </action>

  <verify>
    <automated>grep -n "axPermissionDenied" /Users/mbieling/claude/voice/VoiceScribe/SettingsView.swift && grep -n "Privacy_Accessibility" /Users/mbieling/claude/voice/VoiceScribe/SettingsView.swift && grep -n "toggleOutputMode" /Users/mbieling/claude/voice/VoiceScribe/SettingsView.swift && grep -n "outputMode" /Users/mbieling/claude/voice/VoiceScribe/SettingsView.swift && xcodebuild build -scheme VoiceScribe -destination "platform=macOS" 2>&1 | tail -5</automated>
  </verify>

  <done>
    - SettingsView.swift enthält if appState?.axPermissionDenied == true { ... Banner ... }
    - SettingsView.swift enthält Privacy_Accessibility URL
    - SettingsView.swift enthält KeyboardShortcuts.Recorder(..., name: .toggleOutputMode)
    - SettingsView.swift enthält Picker("Ausgabemodus", selection: $outputMode)
    - xcodebuild build exitiert mit 0
    - Volle Test-Suite grün: xcodebuild test -scheme VoiceScribe exitiert mit 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hotkey-Callback → Defaults[.outputMode] | Hotkey-Event von KeyboardShortcuts; schreibt nur lokalen Defaults-Key |
| showMenu() → NSMenuItem.action | Menü-Actions sind @objc-Methoden; kein Input von außen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | Defaults[.outputMode] via Hotkey | accept | Lokale UserDefaults; User besitzt die Daten; kein sicherheitskritischer Effekt |
| T-04-08 | Denial of Service | AX-Check in setupAudioController | mitigate | AXIsProcessTrusted() ist ein einfacher Bool-Return, kein blocking call; kein Crash-Risiko |
| T-04-09 | Information Disclosure | NSWorkspace.shared.open() für Systemeinstellungen | accept | Öffnet nur Systemeinstellungen, keine sensitiven Daten übertragen |
</threat_model>

<verification>
Nach Plan 03 (vollständige Phase-4-Code-Verifikation):
- `grep "TextOutputService.shared.output" VoiceScribe/AppDelegate.swift` → Treffer
- `grep "AXIsProcessTrusted" VoiceScribe/AppDelegate.swift` → Treffer
- `grep "toggleOutputMode" VoiceScribe/AppDelegate.swift` → Treffer (setupOutputModeHotkey)
- `grep "axPermissionDenied" VoiceScribe/SettingsView.swift` → Treffer
- `grep "Privacy_Accessibility" VoiceScribe/SettingsView.swift` → Treffer
- `xcodebuild build -scheme VoiceScribe` → exit 0
- `xcodebuild test -scheme VoiceScribe` → alle Tests PASSED
</verification>

<success_criteria>
- print()-Stub ist entfernt, TextOutputService.shared.output() ruft korrekte Methode auf
- AXIsProcessTrusted() wird beim App-Start geprüft, Ergebnis in AppState.axPermissionDenied
- toggleOutputMode-Hotkey funktioniert und togglet Defaults[.outputMode]
- Menü zeigt Häkchen für aktiven Modus (.field oder .clipboard)
- SettingsView zeigt roten Banner wenn axPermissionDenied == true
- SettingsView hat konfigurierbaren KeyboardShortcuts.Recorder für toggleOutputMode
- Build und volle Test-Suite grün
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04-text-output/04-03-SUMMARY.md` erstellen.
</output>
