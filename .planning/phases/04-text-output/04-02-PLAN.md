---
phase: 04-text-output
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - VoiceScribe/TextOutput/TextOutputService.swift
  - VoiceScribeTests/TextOutputServiceTests.swift
autonomous: true
requirements:
  - OUT-01
  - OUT-02

must_haves:
  truths:
    - "TextOutputService ist @MainActor final class"
    - "AX-Injektion fügt Text am Cursor ein (Unicode-Scalar-korrekt, per D-01/D-03)"
    - "2040-Zeichen-Guard ist vorhanden: newText.count > 2000 → Clipboard-Fallback"
    - "Clipboard-Pfad ruft clearContents() dann setString() auf (per OUT-02)"
    - "Bei mode == .clipboard oder axPermitted == false → Clipboard, kein AX-Versuch (per D-04)"
    - "Bei nil focusedElement oder AXError → stille Rückkehr zu .idle ohne Crash (per D-05)"
    - "Unit-Tests laufen grün ohne echte AX-Permission (via Protocol/Mock)"
  artifacts:
    - path: "VoiceScribe/TextOutput/TextOutputService.swift"
      provides: "@MainActor TextOutputService mit output(), injectViaAX(), writeToClipboard()"
      exports: ["TextOutputServiceProtocol", "TextOutputService"]
    - path: "VoiceScribeTests/TextOutputServiceTests.swift"
      provides: "Unit-Tests für String-Insert-Logik, 2040-Guard, Modus-Switching"
      contains: "TextOutputServiceTests"
  key_links:
    - from: "TextOutputService.output()"
      to: "AXUIElementSetAttributeValue"
      via: "injectViaAX()"
      pattern: "AXUIElementSetAttributeValue"
    - from: "TextOutputService.output()"
      to: "NSPasteboard.general"
      via: "writeToClipboard()"
      pattern: "NSPasteboard.general"
---

<objective>
Implementiert TextOutputService als @MainActor-Klasse mit AX-Injektion und Clipboard-Fallback,
plus vollständige Unit-Tests via Protocol/Mock ohne echte AX-Permission.

Purpose: Kern der Text-Ausgabe — ersetzt in Plan 03 den print()-Stub. Tests validieren die
gesamte String-Manipulations-Logik, den 2040-Zeichen-Guard und das Modus-Switching.
Output: TextOutputService.swift (neue Datei), TextOutputServiceTests.swift (neue Datei), Build grün, Tests grün.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/PROJECT.md
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/STATE.md

<!-- Parallel zu Plan 01 — OutputMode-Enum wird durch Plan 01 in Defaults+Keys.swift angelegt.
     Dieser Plan hängt NICHT von Plan 01 ab (wave: 1), aber der Executor muss wissen:
     OutputMode existiert nach Plan 01 in VoiceScribe/Extensions/Defaults+Keys.swift.
     Falls Plan 01 noch nicht fertig ist, OutputMode lokal vorwärts-deklarieren und nach
     Plan-01-Merge die lokale Deklaration entfernen. -->

<interfaces>
<!-- AX-API-Aufrufsequenz aus RESEARCH.md — EXAKT so implementieren -->
<!--
KRITISCHE PATTERNS (alle aus 04-RESEARCH.md verifiziert):

1. @MainActor: ALLE AXUIElement*-Calls müssen auf @MainActor laufen
   → TextOutputService: @MainActor final class

2. AXValue → CFRange (NICHT direkt casten):
   var range = CFRange()
   AXValueGetValue(axVal, .cfRange, &range)

3. Unicode-Scalar-Insert (nicht String.Index):
   var scalars = Array(existing.unicodeScalars)
   scalars.replaceSubrange(loc..<end, with: Array(newText.unicodeScalars))
   let composed = String(String.UnicodeScalarView(scalars))

4. 2040-Zeichen-Guard (PFLICHT, EXC_BAD_ACCESS-Schutz):
   guard composed.count <= 2000 else {
       writeToClipboard(newText)
       return
   }

5. AXUIElementCreateSystemWide():
   let sysWide = AXUIElementCreateSystemWide()
   var focusedRef: CFTypeRef?
   guard AXUIElementCopyAttributeValue(sysWide, kAXFocusedUIElementAttribute as CFString, &focusedRef) == .success,
         let focusedRef else { return }  // D-05: stille Rückkehr
   let focused = focusedRef as! AXUIElement

6. Cursor-Position schreiben (best-effort, Fehler ignorieren):
   var newRange = CFRange(location: safeLoc + insertScalars.count, length: 0)
   if let axRange = AXValueCreate(.cfRange, &newRange) {
       AXUIElementSetAttributeValue(focused, kAXSelectedTextRangeAttribute as CFString, axRange)
   }

7. Clipboard (immer clearContents() DANN setString()):
   NSPasteboard.general.clearContents()
   NSPasteboard.general.setString(text, forType: .string)

8. Testbarkeit via Protokoll:
   protocol TextInjectorProtocol {
       func inject(_ text: String) -> Bool
   }
   → MockTextInjector für Unit-Tests
-->

Aus VoiceScribe/AppState.swift:
```swift
// AppState ist @MainActor @Observable — TextOutputService.output() wird
// von AppDelegate aus @MainActor-Kontext aufgerufen.
var axPermissionDenied: Bool = false  // nach Plan 01 vorhanden
```

Aus VoiceScribe/Extensions/Defaults+Keys.swift (nach Plan 01):
```swift
enum OutputMode: String, Defaults.Serializable {
    case field
    case clipboard
}
extension Defaults.Keys {
    static let outputMode = Key<OutputMode>("outputMode", default: .field)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TextOutputServiceProtocol + TextOutputService (AX + Clipboard)</name>
  <files>VoiceScribe/TextOutput/TextOutputService.swift</files>

  <read_first>
    - /Users/mbieling/claude/voice/.planning/phases/04-text-output/04-RESEARCH.md — Pattern 1 (vollständige AX-Implementierung), Anti-Patterns, Don't-Hand-Roll-Abschnitt
    - VoiceScribe/AppState.swift — axPermissionDenied-Property (nach Plan 01 vorhanden)
  </read_first>

  <behavior>
    - output("", mode: .field, axPermitted: true) → kein AX-Aufruf (leerer Text frühzeitig ignoriert)
    - output("hello", mode: .clipboard, axPermitted: true) → Clipboard (mode .clipboard übersteuert AX-Permission)
    - output("hello", mode: .field, axPermitted: false) → Clipboard (keine Permission = Clipboard per D-04)
    - output("hello", mode: .field, axPermitted: true) → AX-Injektion versucht
    - AX-Injektion mit newText.count > 2000 → Clipboard-Fallback statt EXC_BAD_ACCESS
    - focusedElement nil → stille Rückkehr, kein Crash (D-05)
    - AXError bei SetAttributeValue → stille Rückkehr (D-05)
    - Cursor-Schreiben via kAXSelectedTextRangeAttribute schlägt fehl → ignorieren (best-effort, Pitfall 4)
  </behavior>

  <action>
Erstelle das Verzeichnis VoiceScribe/TextOutput/ (neue Gruppe) und lege an:
VoiceScribe/TextOutput/TextOutputService.swift

```swift
// VoiceScribe/TextOutput/TextOutputService.swift
// Zweck: Text-Ausgabe nach Transkription — AX-Injektion (D-01) oder Clipboard (D-04).
// Implementiert: OUT-01 (AX-Injektion an Cursor-Position), OUT-02 (Clipboard-Fallback).
// Quellen: 04-RESEARCH.md Pattern 1-3; Apple Developer Forums thread 658733 (2040-Limit).

import AppKit
import ApplicationServices

// MARK: - Protocol (für Unit-Tests via Mock)

/// Testbares Interface für Text-Ausgabe.
/// Echter Code: TextOutputService.shared
/// Tests: MockTextOutputService
protocol TextOutputServiceProtocol: AnyObject {
    /// Gibt text aus — entweder via AX-Injektion oder Clipboard.
    /// - Parameters:
    ///   - text: Der auszugebende Text (leer → wird ignoriert).
    ///   - mode: Ausgabemodus aus Defaults (D-07).
    ///   - axPermitted: Ergebnis von AXIsProcessTrusted() (D-10).
    @MainActor func output(_ text: String, mode: OutputMode, axPermitted: Bool)
}

// MARK: - Service

/// Text-Ausgabe-Service. @MainActor: alle AX-Calls müssen auf dem Main Thread laufen.
/// Pitfall: AXUIElement*-Funktionen sind nicht thread-safe (RESEARCH.md Pitfall 2).
@MainActor
final class TextOutputService: TextOutputServiceProtocol {

    static let shared = TextOutputService()
    private init() {}

    // MARK: - Public Entry Point

    func output(_ text: String, mode: OutputMode, axPermitted: Bool) {
        guard !text.isEmpty else { return }

        // D-04: Kein AX-Versuch wenn Permission fehlt
        // D-07: .clipboard-Modus übersteuert immer AX-Versuch
        if mode == .clipboard || !axPermitted {
            writeToClipboard(text)
            return
        }

        injectViaAX(text)
    }

    // MARK: - AX-Injektion (OUT-01, D-01, D-03)

    private func injectViaAX(_ text: String) {
        // Schritt 1: System-weites fokussiertes Element holen
        let sysWide = AXUIElementCreateSystemWide()
        var focusedRef: CFTypeRef?
        guard AXUIElementCopyAttributeValue(
            sysWide,
            kAXFocusedUIElementAttribute as CFString,
            &focusedRef
        ) == .success, let focusedRef else {
            // D-05: Kein fokussiertes Element — stille Rückkehr (Pitfall 3)
            return
        }
        let focused = focusedRef as! AXUIElement  // swiftlint:disable:this force_cast

        // Schritt 2: Existierenden Text lesen
        var valueRef: CFTypeRef?
        guard AXUIElementCopyAttributeValue(
            focused,
            kAXValueAttribute as CFString,
            &valueRef
        ) == .success, let existingCF = valueRef,
              let existing = existingCF as? String else {
            // D-05: kein lesbarer kAXValueAttribute — stille Rückkehr
            return
        }

        // Schritt 3: Cursor-Range lesen (Fallback: Ende des Textes)
        var rangeRef: CFTypeRef?
        var cursorRange = CFRange(location: existing.unicodeScalars.count, length: 0)
        if AXUIElementCopyAttributeValue(
            focused,
            kAXSelectedTextRangeAttribute as CFString,
            &rangeRef
        ) == .success, let rv = rangeRef {
            // KRITISCH: CFRange ist ein Struct, kein Objekt — nicht via `as?` casten.
            // Korrekte Methode: AXValueGetValue (RESEARCH.md Pitfall 5 / Don't-Hand-Roll)
            let axVal = rv as! AXValue  // swiftlint:disable:this force_cast
            AXValueGetValue(axVal, .cfRange, &cursorRange)
        }

        // Schritt 4: Text am Cursor einsetzen (Unicode-Scalar-korrekt, D-03)
        // AX-Ranges sind Unicode-Scalar-Indizes, nicht String.Index (RESEARCH.md Don't-Hand-Roll)
        var scalars = Array(existing.unicodeScalars)
        let insertScalars = Array(text.unicodeScalars)
        let safeLoc = min(cursorRange.location, scalars.count)
        let safeEnd = min(safeLoc + max(cursorRange.length, 0), scalars.count)
        scalars.replaceSubrange(safeLoc..<safeEnd, with: insertScalars)
        let composed = String(String.UnicodeScalarView(scalars))

        // Schritt 5: 2040-Zeichen-Guard (PFLICHT — EXC_BAD_ACCESS-Schutz)
        // Apple Developer Forums thread 658733: kAXValueAttribute crasht bei >~2040 Zeichen
        // ohne AXError-Rückmeldung. Guard auf 2000 für Puffer.
        guard composed.count <= 2000 else {
            writeToClipboard(text)
            return
        }

        // Schritt 6: Zusammengesetzten Text schreiben
        guard AXUIElementSetAttributeValue(
            focused,
            kAXValueAttribute as CFString,
            composed as CFTypeRef
        ) == .success else {
            // D-05: AX-Fehler bei vorhandener Permission — stille Rückkehr
            return
        }

        // Schritt 7: Cursor hinter eingefügten Text setzen (best-effort, Pitfall 4)
        // Manche Apps exponieren kAXSelectedTextRangeAttribute nicht als schreibbar → ignorieren
        var newCursorRange = CFRange(location: safeLoc + insertScalars.count, length: 0)
        if let axRange = AXValueCreate(.cfRange, &newCursorRange) {
            AXUIElementSetAttributeValue(
                focused,
                kAXSelectedTextRangeAttribute as CFString,
                axRange
            )
            // Fehler von kAXSelectedTextRangeAttribute-Set werden ignoriert (Pitfall 4)
        }
    }

    // MARK: - Clipboard (OUT-02)

    func writeToClipboard(_ text: String) {
        // WICHTIG: clearContents() muss VOR setString() kommen.
        // Ohne clearContents() wird die Pasteboard-Version-ID nicht inkrementiert
        // und andere Apps sehen keine Änderung (RESEARCH.md Don't-Hand-Roll).
        NSPasteboard.general.clearContents()
        NSPasteboard.general.setString(text, forType: .string)
    }
}
```

HINWEIS: Das Verzeichnis VoiceScribe/TextOutput/ muss als neue Xcode-Group in der Projektstruktur
erscheinen. Da kein pbxproj-Generator vorhanden ist, die Datei direkt anlegen — der Executor des
nächsten Plans (04-04) ergänzt den pbxproj-Eintrag falls nötig, oder xcodebuild erkennt die Datei
durch SWIFT_INCLUDE_PATHS. Besser: Datei direkt im VoiceScribe/-Ordner ablegen wenn Gruppenanlage
Probleme verursacht: VoiceScribe/TextOutputService.swift (gleicher Inhalt, keine Group-Änderung nötig).

WICHTIGER PFAD-HINWEIS: Das Projekt wurde in Phase 1 mit manuell erstelltem pbxproj aufgebaut.
Neue Swift-Dateien müssen in der .pbxproj-Datei unter PBXBuildFile + PBXFileReference + PBXGroup
eingetragen werden, damit xcodebuild sie kompiliert. Lege die Datei daher im bestehenden
VoiceScribe/-Ordner ab (NICHT in einem neuen Unterordner) und trage sie in VoiceScribe.xcodeproj/project.pbxproj ein:

1. Neue UUIDs generieren (Format: 24 Hex-Zeichen, z.B. mit `uuidgen | tr -d '-' | cut -c1-24 | tr '[:lower:]' '[:upper:]'`)
2. PBXFileReference-Eintrag: `{UUID} /* TextOutputService.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = TextOutputService.swift; sourceTree = "<group>"; };`
3. PBXBuildFile-Eintrag: `{UUID2} /* TextOutputService.swift in Sources */ = {isa = PBXBuildFile; fileRef = {UUID}; };`
4. PBXGroup: Zur Gruppe "VoiceScribe" (mainGroup) die fileRef-UUID hinzufügen
5. PBXSourcesBuildPhase "Sources": die BuildFile-UUID hinzufügen
  </action>

  <verify>
    <automated>test -f /Users/mbieling/claude/voice/VoiceScribe/TextOutputService.swift && grep -n "TextOutputServiceProtocol" /Users/mbieling/claude/voice/VoiceScribe/TextOutputService.swift && grep -n "2000" /Users/mbieling/claude/voice/VoiceScribe/TextOutputService.swift && grep -n "AXUIElementCreateSystemWide" /Users/mbieling/claude/voice/VoiceScribe/TextOutputService.swift && xcodebuild build -scheme VoiceScribe -destination "platform=macOS" 2>&1 | tail -5</automated>
  </verify>

  <done>
    - TextOutputService.swift existiert (in VoiceScribe/ Ordner, eingetragen in pbxproj)
    - Enthält @MainActor final class TextOutputService: TextOutputServiceProtocol
    - Enthält func output(_ text: String, mode: OutputMode, axPermitted: Bool)
    - Enthält 2040-Zeichen-Guard: guard composed.count &lt;= 2000 else { writeToClipboard(text); return }
    - Enthält AXValueGetValue(axVal, .cfRange, &cursorRange) (kein direktes CFRange-Casting)
    - Enthält Unicode-Scalar-replaceSubrange (kein String.Index)
    - xcodebuild build exitiert mit 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-Tests für TextOutputService via MockTextInjector</name>
  <files>VoiceScribeTests/TextOutputServiceTests.swift</files>

  <read_first>
    - VoiceScribe/TextOutputService.swift — soeben erstellt; Protokoll-Interface + Methoden-Signaturen
    - VoiceScribeTests/ (Verzeichnis prüfen) — bestehende Test-Dateien als Vorlage für Import-Struktur und Test-Framework (Swift Testing, nicht XCTest)
  </read_first>

  <behavior>
    - Leerer String → output() tut nichts (Mock wird nicht aufgerufen)
    - mode == .clipboard → schreibt auf Clipboard (NSPasteboard testbar ohne AX)
    - axPermitted == false → schreibt auf Clipboard (nicht AX)
    - mode == .field, axPermitted == true → versucht AX-Injektion (Mock)
    - String-Insert-Logik: "hello world", cursor bei loc=5 len=0, insert " beautiful" → "hello beautiful world"
    - String-Insert mit Emoji: "hi 🎉 end", cursor bei loc=4 len=0, insert "!" → "hi 🎉! end" (Unicode-Scalar-korrekt)
    - 2040-Zeichen-Guard: existing String mit 1990 Zeichen + insert 50 Zeichen = 2040 > 2000 → Clipboard statt AX
    - Clipboard-Pfad: clearContents() wird VOR setString() aufgerufen
  </behavior>

  <action>
Erstelle VoiceScribeTests/TextOutputServiceTests.swift.

Teste die String-Insert-Logik isoliert (pure function extrahieren oder via internen Test-Hook).
Für AX-Calls: MockTextOutputService implementiert TextOutputServiceProtocol und zeichnet
Aufrufe auf ohne echte AX-Permission zu benötigen.

Für die Clipboard-Tests: NSPasteboard.general ist in Unit-Tests direkt nutzbar
(kein Berechtigungs-Dialog, kein AX benötigt).

```swift
// VoiceScribeTests/TextOutputServiceTests.swift
// Zweck: Unit-Tests fuer TextOutputService ohne echte AX-Permission.
// Testabdeckung: OUT-01 (Modus-Routing, String-Insert-Logik, 2040-Guard), OUT-02 (Clipboard), OUT-03 (Modus).
// Framework: Swift Testing (import Testing) — konsistent mit Phase-3-Tests.

import Testing
import AppKit
@testable import VoiceScribe

// MARK: - Mock

/// Testdoppel fuer AX-Injektion — zeichnet Aufrufe auf ohne echte AX-Permission.
@MainActor
final class MockTextOutputService: TextOutputServiceProtocol {
    var outputCalls: [(text: String, mode: OutputMode, axPermitted: Bool)] = []
    var lastClipboardText: String? = nil
    var shouldFallbackToClipboard = false

    func output(_ text: String, mode: OutputMode, axPermitted: Bool) {
        outputCalls.append((text, mode, axPermitted))
        // Clipboard-Verhalten simulieren
        if mode == .clipboard || !axPermitted {
            NSPasteboard.general.clearContents()
            NSPasteboard.general.setString(text, forType: .string)
            lastClipboardText = text
        }
    }
}

// MARK: - String-Insert-Logik (pure function, testbar ohne AX)

/// Testet die Unicode-Scalar-korrekte String-Insertion isoliert.
/// Diese Logik lebt in TextOutputService.injectViaAX() —
/// hier via Hilfsfunktion direkt testbar gemacht.
@MainActor
func insertText(_ newText: String, into existing: String, at loc: Int, replacing len: Int) -> String {
    var scalars = Array(existing.unicodeScalars)
    let insertScalars = Array(newText.unicodeScalars)
    let safeLoc = min(loc, scalars.count)
    let safeEnd = min(safeLoc + max(len, 0), scalars.count)
    scalars.replaceSubrange(safeLoc..<safeEnd, with: insertScalars)
    return String(String.UnicodeScalarView(scalars))
}

// MARK: - Tests

@Suite("TextOutputService — Modus-Routing")
struct TextOutputServiceModusTests {

    @Test("Leerer Text wird ignoriert")
    @MainActor func emptyTextIsIgnored() {
        let mock = MockTextOutputService()
        mock.output("", mode: .field, axPermitted: true)
        #expect(mock.outputCalls.isEmpty)
    }

    @Test("mode .clipboard → Clipboard-Pfad")
    @MainActor func clipboardModeUsesClipboard() {
        let mock = MockTextOutputService()
        mock.output("hallo", mode: .clipboard, axPermitted: true)
        #expect(mock.outputCalls.count == 1)
        #expect(mock.outputCalls[0].mode == .clipboard)
        #expect(NSPasteboard.general.string(forType: .string) == "hallo")
    }

    @Test("axPermitted false → Clipboard-Pfad (D-04)")
    @MainActor func noPermissionUsesClipboard() {
        let mock = MockTextOutputService()
        mock.output("test", mode: .field, axPermitted: false)
        #expect(mock.outputCalls.count == 1)
        #expect(mock.outputCalls[0].axPermitted == false)
        #expect(NSPasteboard.general.string(forType: .string) == "test")
    }
}

@Suite("TextOutputService — String-Insert-Logik")
struct TextOutputServiceStringInsertTests {

    @Test("Einfügen mitten im Text am Cursor")
    @MainActor func insertMidString() {
        let result = insertText(" beautiful", into: "hello world", at: 5, replacing: 0)
        #expect(result == "hello beautiful world")
    }

    @Test("Einfügen am Anfang (loc=0)")
    @MainActor func insertAtStart() {
        let result = insertText("START ", into: "end", at: 0, replacing: 0)
        #expect(result == "START end")
    }

    @Test("Einfügen am Ende (loc = string.count)")
    @MainActor func insertAtEnd() {
        let result = insertText(" end", into: "start", at: 5, replacing: 0)
        #expect(result == "start end")
    }

    @Test("Ersetzen einer Selektion (len > 0)")
    @MainActor func replaceSelection() {
        let result = insertText("neue", into: "altes Wort", at: 0, replacing: 5)
        #expect(result == "neue Wort")
    }

    @Test("Unicode-Scalar-korrekt: Emoji zählt als 1 Scalar")
    @MainActor func emojiInsertCorrect() {
        // "hi 🎉 end" — 🎉 ist U+1F389, 1 Unicode-Scalar, aber 2 UTF-16-Code-Units
        // loc=4 (nach "hi 🎉") soll "!" einfügen → "hi 🎉! end"
        // Unicode-Scalar-Offsets: h=0, i=1, ' '=2, 🎉=3, ' '=4
        let result = insertText("!", into: "hi 🎉 end", at: 4, replacing: 0)
        #expect(result == "hi 🎉! end")
    }

    @Test("loc größer als Textlänge → safe Bounds (kein Crash)")
    @MainActor func safeBoundsWhenLocExceedsLength() {
        let result = insertText("x", into: "abc", at: 99, replacing: 0)
        #expect(result == "abcx")
    }
}

@Suite("TextOutputService — 2040-Zeichen-Guard")
struct TextOutputService2040GuardTests {

    @Test("Composed-Text > 2000 Zeichen → Clipboard-Fallback")
    @MainActor func longTextFallsBackToClipboard() {
        // existing: 1990 Zeichen, insert: 50 Zeichen → composed: 2040 > 2000
        let existing = String(repeating: "a", count: 1990)
        let insert = String(repeating: "b", count: 50)
        // Der Guard schützt vor EXC_BAD_ACCESS (Apple Developer Forums thread 658733)
        // Teste den Guard via direkte composed-Berechnung
        let composed = insertText(insert, into: existing, at: existing.unicodeScalars.count, replacing: 0)
        #expect(composed.count > 2000)
        // Echte TextOutputService.writeToClipboard() ist via shared testbar:
        // Bei composed.count > 2000 wird writeToClipboard(insert) aufgerufen
        // (nicht writeToClipboard(composed) — nur der eingefügte Text aufs Clipboard)
        TextOutputService.shared.writeToClipboard(insert)
        #expect(NSPasteboard.general.string(forType: .string) == insert)
    }

    @Test("Composed-Text = 2000 Zeichen → kein Fallback (Grenzwert)")
    @MainActor func exactBoundaryNoFallback() {
        let existing = String(repeating: "a", count: 1995)
        let insert = String(repeating: "b", count: 5)
        let composed = insertText(insert, into: existing, at: existing.unicodeScalars.count, replacing: 0)
        #expect(composed.count == 2000)
        // composed.count <= 2000 → kein Fallback auf Clipboard (Guard: guard composed.count <= 2000)
        #expect(composed.count <= 2000)
    }
}

@Suite("TextOutputService — Clipboard-Schreiben")
struct TextOutputServiceClipboardTests {

    @Test("writeToClipboard schreibt Text auf Pasteboard")
    @MainActor func writeToClipboard() {
        let service = TextOutputService.shared
        service.writeToClipboard("testinhalt")
        #expect(NSPasteboard.general.string(forType: .string) == "testinhalt")
    }

    @Test("writeToClipboard überschreibt vorherigen Inhalt")
    @MainActor func writeToClipboardOverwritesPrevious() {
        let service = TextOutputService.shared
        service.writeToClipboard("erster Inhalt")
        service.writeToClipboard("zweiter Inhalt")
        #expect(NSPasteboard.general.string(forType: .string) == "zweiter Inhalt")
    }
}
```

HINWEIS zur insertText-Hilfsfunktion: Diese Funktion repliziert die Logik aus
TextOutputService.injectViaAX() für die Testbarkeit. Falls die Implementierung diese Logik
als interne static func oder als Extension auf String bereitstellt, kann der Test direkt
darauf zugreifen via @testable import VoiceScribe. Falls nicht, bleibt die lokale Kopie
in der Test-Datei — funktional identisch.

Die Testdatei muss ebenfalls in der .pbxproj unter dem VoiceScribeTests-Target eingetragen werden:
- PBXFileReference in der Tests-Gruppe
- PBXBuildFile im Sources-Build-Phase des VoiceScribeTests-Targets
  </action>

  <verify>
    <automated>test -f /Users/mbieling/claude/voice/VoiceScribeTests/TextOutputServiceTests.swift && xcodebuild test -scheme VoiceScribe -destination "platform=macOS,arch=arm64" -only-testing "VoiceScribeTests/TextOutputServiceTests" 2>&1 | grep -E "PASSED|FAILED|error:" | tail -20</automated>
  </verify>

  <done>
    - TextOutputServiceTests.swift existiert und ist im Test-Target eingetragen
    - Alle @Test-Annotationen kompilieren (Swift Testing Framework)
    - Alle Tests PASSED (kein AX-Permission erforderlich)
    - xcodebuild test exitiert mit 0 für VoiceScribeTests/TextOutputServiceTests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TranscriptionService → TextOutputService | text: String von lokalem Modell; kein User-Input, kein Netzwerk-Input in Phase 4 |
| TextOutputService → AXUIElement (foreign app) | Text wird in fremde App geschrieben; unidirektional |
| TextOutputService → NSPasteboard | Text landet im shared Pasteboard; andere Apps können lesen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | AXUIElementSetAttributeValue | mitigate | `guard composed.count <= 2000 else { writeToClipboard(text); return }` — verhindert EXC_BAD_ACCESS |
| T-04-04 | Information Disclosure | NSPasteboard.general | accept | Clipboard ist shared auf macOS; akzeptiertes Risiko für lokale Diktat-App; Text kommt von lokalem Modell |
| T-04-05 | Spoofing | AXIsProcessTrusted() | accept | OS-kontrolliert; VoiceScribe kann Permission nicht fälschen; Nutzer muss explizit in Systemeinstellungen zustimmen |
| T-04-06 | Elevation of Privilege | AX schreibt in beliebige App | accept | AX-Permission ist User-granted; App schreibt nur auf explizite Nutzeroption (Hotkey); keine autonome Injection |
</threat_model>

<verification>
Nach Plan 02:
- `test -f VoiceScribe/TextOutputService.swift` → true
- `grep "TextOutputServiceProtocol" VoiceScribe/TextOutputService.swift` → Treffer
- `grep "guard composed.count <= 2000" VoiceScribe/TextOutputService.swift` → Treffer
- `grep "AXValueGetValue" VoiceScribe/TextOutputService.swift` → Treffer (kein direktes CFRange-Casting)
- `grep "replaceSubrange" VoiceScribe/TextOutputService.swift` → Treffer (unicodeScalars)
- `test -f VoiceScribeTests/TextOutputServiceTests.swift` → true
- `xcodebuild test -only-testing VoiceScribeTests/TextOutputServiceTests` → Tests PASSED
</verification>

<success_criteria>
- TextOutputService.swift kompiliert als @MainActor final class
- 2040-Zeichen-Guard explizit vorhanden (kein stummes EXC_BAD_ACCESS möglich)
- AXValueGetValue für CFRange (kein direktes Casting)
- Unicode-Scalar-replaceSubrange (kein String.Index-Offset)
- Unit-Tests grün ohne AX-Permission
- Build und Tests grün
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04-text-output/04-02-SUMMARY.md` erstellen.
</output>
