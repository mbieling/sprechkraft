---
phase: 05-llm-prompt-profiles
plan: 03
type: execute
wave: 1
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - VoiceScribe/Models/PromptProfile.swift
  - VoiceScribe/Extensions/Defaults+Keys.swift
  - VoiceScribe/Extensions/KeyboardShortcuts+Names.swift
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04

must_haves:
  truths:
    - "PromptProfile.swift existiert mit allen 6 Struct-Feldern"
    - "Defaults.Keys.profiles liefert [PromptProfile] mit Default-Profil 'Rohe Transkription'"
    - "KeyboardShortcuts.Name.profile(UUID()) liefert stabile dynamische Namen"
    - "PromptProfileTests GREEN nach Wave 1"
  artifacts:
    - path: "VoiceScribe/Models/PromptProfile.swift"
      provides: "Codable & Defaults.Serializable & Identifiable Struct"
      contains: "struct PromptProfile: Codable, Defaults.Serializable, Identifiable"
      exports: ["PromptProfile", "PromptProfile.defaultProfile"]
    - path: "VoiceScribe/Extensions/Defaults+Keys.swift"
      provides: "profiles Key für UserDefaults-Persistenz"
      contains: "static let profiles = Key<[PromptProfile]>"
    - path: "VoiceScribe/Extensions/KeyboardShortcuts+Names.swift"
      provides: "Dynamische Profil-Hotkey-Namen via UUID"
      contains: "static func profile(_ id: UUID) -> Self"
  key_links:
    - from: "VoiceScribe/Extensions/Defaults+Keys.swift"
      to: "VoiceScribe/Models/PromptProfile.swift"
      via: "Key<[PromptProfile]> default: [PromptProfile.defaultProfile]"
      pattern: "PromptProfile.defaultProfile"
    - from: "VoiceScribe/Extensions/KeyboardShortcuts+Names.swift"
      to: "VoiceScribe/Models/PromptProfile.swift"
      via: "profile(_ id: UUID) -> Self"
      pattern: "profile-\\(id.uuidString)"
---

<objective>
Wave 1 Plan A: Datenmodell + Persistenz + Hotkey-Namen.

Erstellt die drei statischen Fundament-Dateien für Phase 5. Kein UI, kein Networking, kein AppDelegate-Wiring. Reine Daten- und Konfigurations-Schicht.

Purpose: PromptProfileTests (Plan 01) grün machen und den Typ bereitstellen, von dem alle anderen Wave-1+-Pläne abhängen.
Output: PromptProfile.swift, Defaults+Keys.swift (erweitert), KeyboardShortcuts+Names.swift (erweitert).
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.planning/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-01-PLAN.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-RESEARCH.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-PATTERNS.md

<interfaces>
<!-- Bestehende Dateien, die erweitert werden -->

VoiceScribe/Extensions/Defaults+Keys.swift (vollständiger Inhalt):
```swift
import Defaults

enum OutputMode: String, Defaults.Serializable {
    case field
    case clipboard
}

extension Defaults.Keys {
    static let silenceDuration = Key<Double>("silenceDuration", default: 1.5)
    static let selectedMicUID = Key<String?>("selectedMicUID", default: nil)
    static let outputMode = Key<OutputMode>("outputMode", default: .field)
}
```

VoiceScribe/Extensions/KeyboardShortcuts+Names.swift (vollständiger Inhalt):
```swift
import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    static let toggleRecording = Self(
        "toggleRecording",
        default: .init(.r, modifiers: [.option, .command])
    )
    static let toggleOutputMode = Self(
        "toggleOutputMode",
        default: .init(.v, modifiers: [.shift, .command])
    )
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PromptProfile struct (PROF-01, PROF-02, PROF-03, PROF-04)</name>
  <files>VoiceScribe/Models/PromptProfile.swift</files>
  <read_first>
    - .planning/phases/05-llm-prompt-profiles/05-RESEARCH.md (Zeilen 501-525: vollständige Struct-Vorlage)
    - .planning/phases/05-llm-prompt-profiles/05-PATTERNS.md (Zeilen 28-81: PromptProfile-Abschnitt)
    - VoiceScribeTests/PromptProfileTests.swift (was die Tests von der Struct erwarten)
    - VoiceScribe/Extensions/Defaults+Keys.swift (Import-Stil, OutputMode als Codable-Analog)
  </read_first>
  <behavior>
    Tests die grün werden müssen (aus PromptProfileTests.swift):
    - testDefaultProfileShape: profile.name == "Rohe Transkription", isLLMEnabled == false, isDefault == true, prompt == ""
    - testCodableRoundTrip: JSONEncoder().encode(profile) → JSONDecoder().decode ergibt identische Werte
    - testLLMToggleUnabhaengigVonDefault: isLLMEnabled und isDefault sind unabhängige Felder
  </behavior>
  <action>
    Neue Datei `VoiceScribe/Models/PromptProfile.swift` erstellen.

    Falls das Verzeichnis VoiceScribe/Models/ noch nicht existiert, muss es als Ordner angelegt werden
    (Xcode-Projektdatei wird in einem späteren Plan nicht manuell geändert — Models-Ordner bereits
    in RESEARCH.md Empfehlung vorgesehen).

    Vollständiger Datei-Inhalt:
    ```swift
    // VoiceScribe/Models/PromptProfile.swift
    // Zweck: Datenmodell fuer Prompt-Profile (PROF-01 bis PROF-04).
    // D-04: Persistenz via Defaults.Serializable (Codable Array in UserDefaults).
    // D-05: defaultProfile liefert das initiale "Rohe Transkription"-Profil.
    // Kein Hotkey-Feld in der Struct — KeyboardShortcuts speichert Bindings selbst
    // unter dem Key "profile-{id.uuidString}" in UserDefaults (RESEARCH.md Pattern 2).

    import Foundation
    import Defaults

    struct PromptProfile: Codable, Defaults.Serializable, Identifiable {
        var id: UUID
        var name: String
        var prompt: String
        var isLLMEnabled: Bool
        var isThinkingEnabled: Bool
        var isDefault: Bool

        /// D-05: Initiales Default-Profil beim ersten App-Start.
        /// Name "Rohe Transkription": LLM deaktiviert, kein Prompt, als Standard markiert.
        /// Wird als Default-Wert fuer Defaults.Keys.profiles verwendet.
        static var defaultProfile: PromptProfile {
            PromptProfile(
                id: UUID(),
                name: "Rohe Transkription",
                prompt: "",
                isLLMEnabled: false,
                isThinkingEnabled: false,
                isDefault: true
            )
        }
    }
    ```

    WICHTIG: `static var defaultProfile` (nicht `let`) — UUID() muss bei jedem Aufruf
    stabil sein in Tests. Da der Default-Wert von Defaults.Keys.profiles einmalig gelesen
    wird, ist `var` korrekt (UUID wird beim ersten Zugriff fix).

    Die Struct benötigt KEINE explizite `encode(to:)` Implementierung — alle Felder sind
    primitiv (UUID, String, Bool) und von JSONEncoder automatisch enkodierbar.
    `Defaults.Serializable` wird automatisch durch `Codable`-Konformanz gewährt.
  </action>
  <verify>
    <automated>xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' -only-testing:VoiceScribeTests/PromptProfileTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - VoiceScribe/Models/PromptProfile.swift existiert
    - grep "struct PromptProfile: Codable, Defaults.Serializable, Identifiable" VoiceScribe/Models/PromptProfile.swift liefert 1 Treffer
    - grep "var id: UUID" VoiceScribe/Models/PromptProfile.swift liefert 1 Treffer
    - grep "var isLLMEnabled: Bool" VoiceScribe/Models/PromptProfile.swift liefert 1 Treffer
    - grep "var isThinkingEnabled: Bool" VoiceScribe/Models/PromptProfile.swift liefert 1 Treffer
    - grep "var isDefault: Bool" VoiceScribe/Models/PromptProfile.swift liefert 1 Treffer
    - grep "Rohe Transkription" VoiceScribe/Models/PromptProfile.swift liefert 1 Treffer
    - grep "static var defaultProfile" VoiceScribe/Models/PromptProfile.swift liefert 1 Treffer
    - xcodebuild test -only-testing:VoiceScribeTests/PromptProfileTests 2>&1 | grep "Test Suite.*passed" — nach Task 2+3 vollständig
  </acceptance_criteria>
  <done>PromptProfile.swift kompiliert. PromptProfileTests grün nach Task 2 und 3.</done>
</task>

<task type="auto">
  <name>Task 2: Defaults+Keys erweitern (profiles Key) + KeyboardShortcuts+Names erweitern (dynamische Profil-Namen)</name>
  <files>VoiceScribe/Extensions/Defaults+Keys.swift, VoiceScribe/Extensions/KeyboardShortcuts+Names.swift</files>
  <read_first>
    - VoiceScribe/Extensions/Defaults+Keys.swift (vollständiger aktueller Inhalt — MUSS gelesen werden)
    - VoiceScribe/Extensions/KeyboardShortcuts+Names.swift (vollständiger aktueller Inhalt — MUSS gelesen werden)
    - .planning/phases/05-llm-prompt-profiles/05-RESEARCH.md (Pattern 2 Zeilen 206-230, Pattern 1 Zeilen 163-196)
    - .planning/phases/05-llm-prompt-profiles/05-PATTERNS.md (Zeilen 195-257)
    - VoiceScribe/Models/PromptProfile.swift (nach Task 1 vorhanden — Typ-Referenz)
  </read_first>
  <action>
    --- Defaults+Keys.swift ERWEITERN ---

    Den bestehenden Datei-Inhalt beibehalten und APPEND folgenden neuen Key nach dem
    letzten bestehenden Key (outputMode):

    ```swift
    // Phase 5: PROF-01 — [PromptProfile] Persistenz via Defaults.
    // D-04: Codable Array; D-05: ein Default-Profil "Rohe Transkription" beim ersten Start.
    // activeProfileID ist NICHT hier gespeichert — das ist reiner Laufzeit-State in AppState.
    static let profiles = Key<[PromptProfile]>(
        "profiles",
        default: [PromptProfile.defaultProfile]
    )
    ```

    ACHTUNG: Nur den neuen Key hinzufügen, den bestehenden Inhalt (OutputMode, silenceDuration,
    selectedMicUID, outputMode) NICHT ändern.

    --- KeyboardShortcuts+Names.swift ERWEITERN ---

    Den bestehenden Datei-Inhalt beibehalten und APPEND nach toggleOutputMode:

    ```swift
    /// Erzeugt einen stabilen Hotkey-Namen fuer ein Profil anhand seiner UUID.
    /// D-02: UUID-basiert (nicht Index-basiert) — stabil bei Profil-Umsortierung (Phase 6).
    /// RESEARCH.md Pattern 1: dynamische Name-Instanziierung ohne initial: Parameter.
    /// Kein initial: Parameter — Profil-Hotkeys haben keinen vordefinierten Default-Shortcut.
    /// Der String "profile-\(id.uuidString)" ist der Persistence-Key in KeyboardShortcuts UserDefaults.
    static func profile(_ id: UUID) -> Self {
        Self("profile-\(id.uuidString)")
    }
    ```

    ACHTUNG: `static func` (nicht `static let`) — Funktion, die einen Wert zurückgibt,
    kein gespeichertes Property. Kein `initial:`-Parameter.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep -E "(BUILD SUCCEEDED|error:)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "static let profiles = Key<\[PromptProfile\]>" VoiceScribe/Extensions/Defaults+Keys.swift liefert 1 Treffer
    - grep "PromptProfile.defaultProfile" VoiceScribe/Extensions/Defaults+Keys.swift liefert 1 Treffer
    - grep "static func profile(_ id: UUID)" VoiceScribe/Extensions/KeyboardShortcuts+Names.swift liefert 1 Treffer
    - grep "profile-\\\\(id.uuidString)" VoiceScribe/Extensions/KeyboardShortcuts+Names.swift liefert 1 Treffer
    - Die bestehenden Keys (silenceDuration, selectedMicUID, outputMode, toggleRecording, toggleOutputMode) sind UNVERÄNDERT vorhanden
    - xcodebuild build gibt BUILD SUCCEEDED
    - xcodebuild test -only-testing:VoiceScribeTests/PromptProfileTests 2>&1 | grep "passed" — alle 5 Tests grün
  </acceptance_criteria>
  <done>Defaults+Keys.swift enthält profiles-Key. KeyboardShortcuts+Names.swift enthält profile(_:)-Funktion. Alle PromptProfileTests grün (Plan 01 GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserDefaults → PromptProfile | Gespeicherte Daten werden beim Start dekodiert — Codable-Fehler müssen nicht crashed werden |
| PromptProfile → Keychain | Kein API-Key in PromptProfile — Struct enthält nur Name/Prompt/Flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Information Disclosure | PromptProfile struct | mitigate | `var prompt: String` ist Nutzer-eigener Prompt-Text, kein Secret. API-Key ist explizit NICHT in der Struct. Code-Review: grep "groqApiKey\|apiKey\|password" VoiceScribe/Models/PromptProfile.swift → 0 Treffer erwartet. |
| T-5-04 | Tampering | isDefault-Invariante | mitigate | Defaults.Keys.profiles.defaultValue enthält genau 1 Profil mit isDefault=true (D-05). Invariante-Enforcement liegt beim Aufrufer (SettingsView/AppDelegate in Wave 3/4). PromptProfileTests Test 4 verifiziert die Invariante. |
</threat_model>

<verification>
`xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' -only-testing:VoiceScribeTests/PromptProfileTests 2>&1 | grep "Test Suite.*passed"` → alle 5 Tests grün.

`xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "BUILD SUCCEEDED"` → kein Compiler-Fehler.
</verification>

<success_criteria>
- PromptProfile.swift vorhanden mit allen 6 Feldern (id, name, prompt, isLLMEnabled, isThinkingEnabled, isDefault) + defaultProfile
- Defaults.Keys.profiles: Key<[PromptProfile]> mit 1 Default-Element
- KeyboardShortcuts.Name.profile(_:): stable UUID-basierte dynamische Namen
- Plan 01 PromptProfileTests: alle 5 Tests grün (GREEN-Phase abgeschlossen)
- Kein API-Key-Feld in PromptProfile (T-5-01)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/05-llm-prompt-profiles/05-03-SUMMARY.md` erstellen.
</output>
