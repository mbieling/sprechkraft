---
phase: 05-llm-prompt-profiles
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - VoiceScribeTests/PromptProfileTests.swift
autonomous: true
requirements:
  - PROF-01
  - PROF-03
  - PROF-04

must_haves:
  truths:
    - "PromptProfileTests.swift existiert und kompiliert"
    - "Alle 4 Test-Stubs schlagen fehl (RED-Phase)"
    - "xcodebuild test -only-testing:VoiceScribeTests/PromptProfileTests liefert 0 Pass, ≥4 Fail"
  artifacts:
    - path: "VoiceScribeTests/PromptProfileTests.swift"
      provides: "Test-Coverage für PROF-01 (CRUD), PROF-03 (LLM-Toggle), PROF-04 (isDefault-Invariante)"
      contains: "@Suite(\"Prompt Profile"
  key_links:
    - from: "VoiceScribeTests/PromptProfileTests.swift"
      to: "VoiceScribe/Models/PromptProfile.swift"
      via: "@testable import VoiceScribe"
      pattern: "testable import VoiceScribe"
---

<objective>
Wave-0-Build-Gate: PromptProfileTests RED-Stubs anlegen.

Diese Datei kompiliert ab Wave 1 sobald PromptProfile.swift existiert. In Wave 0 schlägt der Build fehl — das ist korrekt und gewollt (RED-Phase). Die Tests definieren den Verhaltensvertrag, den Wave-1-Code erfüllen muss.

Purpose: Sicherstellen, dass PROF-01, PROF-03 und PROF-04 durch automatisierte Tests abgesichert sind, bevor Produktionscode entsteht (Nyquist-Regel).
Output: VoiceScribeTests/PromptProfileTests.swift mit 5 failing Test-Stubs.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-VALIDATION.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-RESEARCH.md

<interfaces>
<!-- Analog-Dateien (bereits vorhanden) — zur Orientierung über Import-Pattern und Test-Struktur -->

Aus VoiceScribeTests/DefaultsKeysTests.swift (analog):
```swift
import Testing
import Defaults
@testable import VoiceScribe

@Suite("Defaults Keys")
struct DefaultsKeysTests {
    @Test("silenceDuration hat Default 1.5")
    func testSilenceDurationDefault() {
        #expect(Defaults.Keys.silenceDuration.defaultValue == 1.5)
    }
}
```

Aus VoiceScribeTests/AppStateTests.swift (analog für @MainActor):
```swift
@Suite("AppState")
@MainActor
struct AppStateTests {
    @Test("recordingState startet mit .idle")
    func testInitialState() { ... }
}
```

PromptProfile (noch nicht vorhanden — wird in Wave 1 erzeugt, Shape laut RESEARCH.md):
```swift
struct PromptProfile: Codable, Defaults.Serializable, Identifiable {
    var id: UUID
    var name: String
    var prompt: String
    var isLLMEnabled: Bool
    var isThinkingEnabled: Bool
    var isDefault: Bool

    static var defaultProfile: PromptProfile
}
extension Defaults.Keys {
    static let profiles = Key<[PromptProfile]>("profiles", default: [PromptProfile.defaultProfile])
}
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: PromptProfileTests RED-Stubs (PROF-01, PROF-03, PROF-04)</name>
  <files>VoiceScribeTests/PromptProfileTests.swift</files>
  <read_first>
    - VoiceScribeTests/DefaultsKeysTests.swift (Import-Pattern, @Suite/@Test/#expect-Stil)
    - VoiceScribeTests/AppStateTests.swift (falls @MainActor-Decorator benötigt wird)
    - .planning/phases/05-llm-prompt-profiles/05-RESEARCH.md (Wave 0 Gaps, Zeilen 666-668)
    - .planning/phases/05-llm-prompt-profiles/05-PATTERNS.md (PromptProfileTests-Abschnitt)
  </read_first>
  <behavior>
    RED-Phase: Alle Tests müssen SCHEITERN, weil PromptProfile.swift noch nicht existiert.
    Keine #expect-Ausdrücke, die durch Tautologien oder leere Implementierungen grün werden könnten.

    Test 1 — PROF-01 Default-Profil-Shape:
      `PromptProfile.defaultProfile.name == "Rohe Transkription"`
      `PromptProfile.defaultProfile.isLLMEnabled == false`
      `PromptProfile.defaultProfile.isDefault == true`
      `PromptProfile.defaultProfile.prompt == ""`

    Test 2 — PROF-01 Defaults-Key hat genau 1 Default-Profil:
      `Defaults.Keys.profiles.defaultValue.count == 1`
      `Defaults.Keys.profiles.defaultValue.first?.isDefault == true`

    Test 3 — PROF-01 Codable Round-Trip (encode → decode → gleiche Werte):
      Ein PromptProfile mit JSONEncoder kodieren, mit JSONDecoder dekodieren,
      id, name, isLLMEnabled, isThinkingEnabled, isDefault vergleichen.

    Test 4 — PROF-04 isDefault-Invariante (genau 1 Default nach Markierung):
      Array mit 3 Profilen, eines als isDefault markieren (alle anderen false setzen),
      `profiles.filter { $0.isDefault }.count == 1` prüfen.

    Test 5 — PROF-03 LLM-Toggle ist unabhängig von isDefault:
      Profil mit isLLMEnabled=true und isDefault=true erstellen,
      sicherstellen dass beide Flags unabhängig sind.
  </behavior>
  <action>
    Datei VoiceScribeTests/PromptProfileTests.swift anlegen.

    Import-Block (identisch zu DefaultsKeysTests.swift):
    ```swift
    import Testing
    import Defaults
    @testable import VoiceScribe
    ```

    Suite-Deklaration:
    ```swift
    @Suite("Prompt Profile (PROF-01, PROF-03, PROF-04)")
    struct PromptProfileTests {
    ```

    Test 1 — Default-Profil-Shape (PROF-01):
    ```swift
    @Test("PromptProfile.defaultProfile hat korrekte Initialwerte (PROF-01)")
    func testDefaultProfileShape() {
        let profile = PromptProfile.defaultProfile
        #expect(profile.name == "Rohe Transkription")
        #expect(profile.isLLMEnabled == false)
        #expect(profile.isThinkingEnabled == false)
        #expect(profile.isDefault == true)
        #expect(profile.prompt == "")
    }
    ```

    Test 2 — Defaults-Key (PROF-01):
    ```swift
    @Test("Defaults.Keys.profiles hat genau 1 Default-Profil beim ersten Start (PROF-01)")
    func testProfilesDefaultKey() {
        #expect(Defaults.Keys.profiles.defaultValue.count == 1)
        #expect(Defaults.Keys.profiles.defaultValue.first?.isDefault == true)
        #expect(Defaults.Keys.profiles.defaultValue.first?.name == "Rohe Transkription")
    }
    ```

    Test 3 — Codable Round-Trip (PROF-01, D-04):
    ```swift
    @Test("PromptProfile Codable Round-Trip: encode dann decode ergibt identische Werte (PROF-01)")
    func testCodableRoundTrip() throws {
        let original = PromptProfile(
            id: UUID(),
            name: "Grammatik-Korrektur",
            prompt: "Korrigiere Grammatikfehler.",
            isLLMEnabled: true,
            isThinkingEnabled: false,
            isDefault: false
        )
        let data = try JSONEncoder().encode(original)
        let decoded = try JSONDecoder().decode(PromptProfile.self, from: data)
        #expect(decoded.id == original.id)
        #expect(decoded.name == original.name)
        #expect(decoded.prompt == original.prompt)
        #expect(decoded.isLLMEnabled == original.isLLMEnabled)
        #expect(decoded.isThinkingEnabled == original.isThinkingEnabled)
        #expect(decoded.isDefault == original.isDefault)
    }
    ```

    Test 4 — isDefault-Invariante (PROF-04):
    ```swift
    @Test("isDefault-Invariante: nach Markierung hat genau 1 Profil isDefault == true (PROF-04)")
    func testIsDefaultInvariante() {
        var profiles = [
            PromptProfile(id: UUID(), name: "A", prompt: "", isLLMEnabled: false,
                         isThinkingEnabled: false, isDefault: true),
            PromptProfile(id: UUID(), name: "B", prompt: "", isLLMEnabled: false,
                         isThinkingEnabled: false, isDefault: false),
            PromptProfile(id: UUID(), name: "C", prompt: "", isLLMEnabled: false,
                         isThinkingEnabled: false, isDefault: false),
        ]
        // Invariante-Enforcement: Profil B als Default markieren
        let targetID = profiles[1].id
        profiles = profiles.map { p in
            var copy = p
            copy.isDefault = (p.id == targetID)
            return copy
        }
        let defaultCount = profiles.filter { $0.isDefault }.count
        #expect(defaultCount == 1)
        #expect(profiles.first { $0.isDefault }?.name == "B")
    }
    ```

    Test 5 — LLM-Toggle unabhängig von isDefault (PROF-03):
    ```swift
    @Test("LLM-Toggle und isDefault sind unabhängige Felder (PROF-03)")
    func testLLMToggleUnabhaengigVonDefault() {
        let profile = PromptProfile(
            id: UUID(),
            name: "Analyse",
            prompt: "Analysiere den Text.",
            isLLMEnabled: true,
            isThinkingEnabled: true,
            isDefault: true
        )
        #expect(profile.isLLMEnabled == true)
        #expect(profile.isDefault == true)
        // Beide Flags sind unabhängig — ein Profil kann LLM UND Default sein
        var noLLM = profile
        noLLM.isLLMEnabled = false
        #expect(noLLM.isDefault == true)  // isDefault bleibt unberührt
    }
    ```

    WICHTIG: Datei soll kompilieren sobald PromptProfile.swift in Wave 1 existiert.
    In Wave 0 scheitert der Build — das ist korrekt (PromptProfile noch nicht definiert).
  </action>
  <verify>
    <automated>
      Wave 0 (erwartet FAIL wegen fehlendem PromptProfile.swift):
      xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep -E "(error:|BUILD FAILED|BUILD SUCCEEDED)" | head -5

      Nach Wave 1 (GREEN-Phase, erwartet PASS):
      xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' -only-testing:VoiceScribeTests/PromptProfileTests 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - VoiceScribeTests/PromptProfileTests.swift existiert
    - grep -c "@Test(" VoiceScribeTests/PromptProfileTests.swift liefert 5
    - grep "import Testing" VoiceScribeTests/PromptProfileTests.swift liefert einen Treffer
    - grep "@testable import VoiceScribe" VoiceScribeTests/PromptProfileTests.swift liefert einen Treffer
    - grep "PROF-01" VoiceScribeTests/PromptProfileTests.swift liefert ≥2 Treffer
    - grep "PROF-04" VoiceScribeTests/PromptProfileTests.swift liefert ≥1 Treffer
    - grep "PROF-03" VoiceScribeTests/PromptProfileTests.swift liefert ≥1 Treffer
    - grep "Rohe Transkription" VoiceScribeTests/PromptProfileTests.swift liefert ≥1 Treffer
  </acceptance_criteria>
  <done>VoiceScribeTests/PromptProfileTests.swift enthält 5 Test-Stubs, die PROF-01/03/04 abdecken. Datei kompiliert nach Wave 1 (PromptProfile.swift vorhanden).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Produktionscode | Tests importieren via @testable — kein Trust-Boundary-Risiko, nur Testumgebung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Information Disclosure | PromptProfile struct | accept | Kein API-Key-Feld in PromptProfile — Struct enthält nur Name/Prompt/Flags. Keine Keychain-Daten in Tests. |
| T-5-04 | Tampering | isDefault-Invariante | mitigate | Test 4 verifiziert explizit, dass genau 1 Profil isDefault==true hat nach Markierungsoperation |
</threat_model>

<verification>
Nach Wave 0: Build schlägt wegen fehlendem PromptProfile.swift fehl (erwartet).
Nach Wave 1: `xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' -only-testing:VoiceScribeTests/PromptProfileTests` — alle 5 Tests grün.
</verification>

<success_criteria>
- 5 Test-Stubs decken PROF-01 (2 Tests: Shape + Key), PROF-01-Codable (1 Test), PROF-04 (1 Test), PROF-03 (1 Test)
- Kein API-Key taucht in irgendeinem Test auf (T-5-01)
- Tests kompilieren nach Wave 1 ohne Änderung
</success_criteria>

<output>
Nach Abschluss `.planning/phases/05-llm-prompt-profiles/05-01-SUMMARY.md` erstellen.
</output>
