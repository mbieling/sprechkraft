---
phase: 05-llm-prompt-profiles
plan: 05
type: execute
wave: 2
depends_on:
  - "05-03"
  - "05-04"
files_modified:
  - SPRECHKRAFT/AppState.swift
  - SPRECHKRAFT/AppDelegate.swift
autonomous: true
requirements:
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - SET-01

must_haves:
  truths:
    - "AppState hat activeProfileID: UUID? und groqKeyMissing: Bool Properties"
    - "AppDelegate registriert Profil-Hotkeys via onKeyDown (nicht onKeyUp)"
    - "onRecordingComplete routet via aktives Profil: LLM oder direkt TextOutputService"
    - "Groq API Key wird bei App-Start aus Keychain gelesen, AppState.groqKeyMissing gesetzt"
    - "Icon wechselt zu .llmProcessing während Groq-Call und zurück zu .idle danach"
  artifacts:
    - path: "SPRECHKRAFT/AppState.swift"
      provides: "activeProfileID + groqKeyMissing Properties"
      contains: "var activeProfileID: UUID?"
    - path: "SPRECHKRAFT/AppDelegate.swift"
      provides: "setupProfileHotkeys() + LLM-Routing in onRecordingComplete + Keychain-Check"
      contains: "setupProfileHotkeys"
  key_links:
    - from: "SPRECHKRAFT/AppDelegate.swift"
      to: "SPRECHKRAFT/AppState.swift"
      via: "appState?.activeProfileID = profile.id"
      pattern: "activeProfileID"
    - from: "SPRECHKRAFT/AppDelegate.swift"
      to: "SPRECHKRAFT/Services/GroqService.swift"
      via: "GroqService.shared.process(transcript:profile:apiKey:)"
      pattern: "GroqService.shared.process"
    - from: "SPRECHKRAFT/AppDelegate.swift"
      to: "Keychain"
      via: "keychain[\"groqApiKey\"]"
      pattern: "keychain\\[.groqApiKey"
---

<objective>
Wave 2: AppState erweitern + AppDelegate verdrahten.

Die komplette LLM-Routing-Logik in onRecordingComplete einbauen, Profil-Hotkeys registrieren und die Keychain-Initialisierung am App-Start durchführen.

Purpose: Ende-zu-Ende-Pipeline schließen — Aufnahme → Transkription → aktives Profil ermitteln → optional Groq → TextOutputService. PROF-02, PROF-03, PROF-04, PROF-05 und SET-01 vollständig verdrahtet.
Output: AppState.swift (2 neue Properties) + AppDelegate.swift (3 neue Sektionen).
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.planning/workflows/execute-plan.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-RESEARCH.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-PATTERNS.md

<interfaces>
<!-- Bestehende Dateien vollständig lesen — Interfaces hier als Schnellreferenz -->

AppState.swift (aktuell, nach Phase 4):
```swift
@MainActor @Observable final class AppState {
    var recordingState: RecordingState = .idle
    var audioLevel: CGFloat = 0.0
    var micPermissionDenied: Bool = false
    var isModelReady: Bool = false
    var axPermissionDenied: Bool = false

    func toggleRecording()
    func resetToIdle()
}
```

AppDelegate.swift — wichtige bestehende Methoden:
```swift
// applicationDidFinishLaunching: updateIcon(), setupHotkey(), setupTranscription(), setupOutputModeHotkey()
// setupAudioController(): audioController?.onRecordingComplete = { [weak self] samples, sampleRate in ... }
// showMenu(): NSMenu-Aufbau mit OutputMode-Häkchen-Pattern
// updateIcon(): Observation-B Pattern
```

Keychain-API (KeychainAccess, aus RESEARCH.md Pattern 4):
```swift
import KeychainAccess
private let keychain = Keychain(service: Bundle.main.bundleIdentifier ?? "com.sprechkraft")
let apiKey: String? = keychain["groqApiKey"]    // Lesen (nil wenn nicht gesetzt)
keychain["groqApiKey"] = enteredKey             // Schreiben
```

PromptProfile (aus Plan 03):
```swift
struct PromptProfile: Codable, Defaults.Serializable, Identifiable {
    var id: UUID; var name: String; var prompt: String
    var isLLMEnabled: Bool; var isThinkingEnabled: Bool; var isDefault: Bool
}
// Defaults.Keys.profiles: Key<[PromptProfile]>
// KeyboardShortcuts.Name.profile(_ id: UUID) -> Self
```

GroqService (aus Plan 04):
```swift
actor GroqService {
    static let shared = GroqService()
    func process(transcript: String, profile: PromptProfile, apiKey: String) async throws -> String
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppState erweitern — activeProfileID + groqKeyMissing (PROF-02, PROF-04, SET-01)</name>
  <files>SPRECHKRAFT/AppState.swift</files>
  <read_first>
    - SPRECHKRAFT/AppState.swift (vollständiger aktueller Inhalt — MUSS gelesen werden)
    - .planning/phases/05-llm-prompt-profiles/05-PATTERNS.md (AppState-Abschnitt Zeilen 455-486)
    - .planning/phases/05-llm-prompt-profiles/05-CONTEXT.md (D-02: "Erster gewinnt")
  </read_first>
  <action>
    Zwei neue Properties in AppState einfügen — NACH `var axPermissionDenied: Bool = false` (Zeile 80)
    und VOR `init() {}`.

    Beide Properties nach dem Muster der bestehenden Properties mit Docstring einfügen:

    ```swift
    /// ID des waehrend der laufenden Aufnahme aktivierten Profils via Profil-Hotkey.
    /// nil = kein Profil-Hotkey gedrueckt → Standard-Profil greift in onRecordingComplete.
    /// D-02: Erster gewinnt — wird in setupProfileHotkeys() onKeyDown gesetzt,
    ///        spaetere Hotkey-Events waehrend derselben Aufnahme werden ignoriert.
    /// Wird in onRecordingComplete gelesen und sofort auf nil zurueckgesetzt.
    var activeProfileID: UUID? = nil

    /// true wenn kein Groq API-Key im macOS Keychain vorhanden ist.
    /// Wird in applicationDidFinishLaunching geprueft (analog axPermissionDenied).
    /// Konsumiert von SettingsView fuer den roten Groq-API-Key-Banner (SET-01).
    /// T-5-01: AppState cached NIE den Key selbst — nur dieses Bool-Flag.
    var groqKeyMissing: Bool = false
    ```

    Den Rest der Datei (toggleRecording, resetToIdle) NICHT ändern.
  </action>
  <verify>
    <automated>xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep -E "(BUILD SUCCEEDED|error:)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "var activeProfileID: UUID? = nil" SPRECHKRAFT/AppState.swift liefert 1 Treffer
    - grep "var groqKeyMissing: Bool = false" SPRECHKRAFT/AppState.swift liefert 1 Treffer
    - Die bestehenden Properties (recordingState, audioLevel, micPermissionDenied, isModelReady, axPermissionDenied) sind UNVERÄNDERT vorhanden
    - grep "apiKey\|groqApiKey\|keychain" SPRECHKRAFT/AppState.swift liefert 0 Treffer (T-5-01/T-5-02: kein API-Key im AppState)
    - xcodebuild build gibt BUILD SUCCEEDED
  </acceptance_criteria>
  <done>AppState hat activeProfileID und groqKeyMissing. Kein API-Key in AppState (T-5-01/T-5-02).</done>
</task>

<task type="auto">
  <name>Task 2: AppDelegate — setupProfileHotkeys + LLM-Routing + Keychain-Init (PROF-02, PROF-03, PROF-04, PROF-05, SET-01)</name>
  <files>SPRECHKRAFT/AppDelegate.swift</files>
  <read_first>
    - SPRECHKRAFT/AppDelegate.swift (vollständiger aktueller Inhalt — MUSS gelesen werden, alle 300+ Zeilen)
    - .planning/phases/05-llm-prompt-profiles/05-RESEARCH.md (Pattern 5 vollständig Zeilen 348-406; Pattern 1 Zeilen 163-196; Pitfall 1+2 Zeilen 447-462)
    - .planning/phases/05-llm-prompt-profiles/05-PATTERNS.md (AppDelegate-Abschnitt Zeilen 378-451)
    - SPRECHKRAFT/AppState.swift (nach Task 1: mit activeProfileID + groqKeyMissing)
    - SPRECHKRAFT/Services/GroqService.swift (nach Plan 04: GroqService.shared.process Signatur)
  </read_first>
  <action>
    DREI Änderungen am AppDelegate, in dieser Reihenfolge durchführen:

    === ÄNDERUNG 1: KeychainAccess Import + Keychain Property ===

    Am Anfang der Datei nach den bestehenden Imports hinzufügen:
    ```swift
    import KeychainAccess
    ```

    Im class body nach `private let transcriptionService = TranscriptionService()` einfügen:
    ```swift
    /// Keychain-Instanz fuer Groq API-Key (SET-01, T-5-01).
    /// Service-Name = Bundle-Identifier fuer Keychain-Isolation zwischen Apps.
    private let keychain = Keychain(service: Bundle.main.bundleIdentifier ?? "com.sprechkraft")
    ```

    === ÄNDERUNG 2: applicationDidFinishLaunching erweitern ===

    In applicationDidFinishLaunching NACH `setupOutputModeHotkey()` (letzte bestehende Zeile
    vor der schließenden Klammer) einfügen:
    ```swift
    setupProfileHotkeys()
    // SET-01: Groq API-Key Verfuegbarkeit pruefen — Banner in SettingsView (Phase 5)
    // T-5-02: Key wird nicht gecacht — groqKeyMissing ist nur ein Bool-Flag
    appState?.groqKeyMissing = (keychain["groqApiKey"] == nil || keychain["groqApiKey"]?.isEmpty == true)
    ```

    === ÄNDERUNG 3: onRecordingComplete in setupAudioController() ersetzen ===

    Den bestehenden `audioController?.onRecordingComplete = { ... }` Block (Zeilen 76-93)
    VOLLSTÄNDIG durch den folgenden Block ersetzen:

    ```swift
    // Phase 5: LLM-Routing nach Transkription (PROF-03, PROF-05)
    // D-10: Stille Fallback — bei Fehler oder fehlendem Key wird rawText ausgegeben.
    audioController?.onRecordingComplete = { [weak self] samples, sampleRate in
        guard let self else { return }
        Task {
            let text = await self.transcriptionService.transcribeWithResampling(samples, sampleRate: sampleRate)
            await MainActor.run {
                guard let text else {
                    self.appState?.resetToIdle()
                    self.updateIcon()
                    return
                }

                // D-02: Profil-ID lesen und sofort zuruecksetzen (naechste Aufnahme sauber)
                let profileID = self.appState?.activeProfileID
                self.appState?.activeProfileID = nil

                // Aktives Profil ermitteln: per Hotkey (PROF-02) → Default (PROF-04) → erstes
                let profiles = Defaults[.profiles]
                let activeProfile = profiles.first { $0.id == profileID }
                    ?? profiles.first { $0.isDefault }
                    ?? profiles.first   // Absoluter Fallback: erstes Profil

                // Ausgabe-Parameter
                let mode = Defaults[.outputMode]
                let axPermitted = !(self.appState?.axPermissionDenied ?? true)

                if let activeProfile, activeProfile.isLLMEnabled {
                    // PROF-05: LLM-Pfad — Icon zu .llmProcessing (FEED-01, lila pulsierend 1.2s)
                    self.appState?.recordingState = .llmProcessing
                    self.updateIcon()  // Observation-B: sofort nach State-Mutation

                    Task {
                        // T-5-02: Key unmittelbar vor Request aus Keychain lesen — nie gecacht
                        let apiKey = self.keychain["groqApiKey"]
                        let outputText: String

                        if let key = apiKey, !key.isEmpty {
                            do {
                                outputText = try await GroqService.shared.process(
                                    transcript: text,
                                    profile: activeProfile,
                                    apiKey: key
                                )
                            } catch {
                                // D-10: Stille Fallback bei Groq-Fehler (Timeout, API-Fehler, etc.)
                                outputText = text
                            }
                        } else {
                            // D-10: Kein Key → Fallback zu Raw-Text (Banner zeigt SET-01-Warnung)
                            outputText = text
                        }

                        await MainActor.run {
                            TextOutputService.shared.output(outputText, mode: mode, axPermitted: axPermitted)
                            self.appState?.resetToIdle()
                            self.updateIcon()  // Observation-B: .llmProcessing → .idle
                        }
                    }
                } else {
                    // Direkt-Pfad: LLM deaktiviert → Raw-Transkript ausgeben
                    TextOutputService.shared.output(text, mode: mode, axPermitted: axPermitted)
                    self.appState?.resetToIdle()
                    self.updateIcon()
                }
            }
        }
    }
    ```

    === ÄNDERUNG 4: setupProfileHotkeys() Methode hinzufügen ===

    Als neue private Methode unter der bestehenden `// MARK: - Output-Mode Hotkey`-Section
    eine neue `// MARK: - Profile Hotkeys (PROF-02)`-Section mit folgender Methode einfügen:

    ```swift
    // MARK: - Profile Hotkeys (PROF-02)

    /// Registriert globale onKeyDown-Handler fuer alle gespeicherten Profile.
    /// Pitfall 1 (RESEARCH.md): onKeyDown statt onKeyUp — Profil muss WAEHREND der Aufnahme aktiv sein.
    /// Pitfall 2 (RESEARCH.md): Vor Neuregistrierung alle alten Handler entfernen.
    /// Wird nach Profil-Aenderungen (ProfileEditorSheet) erneut aufgerufen.
    func setupProfileHotkeys() {
        // Alle bisherigen Profil-Hotkey-Handler zuruecksetzen (Pitfall 2: keine doppelten Handler)
        // KeyboardShortcuts.onKeyDown ueberschreibt vorherigen Handler fuer denselben Name —
        // aber zur Sicherheit zuerst fuer alle bekannten Profile-UUIDs deregistrieren.
        let currentProfiles = Defaults[.profiles]
        for profile in currentProfiles {
            KeyboardShortcuts.disable(.profile(profile.id))
        }

        // Neue Handler registrieren
        for profile in currentProfiles {
            let name = KeyboardShortcuts.Name.profile(profile.id)
            KeyboardShortcuts.onKeyDown(for: name) { [weak self] in
                Task { @MainActor [weak self] in
                    guard let self,
                          self.appState?.recordingState == .recording,
                          self.appState?.activeProfileID == nil   // D-02: Erster gewinnt
                    else { return }
                    self.appState?.activeProfileID = profile.id
                }
            }
        }
    }
    ```

    HINWEIS zu KeyboardShortcuts.disable: Falls `disable(_:)` nicht in der API existiert
    (A2 Assumption aus RESEARCH.md), alternative Methode in Xcode-Autocomplete prüfen:
    `KeyboardShortcuts.reset(for:)` oder schlicht erneutes `onKeyDown` registrieren
    (überschreibt vorherigen Handler wenn Library letzten Handler gewinnen lässt).
    Im Xcode-Projekt muss die exakte API-Methode verifiziert werden.

    === ÄNDERUNG 5: showMenu() — Profil-Häkchen im NSMenu (D-03) ===

    In showMenu() NACH `menu.addItem(.separator())` (der erste separator nach dem titleItem)
    und VOR den bestehenden OutputMode-Items folgende Profil-Gruppe einfügen:

    ```swift
    // PROF-04/D-03: Profil-Auswahl im Menü mit Häkchen (analog OutputMode-Häkchen)
    let profiles = Defaults[.profiles]
    let activeProfileID = appState?.activeProfileID

    for profile in profiles {
        let item = NSMenuItem(
            title: profile.name,
            action: #selector(setActiveProfileFromMenu(_:)),
            keyEquivalent: ""
        )
        item.representedObject = profile.id as AnyObject
        // Häkchen: aktives Profil (via Hotkey) ODER Default-Profil wenn keins aktiv
        let isActive = (activeProfileID != nil)
            ? profile.id == activeProfileID
            : profile.isDefault
        item.state = isActive ? .on : .off
        item.target = self
        menu.addItem(item)
    }

    menu.addItem(.separator())
    ```

    Dazu die @objc-Action hinzufügen (unter `// MARK: - Menu Actions`):
    ```swift
    @objc private func setActiveProfileFromMenu(_ sender: NSMenuItem) {
        guard let profileID = sender.representedObject as? UUID else { return }
        appState?.activeProfileID = profileID
    }
    ```
  </action>
  <verify>
    <automated>xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep -E "(BUILD SUCCEEDED|error:)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "import KeychainAccess" SPRECHKRAFT/AppDelegate.swift liefert 1 Treffer
    - grep "private let keychain = Keychain" SPRECHKRAFT/AppDelegate.swift liefert 1 Treffer
    - grep "setupProfileHotkeys()" SPRECHKRAFT/AppDelegate.swift liefert ≥2 Treffer (Definition + Aufruf in applicationDidFinishLaunching)
    - grep "groqKeyMissing" SPRECHKRAFT/AppDelegate.swift liefert 1 Treffer
    - grep "onKeyDown" SPRECHKRAFT/AppDelegate.swift liefert ≥1 Treffer (setupProfileHotkeys)
    - grep "GroqService.shared.process" SPRECHKRAFT/AppDelegate.swift liefert 1 Treffer
    - grep "activeProfileID = nil" SPRECHKRAFT/AppDelegate.swift liefert 1 Treffer (Reset nach Nutzung)
    - grep "recordingState = .llmProcessing" SPRECHKRAFT/AppDelegate.swift liefert 1 Treffer
    - grep "print.*apiKey\|NSLog.*apiKey\|print.*groqApiKey" SPRECHKRAFT/AppDelegate.swift liefert 0 Treffer (T-5-01)
    - xcodebuild build gibt BUILD SUCCEEDED
    - xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep "Test Suite.*passed" — alle bestehenden Tests grün
  </acceptance_criteria>
  <done>AppDelegate verdrahtet: Profil-Hotkeys registriert, LLM-Routing aktiv, Keychain-Check beim Start, Menü-Häkchen für Profile, Icon-State .llmProcessing während Groq-Call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keychain → AppDelegate | API-Key wird per `keychain["groqApiKey"]` unmittelbar vor jedem Groq-Call gelesen |
| AppDelegate → GroqService | API-Key als Parameter übergeben, nie in AppState oder als Property gecacht |
| NSMenu → AppState | Menü-Klick setzt activeProfileID — nur in .idle-State sinnvoll (Menü öffnet sich nicht während Aufnahme) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Information Disclosure | Keychain-Zugriff in AppDelegate | mitigate | `keychain["groqApiKey"]` unmittelbar vor Groq-Call, Ergebnis als lokale `let apiKey`-Variable — nicht in AppState geschrieben. Kein print/NSLog des Keys. acceptance_criteria: grep 0 Treffer für key-Logging. |
| T-5-02 | Information Disclosure | API-Key Caching | mitigate | AppState hat NUR `groqKeyMissing: Bool`. Key lebt als lokale Variable im Task{}-Closure, wird nach dem Call nicht weitergeführt. |
| T-5-03 | Spoofing | Groq API HTTPS | accept | Gesichert in GroqService.swift (Plan 04). AppDelegate ruft nur `GroqService.shared.process()` — URL liegt im Service. |
| T-5-04 | Tampering | isDefault-Invariante in onRecordingComplete | mitigate | `profiles.first { $0.isDefault } ?? profiles.first` — defensiver Fallback verhindert nil-Crash wenn Invariante gebrochen. PromptProfileTests Test 4 verifiziert Invariante beim CRUD. |
</threat_model>

<verification>
`xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep "BUILD SUCCEEDED"` — kein Fehler.

`xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep "Test Suite.*passed"` — alle Tests (PromptProfileTests + GroqServiceTests) grün.
</verification>

<success_criteria>
- AppState: activeProfileID + groqKeyMissing vorhanden, kein API-Key-Field (T-5-01/T-5-02)
- AppDelegate: setupProfileHotkeys mit onKeyDown (nicht onKeyUp) — Pitfall 1 vermieden
- AppDelegate: onRecordingComplete mit vollständigem LLM-Routing (LLM → GroqService → TextOutputService; kein LLM → direkt TextOutputService)
- AppDelegate: keychain["groqApiKey"] unmittelbar vor Groq-Call gelesen — T-5-02 eingehalten
- AppDelegate: .llmProcessing State + updateIcon() korrekt gesetzt/resettet
- NSMenu: Profil-Häkchen analog OutputMode-Pattern (D-03)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/05-llm-prompt-profiles/05-05-SUMMARY.md` erstellen.
</output>
