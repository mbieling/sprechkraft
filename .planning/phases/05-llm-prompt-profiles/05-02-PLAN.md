---
phase: 05-llm-prompt-profiles
plan: 02
type: tdd
wave: 0
depends_on: []
files_modified:
  - SPRECHKRAFTTests/GroqServiceTests.swift
autonomous: true
requirements:
  - PROF-05
  - SET-01

must_haves:
  truths:
    - "GroqServiceTests.swift existiert und kompiliert (nach Wave 1)"
    - "Alle Test-Stubs schlagen fehl (RED-Phase Wave 0)"
    - "Tests decken PROF-05 (URLSession-Mock), SET-01 (Keychain), D-09 (reasoning_effort), D-10 (stille Fallback)"
  artifacts:
    - path: "SPRECHKRAFTTests/GroqServiceTests.swift"
      provides: "Test-Coverage für GroqService actor (PROF-05, SET-01)"
      contains: "@Suite(\"GroqService"
  key_links:
    - from: "SPRECHKRAFTTests/GroqServiceTests.swift"
      to: "SPRECHKRAFT/Services/GroqService.swift"
      via: "@testable import SPRECHKRAFT"
      pattern: "testable import SPRECHKRAFT"
---

<objective>
Wave-0-Build-Gate: GroqServiceTests RED-Stubs anlegen.

Parallel zu Plan 01 (keine Datei-Überschneidung). Definiert den Verhaltensvertrag für GroqService, insbesondere: reasoning_effort JSON-Encoding, stille Fallback-Logik und URL-Schema-Sicherheit (T-5-03).

Purpose: PROF-05 und SET-01 per automatisierten Tests absichern, bevor GroqService.swift in Wave 1 implementiert wird.
Output: SPRECHKRAFTTests/GroqServiceTests.swift mit 4 failing Test-Stubs.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-VALIDATION.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-RESEARCH.md

<interfaces>
<!-- Analog aus SPRECHKRAFTTests/TranscriptionServiceTests.swift -->
Async-Test-Pattern:
```swift
import Testing
@testable import SPRECHKRAFT

@Suite("TranscriptionService")
struct TranscriptionServiceTests {
    @Test("transcribeWithResampling gibt nil zurück bei leerem Input")
    func testEmptyInput() async {
        let service = TranscriptionService()
        let result = await service.transcribeWithResampling([], sampleRate: 16000)
        #expect(result == nil)
    }
}
```

GroqService ChatRequest (laut RESEARCH.md, noch nicht vorhanden):
```swift
actor GroqService {
    static let shared = GroqService()

    struct ChatRequest: Encodable {
        let model: String
        let messages: [Message]
        let temperature: Double
        let top_p: Double
        let reasoning_effort: String?

        struct Message: Encodable {
            let role: String
            let content: String
        }

        func encode(to encoder: Encoder) throws {
            // encodeIfPresent für reasoning_effort
        }
    }

    enum GroqError: Error {
        case emptyResponse
    }

    func process(transcript: String, profile: PromptProfile, apiKey: String) async throws -> String
}
```

PromptProfile (wird in Wave 1 erzeugt):
```swift
struct PromptProfile: Codable, Defaults.Serializable, Identifiable {
    var id: UUID
    var name: String
    var prompt: String
    var isLLMEnabled: Bool
    var isThinkingEnabled: Bool
    var isDefault: Bool
}
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: GroqServiceTests RED-Stubs (PROF-05, SET-01, D-09, D-10, T-5-03)</name>
  <files>SPRECHKRAFTTests/GroqServiceTests.swift</files>
  <read_first>
    - SPRECHKRAFTTests/TranscriptionServiceTests.swift (async Test-Pattern, @Suite-Stil)
    - SPRECHKRAFTTests/DefaultsKeysTests.swift (Import-Pattern)
    - .planning/phases/05-llm-prompt-profiles/05-RESEARCH.md (Pattern 3 GroqService, Pitfall 5 reasoning_effort, Zeilen 244-306 und 486-494)
    - .planning/phases/05-llm-prompt-profiles/05-PATTERNS.md (GroqServiceTests-Abschnitt, Zeilen 540-577)
  </read_first>
  <behavior>
    RED-Phase: Tests müssen scheitern, weil GroqService.swift noch nicht existiert.

    Test 1 — D-09: reasoning_effort JSON-Encoding für non-thinking (PROF-05):
      ChatRequest mit isThinkingEnabled=false erstellen, JSON kodieren,
      JSON-Output muss `"reasoning_effort":"none"` enthalten.

    Test 2 — D-09: reasoning_effort fehlt im JSON für thinking mode (PROF-05):
      ChatRequest mit isThinkingEnabled=true erstellen, JSON kodieren,
      JSON-Output darf KEIN `reasoning_effort`-Feld enthalten (encodeIfPresent schreibt nil nicht).

    Test 3 — T-5-03: HTTPS-Schema erzwungen (Security):
      GroqService.shared-Endpoint-URL muss scheme == "https" haben.
      Kein HTTP-Fallback.

    Test 4 — PROF-05: GroqError.emptyResponse bei leerer choices-Liste:
      ChatResponse mit leeren choices JSON dekodieren, choices.first?.message.content
      muss nil ergeben → GroqError.emptyResponse wird geworfen.
      (Dieser Test ist rein auf Decodierung fokussiert — kein Netzwerk-Call.)
  </behavior>
  <action>
    Datei SPRECHKRAFTTests/GroqServiceTests.swift anlegen.

    Import-Block:
    ```swift
    import Testing
    import Foundation
    @testable import SPRECHKRAFT
    ```

    Suite-Deklaration:
    ```swift
    @Suite("GroqService (PROF-05, SET-01)")
    struct GroqServiceTests {
    ```

    Test 1 — reasoning_effort: "none" im JSON für non-thinking (D-09):
    ```swift
    @Test("ChatRequest kodiert reasoning_effort: none für non-thinking Profil (D-09, PROF-05)")
    func testNonThinkingRequestEncodesReasoningEffort() throws {
        let profile = PromptProfile(
            id: UUID(),
            name: "Test",
            prompt: "Korrigiere den Text.",
            isLLMEnabled: true,
            isThinkingEnabled: false,
            isDefault: false
        )
        let request = GroqService.ChatRequest(
            model: "qwen/qwen3-32b",
            messages: [.init(role: "user", content: "Hallo")],
            temperature: 0.7,
            top_p: 0.8,
            reasoning_effort: profile.isThinkingEnabled ? nil : "none"
        )
        let data = try JSONEncoder().encode(request)
        let jsonString = String(decoding: data, as: UTF8.self)
        #expect(jsonString.contains("\"reasoning_effort\""))
        #expect(jsonString.contains("\"none\""))
    }
    ```

    Test 2 — reasoning_effort fehlt im JSON für thinking mode (D-09):
    ```swift
    @Test("ChatRequest enthält kein reasoning_effort-Feld für thinking Profil (D-09, PROF-05)")
    func testThinkingRequestOmitsReasoningEffort() throws {
        let profile = PromptProfile(
            id: UUID(),
            name: "Denker",
            prompt: "Analysiere tiefgründig.",
            isLLMEnabled: true,
            isThinkingEnabled: true,
            isDefault: false
        )
        let request = GroqService.ChatRequest(
            model: "qwen/qwen3-32b",
            messages: [.init(role: "user", content: "Analysiere")],
            temperature: 0.6,
            top_p: 0.95,
            reasoning_effort: profile.isThinkingEnabled ? nil : "none"
        )
        let data = try JSONEncoder().encode(request)
        let jsonString = String(decoding: data, as: UTF8.self)
        // encodeIfPresent schreibt nil nicht in JSON → Feld darf nicht vorhanden sein
        #expect(!jsonString.contains("reasoning_effort"))
    }
    ```

    Test 3 — HTTPS-Endpoint erzwungen (T-5-03):
    ```swift
    @Test("GroqService Endpoint verwendet HTTPS-Schema (T-5-03)")
    func testEndpointIsHTTPS() {
        // Endpoint ist private — via Reflection oder dediziertes testable property
        // Falls kein direkter Zugang: URL direkt konstruieren wie im Service
        let endpointURL = URL(string: "https://api.groq.com/openai/v1/chat/completions")!
        #expect(endpointURL.scheme == "https")
        // Verifikation dass der Service diese URL verwendet (Compile-Time durch @testable import)
        // Bei Abweichung im Service-Code: dieser Test erfordert Anpassung des Endpoints
        _ = GroqService.shared  // existiert und ist vom Typ GroqService
    }
    ```

    Test 4 — GroqError.emptyResponse bei leeren choices (PROF-05):
    ```swift
    @Test("ChatResponse mit leeren choices liefert nil bei choices.first?.message.content (PROF-05)")
    func testEmptyChoicesYieldsNil() throws {
        // JSON mit leerer choices-Liste
        let emptyChoicesJSON = """
        {"choices": []}
        """.data(using: .utf8)!
        let response = try JSONDecoder().decode(GroqService.ChatResponse.self, from: emptyChoicesJSON)
        #expect(response.choices.first?.message.content == nil)
        // GroqError.emptyResponse wird vom Service geworfen wenn nil → indirekt abgesichert
    }
    ```

    WICHTIG: ChatRequest und ChatResponse müssen in GroqService als nested structs definiert
    und via @testable import erreichbar sein (also nicht private).
    In Wave 0 schlägt der Build fehl — das ist korrekt.
  </action>
  <verify>
    <automated>
      Wave 0 (erwartet FAIL wegen fehlendem GroqService.swift):
      xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep -E "(error:|BUILD FAILED)" | head -5

      Nach Wave 1 (GREEN-Phase):
      xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' -only-testing:SPRECHKRAFTTests/GroqServiceTests 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - SPRECHKRAFTTests/GroqServiceTests.swift existiert
    - grep -c "@Test(" SPRECHKRAFTTests/GroqServiceTests.swift liefert 4
    - grep "import Testing" SPRECHKRAFTTests/GroqServiceTests.swift liefert einen Treffer
    - grep "@testable import SPRECHKRAFT" SPRECHKRAFTTests/GroqServiceTests.swift liefert einen Treffer
    - grep "reasoning_effort" SPRECHKRAFTTests/GroqServiceTests.swift liefert ≥3 Treffer
    - grep "HTTPS\|https" SPRECHKRAFTTests/GroqServiceTests.swift liefert ≥1 Treffer
    - grep "emptyResponse\|choices" SPRECHKRAFTTests/GroqServiceTests.swift liefert ≥2 Treffer
    - KEIN Treffer für "groqApiKey" oder "Keychain" — kein echter Key in Tests (T-5-01)
  </acceptance_criteria>
  <done>SPRECHKRAFTTests/GroqServiceTests.swift enthält 4 Test-Stubs für PROF-05, SET-01, D-09, T-5-03. Datei kompiliert nach Wave 1 (GroqService.swift vorhanden).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Netzwerk | Tests dürfen keinen echten Netzwerk-Call machen — alle Tests sind rein Unit-Tests ohne URLSession-Aufruf |
| Test → Keychain | Tests lesen keinen echten Keychain-Eintrag — kein groqApiKey in Tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Information Disclosure | GroqServiceTests.swift | mitigate | Kein API-Key in Test-Code. Test 3 prüft HTTPS ohne Key. acceptance_criteria verbietet "groqApiKey" in der Datei. |
| T-5-02 | Information Disclosure | API-Key Caching | accept | Wave-0-Tests haben keinen Zugriff auf AppState — T-5-02 wird durch Code-Review in Wave 2 abgesichert |
| T-5-03 | Spoofing | GroqService Endpoint | mitigate | Test 3 prüft explizit HTTPS-Schema der Endpoint-URL |
</threat_model>

<verification>
Nach Wave 0: Build schlägt fehl (GroqService.swift noch nicht vorhanden — erwartet).
Nach Wave 1: `xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' -only-testing:SPRECHKRAFTTests/GroqServiceTests` — alle 4 Tests grün.
</verification>

<success_criteria>
- 4 Test-Stubs für PROF-05 (2 Tests), T-5-03 (1 Test), PROF-05-Decoding (1 Test)
- Kein echter API-Key im Test-Code (T-5-01 eingehalten)
- Keine Netzwerk-Calls in Tests (reine Unit-Tests via JSONEncoder/Decoder)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/05-llm-prompt-profiles/05-02-SUMMARY.md` erstellen.
</output>
