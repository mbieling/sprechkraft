---
phase: 05-llm-prompt-profiles
plan: 06
type: execute
wave: 3
depends_on:
  - "05-05"
files_modified:
  - VoiceScribe/Views/ProfileEditorSheet.swift
  - VoiceScribe/Views/SettingsView.swift
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - SET-01

must_haves:
  truths:
    - "SettingsView hat neue Section 'Prompt-Profile' mit Profilliste + Groq-API-Key-Feld"
    - "Groq-API-Key-Banner erscheint wenn groqKeyMissing == true"
    - "Klick auf Profilzeile öffnet ProfileEditorSheet als .sheet(item:)"
    - "ProfileEditorSheet enthält alle 5 Sektionen (Name, Hotkey, KI, Prompt, Aktionen)"
    - "Profil löschen ist ausgegraut wenn nur 1 Profil vorhanden (D-06)"
    - "isDefault-Invariante wird bei 'Als Standard markieren' sichergestellt"
  artifacts:
    - path: "VoiceScribe/Views/ProfileEditorSheet.swift"
      provides: "Sheet-Modal für Profil-CRUD (D-12)"
      contains: "struct ProfileEditorSheet: View"
      exports: ["ProfileEditorSheet"]
    - path: "VoiceScribe/Views/SettingsView.swift"
      provides: "Section('Prompt-Profile') mit Groq-Banner + Profilliste + Sheet"
      contains: "Section(\"Prompt-Profile\")"
  key_links:
    - from: "VoiceScribe/Views/SettingsView.swift"
      to: "VoiceScribe/Views/ProfileEditorSheet.swift"
      via: ".sheet(item: $editingProfile)"
      pattern: "editingProfile"
    - from: "VoiceScribe/Views/SettingsView.swift"
      to: "VoiceScribe/AppState.swift"
      via: "appState?.groqKeyMissing"
      pattern: "groqKeyMissing"
    - from: "VoiceScribe/Views/ProfileEditorSheet.swift"
      to: "VoiceScribe/Extensions/Defaults+Keys.swift"
      via: "Defaults[.profiles]"
      pattern: "Defaults\\[.profiles\\]"
    - from: "VoiceScribe/Views/ProfileEditorSheet.swift"
      to: "VoiceScribe/AppDelegate.swift"
      via: "AppDelegate.setupProfileHotkeys() nach Profil-Änderung"
      pattern: "setupProfileHotkeys"
---

<objective>
Wave 3: Settings-UI + ProfileEditorSheet implementieren.

Zwei neue View-Dateien mit vollständiger Profil-CRUD-Funktionalität. UI-SPEC (05-UI-SPEC.md) ist verbindlich für alle Layout-, Farb-, Typografie- und Copywriting-Details.

Purpose: PROF-01 (CRUD-UI), PROF-02 (Hotkey-Recorder im Sheet), PROF-03 (LLM-Toggle), PROF-04 (Standard-Markierung), SET-01 (API-Key-Feld + Banner) in der Benutzeroberfläche abbilden.
Output: ProfileEditorSheet.swift (neu) + SettingsView.swift (Section erweitert).
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.planning/workflows/execute-plan.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-UI-SPEC.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-PATTERNS.md

<interfaces>
<!-- Bestehende SettingsView — Schlüssel-Patterns -->

Permission-Banner-Pattern aus SettingsView.swift (Zeilen 119-145):
```swift
if appState?.axPermissionDenied == true {
    HStack(spacing: DesignTokens.Spacing.sm) {
        Image(systemName: "hand.raised.slash.fill").foregroundStyle(.white)
        VStack(alignment: .leading, spacing: 2) {
            Text("Bedienungshilfen-Zugriff erforderlich")
                .font(.system(size: 13, weight: .medium)).foregroundStyle(.white)
            Text("Ohne Bedienungshilfen-Zugriff ...")
                .font(.system(size: 11)).foregroundStyle(.white.opacity(0.9))
        }
        Spacer()
        Button("Einstellungen öffnen") { NSWorkspace.shared.open(URL(...)) }
            .buttonStyle(.bordered)
    }
    .padding(DesignTokens.Spacing.sm)
    .background(Color(.systemRed)).cornerRadius(8)
    .accessibilityLabel("...")
}
```

KeyboardShortcuts.Recorder-Pattern (SettingsView.swift Zeile 161):
```swift
KeyboardShortcuts.Recorder("Modus-Wechsel-Hotkey", name: .toggleOutputMode)
```
Für dynamische Profil-Namen: `KeyboardShortcuts.Recorder("Profil-Hotkey", name: .profile(draft.id))`

AppState (nach Plan 05):
```swift
var activeProfileID: UUID? = nil
var groqKeyMissing: Bool = false
```

DesignTokens.Spacing (aus DesignTokens.swift):
```swift
// xs=4, sm=8, md=16, lg=24, xl=32
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfileEditorSheet.swift (D-12, PROF-01, PROF-02, PROF-03, PROF-04, D-06)</name>
  <files>VoiceScribe/Views/ProfileEditorSheet.swift</files>
  <read_first>
    - VoiceScribe/Views/SettingsView.swift (vollständig — Import-Stil, Form-Pattern, KeyboardShortcuts.Recorder, DesignTokens)
    - .planning/phases/05-llm-prompt-profiles/05-UI-SPEC.md (Sheet-Modal Abschnitt 3, Interaction Contract, Copywriting Contract)
    - .planning/phases/05-llm-prompt-profiles/05-PATTERNS.md (ProfileEditorSheet-Abschnitt Zeilen 322-375)
    - VoiceScribe/Models/PromptProfile.swift (Struct-Felder)
    - VoiceScribe/Extensions/Defaults+Keys.swift (Defaults[.profiles] Zugriff)
    - VoiceScribe/Extensions/KeyboardShortcuts+Names.swift (KeyboardShortcuts.Name.profile(_:))
    - VoiceScribe/AppDelegate.swift (setupProfileHotkeys — muss nach Profil-Änderung aufgerufen werden)
  </read_first>
  <action>
    Neue Datei `VoiceScribe/Views/ProfileEditorSheet.swift` erstellen.

    Das Sheet wird via `.sheet(item: $editingProfile)` aus SettingsView geöffnet.
    Es empfängt das Profil als initialisierten State (Draft-Pattern: lokale @State-Kopie).

    Vollständige Implementierung:
    ```swift
    // VoiceScribe/Views/ProfileEditorSheet.swift
    // Zweck: Sheet-Modal fuer Profil-CRUD (D-12, PROF-01 bis PROF-04).
    // UI-SPEC: 05-UI-SPEC.md §Sheet-Modal "Profil bearbeiten"
    // D-06: Löschen-Button ausgegraut wenn nur 1 Profil vorhanden.
    // D-09: Thinking-Toggle nur sichtbar wenn LLM aktiviert.
    // isDefault-Invariante: beim Markieren alle anderen Profile auf false setzen.

    import SwiftUI
    import Defaults
    import KeyboardShortcuts

    struct ProfileEditorSheet: View {
        // Draft-Pattern: Änderungen werden erst bei "Profil sichern" übernommen
        @State private var draft: PromptProfile
        let isOnlyProfile: Bool    // D-06: Löschen deaktiviert wenn true
        var onSave: (PromptProfile) -> Void
        var onDelete: () -> Void
        var onSetDefault: () -> Void

        @Environment(\.dismiss) private var dismiss

        init(profile: PromptProfile,
             isOnlyProfile: Bool,
             onSave: @escaping (PromptProfile) -> Void,
             onDelete: @escaping () -> Void,
             onSetDefault: @escaping () -> Void) {
            _draft = State(initialValue: profile)
            self.isOnlyProfile = isOnlyProfile
            self.onSave = onSave
            self.onDelete = onDelete
            self.onSetDefault = onSetDefault
        }

        var body: some View {
            NavigationStack {
                Form {
                    // 1. Profil-Name (UI-SPEC: erstes Feld, sofort editierbar)
                    Section {
                        TextField("Name", text: $draft.name)
                            .font(.system(size: 13))
                    }

                    // 2. Aktivierungs-Hotkey (PROF-02)
                    Section("Aktivierungs-Hotkey") {
                        KeyboardShortcuts.Recorder("Profil-Hotkey", name: .profile(draft.id))
                        Text("Halte diesen Hotkey während der Aufnahme, um das Profil zu aktivieren.")
                            .font(.system(size: 11))
                            .foregroundStyle(.secondary)
                    }

                    // 3. KI-Verarbeitung (PROF-03, D-09)
                    Section("KI-Verarbeitung") {
                        Toggle("LLM-Verarbeitung aktivieren", isOn: $draft.isLLMEnabled)
                            .accessibilityLabel("LLM-Verarbeitung aktivieren, \(draft.isLLMEnabled ? "aktiviert" : "deaktiviert")")
                        if draft.isLLMEnabled {
                            Toggle("Thinking-Modus (qwen3 Chain-of-Thought)", isOn: $draft.isThinkingEnabled)
                        }
                    }

                    // 4. Prompt-Text — nur sichtbar wenn LLM aktiviert (D-09)
                    if draft.isLLMEnabled {
                        Section("Prompt") {
                            TextEditor(text: $draft.prompt)
                                .font(.system(size: 13))
                                .frame(minHeight: 80)
                                .accessibilityLabel("Prompt-Text")
                            Text("Der Prompt wird dem Transkript vorangestellt und an Groq qwen3-32b gesendet.")
                                .font(.system(size: 11))
                                .foregroundStyle(.secondary)
                        }
                    }

                    // 5. Aktionen — destruktive Aktion immer zuletzt (UI-SPEC)
                    Section {
                        // D-13: "Als Standard markieren" — ausgegraut wenn bereits Standard
                        Button("Als Standard markieren") {
                            onSetDefault()
                            dismiss()
                        }
                        .disabled(draft.isDefault)
                        .accessibilityHint(draft.isDefault ? "Dieses Profil ist bereits als Standard markiert." : "")

                        // D-06: Löschen ausgegraut wenn letztes Profil
                        Button(role: .destructive) {
                            onDelete()
                            dismiss()
                        } label: {
                            Text("Profil löschen")
                        }
                        .disabled(isOnlyProfile)
                        .accessibilityHint(isOnlyProfile ? "Kann nicht gelöscht werden, da es das letzte Profil ist." : "")
                    }
                }
                .formStyle(.grouped)
                .navigationTitle(draft.name.trimmingCharacters(in: .whitespaces).isEmpty
                    ? "Neues Profil" : draft.name)
                .frame(width: 420, minHeight: 380)
                .toolbar {
                    ToolbarItem(placement: .cancellationAction) {
                        Button("Abbrechen") { dismiss() }
                    }
                    ToolbarItem(placement: .confirmationAction) {
                        Button("Profil sichern") {
                            onSave(draft)
                            dismiss()
                        }
                        .disabled(draft.name.trimmingCharacters(in: .whitespaces).isEmpty)
                    }
                }
            }
        }
    }
    ```

    WICHTIG: `.transition(.opacity)` für LLM-Toggle-Sichtbarkeit — nicht benötigt wenn
    SwiftUI if/else bereits sanft blendet. Falls Ruckeln auftritt: `withAnimation { }` um
    den isLLMEnabled-Toggle-Handler wickeln.

    Die Callbacks (onSave, onDelete, onSetDefault) kapseln die Defaults[.profiles]-Mutation
    in SettingsView — ProfileEditorSheet selbst schreibt NICHT direkt in Defaults.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep -E "(BUILD SUCCEEDED|error:)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - VoiceScribe/Views/ProfileEditorSheet.swift existiert
    - grep "struct ProfileEditorSheet: View" VoiceScribe/Views/ProfileEditorSheet.swift liefert 1 Treffer
    - grep "KeyboardShortcuts.Recorder" VoiceScribe/Views/ProfileEditorSheet.swift liefert 1 Treffer
    - grep "Profil löschen" VoiceScribe/Views/ProfileEditorSheet.swift liefert 1 Treffer
    - grep "isOnlyProfile" VoiceScribe/Views/ProfileEditorSheet.swift liefert ≥2 Treffer (Deklaration + .disabled)
    - grep "Als Standard markieren" VoiceScribe/Views/ProfileEditorSheet.swift liefert 1 Treffer
    - grep "Profil-Hotkey" VoiceScribe/Views/ProfileEditorSheet.swift liefert 1 Treffer
    - grep "Neues Profil" VoiceScribe/Views/ProfileEditorSheet.swift liefert 1 Treffer (leerer Name-Fallback)
    - grep "Prompt-Text" VoiceScribe/Views/ProfileEditorSheet.swift liefert 1 Treffer (accessibilityLabel)
    - xcodebuild build gibt BUILD SUCCEEDED
  </acceptance_criteria>
  <done>ProfileEditorSheet.swift kompiliert mit allen 5 Sektionen, D-06/D-09/D-12/D-13 korrekt implementiert.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsView.swift — Section 'Prompt-Profile' + Groq-Banner + Sheet-Integration (PROF-01, SET-01)</name>
  <files>VoiceScribe/Views/SettingsView.swift</files>
  <read_first>
    - VoiceScribe/Views/SettingsView.swift (vollständiger aktueller Inhalt — MUSS gelesen werden)
    - .planning/phases/05-llm-prompt-profiles/05-UI-SPEC.md (Komponenten-Inventar §1 SettingsView, §2 ProfileRowView, §4 Copywriting)
    - .planning/phases/05-llm-prompt-profiles/05-PATTERNS.md (SettingsView-Abschnitt Zeilen 261-318)
    - VoiceScribe/Views/ProfileEditorSheet.swift (nach Task 1 vorhanden — Callback-Signaturen)
    - VoiceScribe/AppDelegate.swift (setupProfileHotkeys — nach Profil-Änderung erneut aufrufen)
    - VoiceScribe/Constants/DesignTokens.swift (Spacing-Tokens)
  </read_first>
  <action>
    SettingsView.swift ERWEITERN — neue Section "Prompt-Profile" nach der bestehenden
    Textausgabe-Section einfügen.

    === SCHRITT 1: Neue @State-Properties in SettingsView einfügen ===

    Nach den bestehenden @State-Properties (availableMics, etc.) einfügen:
    ```swift
    // Phase 5: Profil-Sheet State
    @State private var editingProfile: PromptProfile? = nil
    @FocusState private var apiKeyFocused: Bool
    @State private var groqApiKeyInput: String = ""
    ```

    === SCHRITT 2: onAppear/onLoad — groqApiKeyInput initialisieren ===

    In der bestehenden `.onAppear { }` Closure (oder am Ende von `body` als `.task`) einfügen:
    ```swift
    // Groq API Key aus Keychain laden — nur für Anzeige (maskiert via SecureField)
    // KeychainAccess aus AppDelegate nicht direkt zugänglich — via Notification oder
    // durch direkten Keychain-Zugriff in SettingsView selbst.
    // HINWEIS: SettingsView braucht eigene Keychain-Instanz für Lese-/Schreibzugriff.
    ```

    Da SettingsView keinen direkten Zugriff auf AppDelegate.keychain hat, muss SettingsView
    eine eigene Keychain-Instanz erstellen:
    ```swift
    private let keychain = Keychain(service: Bundle.main.bundleIdentifier ?? "com.voicescribe")
    ```
    (Import `import KeychainAccess` am Anfang der Datei hinzufügen.)

    In `.onAppear {}`:
    ```swift
    groqApiKeyInput = keychain["groqApiKey"] ?? ""
    ```

    === SCHRITT 3: Neue Section "Prompt-Profile" einfügen ===

    NACH der bestehenden Section für Textausgabe (outputMode) und VOR der schließenden
    `}` von `Form { }` folgende Section einfügen:

    ```swift
    // MARK: — Prompt-Profile (PROF-01 bis PROF-04, SET-01)
    Section("Prompt-Profile") {
        // SET-01: Groq API-Key-Banner — analog axPermissionDenied-Banner (T-5-01)
        if appState?.groqKeyMissing == true {
            HStack(spacing: DesignTokens.Spacing.sm) {
                Image(systemName: "key.slash")
                    .foregroundStyle(.white)
                VStack(alignment: .leading, spacing: 2) {
                    Text("Groq API-Schlüssel fehlt")
                        .font(.system(size: 13, weight: .semibold))
                        .foregroundStyle(.white)
                    Text("Ohne API-Schlüssel ist LLM-Verarbeitung nicht möglich. Füge deinen Schlüssel ein.")
                        .font(.system(size: 11))
                        .foregroundStyle(.white.opacity(0.9))
                }
                Spacer()
                Button("Schlüssel eingeben") {
                    apiKeyFocused = true  // Fokus auf SecureField springen
                }
                .buttonStyle(.bordered)
            }
            .padding(DesignTokens.Spacing.sm)
            .background(Color(.systemRed))
            .cornerRadius(8)
            .accessibilityLabel("Groq API-Schlüssel fehlt. Füge deinen Schlüssel in das Eingabefeld ein.")
        }

        // SET-01: API-Key-Eingabe (Pitfall 4: sofort bei onChange in Keychain schreiben)
        SecureField("API-Schlüssel", text: $groqApiKeyInput)
            .textContentType(.password)
            .focused($apiKeyFocused)
            .onChange(of: groqApiKeyInput) { _, newValue in
                // T-5-01: Key sofort in Keychain schreiben (nie in UserDefaults/AppState)
                keychain["groqApiKey"] = newValue.isEmpty ? nil : newValue
                appState?.groqKeyMissing = newValue.isEmpty
            }
        Text("Schlüssel wird sicher im macOS Keychain gespeichert.")
            .font(.system(size: 11))
            .foregroundStyle(.secondary)

        // Profilliste (PROF-01, PROF-02, PROF-03, PROF-04)
        ForEach(Defaults[.profiles]) { profile in
            HStack {
                Text(profile.name)
                    .font(.system(size: 13))
                    .lineLimit(1)
                Spacer()
                if profile.isDefault {
                    Text("⭐")  // D-13: Unicode U+2B50
                        .font(.system(size: 13))
                        .accessibilityLabel("Standard-Profil")
                }
                Image(systemName: "chevron.right")
                    .foregroundStyle(.secondary)
                    .font(.system(size: 11, weight: .semibold))
            }
            .contentShape(Rectangle())
            .onTapGesture { editingProfile = profile }
        }

        // Neues Profil anlegen
        Button {
            let newProfile = PromptProfile(
                id: UUID(),
                name: "",
                prompt: "",
                isLLMEnabled: false,
                isThinkingEnabled: false,
                isDefault: false
            )
            editingProfile = newProfile
        } label: {
            Label("Profil hinzufügen", systemImage: "plus")
                .font(.system(size: 13))
        }
        .buttonStyle(.borderless)
    }
    ```

    === SCHRITT 4: .sheet(item: $editingProfile) anhängen ===

    Am bestehenden `Form { ... }.formStyle(.grouped)` Block als weiteren Modifier anhängen:

    ```swift
    .sheet(item: $editingProfile) { profile in
        let profiles = Defaults[.profiles]
        let isOnlyProfile = profiles.count <= 1

        ProfileEditorSheet(
            profile: profile,
            isOnlyProfile: isOnlyProfile,
            onSave: { updatedProfile in
                var current = Defaults[.profiles]
                if let idx = current.firstIndex(where: { $0.id == updatedProfile.id }) {
                    current[idx] = updatedProfile
                } else {
                    // Neues Profil — hinzufügen (ohne isDefault zu überschreiben)
                    current.append(updatedProfile)
                }
                Defaults[.profiles] = current
                // Profil-Hotkeys neu registrieren nach Änderung (Pitfall 2)
                NotificationCenter.default.post(name: .refreshProfileHotkeys, object: nil)
            },
            onDelete: {
                var current = Defaults[.profiles]
                current.removeAll { $0.id == profile.id }
                // Wenn gelöschtes Profil das Default war: erstes verbleibendes als Default (Pitfall 3)
                if profile.isDefault, let firstIdx = current.indices.first {
                    current[firstIdx].isDefault = true
                }
                Defaults[.profiles] = current
                // Profil-Hotkey-Binding löschen
                KeyboardShortcuts.reset(.profile(profile.id))
                NotificationCenter.default.post(name: .refreshProfileHotkeys, object: nil)
            },
            onSetDefault: {
                // PROF-04 isDefault-Invariante: alle anderen auf false, dieses auf true
                var current = Defaults[.profiles]
                current = current.map { p in
                    var copy = p; copy.isDefault = (p.id == profile.id); return copy
                }
                Defaults[.profiles] = current
            }
        )
    }
    ```

    === SCHRITT 5: Notification.Name für Hotkey-Refresh ===

    In AppDelegate.swift muss `Notification.Name.refreshProfileHotkeys` definiert werden
    und ein Observer in `applicationDidFinishLaunching` registriert werden:

    In AppDelegate.swift (Notification.Name Extension):
    ```swift
    extension Notification.Name {
        // bestehend: static let openSettings
        static let refreshProfileHotkeys = Notification.Name("com.voicescribe.refreshProfileHotkeys")
    }
    ```

    In AppDelegate.applicationDidFinishLaunching nach setupProfileHotkeys():
    ```swift
    NotificationCenter.default.addObserver(
        self,
        selector: #selector(handleRefreshProfileHotkeys),
        name: .refreshProfileHotkeys,
        object: nil
    )
    ```

    Neue @objc-Methode in AppDelegate:
    ```swift
    @objc private func handleRefreshProfileHotkeys() {
        setupProfileHotkeys()
    }
    ```

    HINWEIS: Diese Änderung an AppDelegate.swift ist eine ERGÄNZUNG zu Plan 05.
    Sie berührt nur die Notification.Name Extension und applicationDidFinishLaunching.
    Kein Conflict mit Plan 05 da Plan 06 nach Plan 05 ausgeführt wird (Wave 3 > Wave 2).
  </action>
  <verify>
    <automated>xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep -E "(BUILD SUCCEEDED|error:)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "Section(\"Prompt-Profile\")" VoiceScribe/Views/SettingsView.swift liefert 1 Treffer
    - grep "groqKeyMissing" VoiceScribe/Views/SettingsView.swift liefert ≥2 Treffer (Banner-Kondition + onChange)
    - grep "SecureField" VoiceScribe/Views/SettingsView.swift liefert 1 Treffer
    - grep "editingProfile" VoiceScribe/Views/SettingsView.swift liefert ≥3 Treffer (State, sheet, onTapGesture)
    - grep "ProfileEditorSheet" VoiceScribe/Views/SettingsView.swift liefert 1 Treffer (im sheet-Modifier)
    - grep "⭐\|U+2B50" VoiceScribe/Views/SettingsView.swift liefert 1 Treffer (D-13)
    - grep "Profil hinzufügen" VoiceScribe/Views/SettingsView.swift liefert 1 Treffer
    - grep "keychain\[.groqApiKey.\]" VoiceScribe/Views/SettingsView.swift liefert ≥1 Treffer (onChange-Schreiben)
    - grep "UserDefaults\|Defaults\[.outputMode" VoiceScribe/Views/SettingsView.swift sollte KEINE neuen groqApiKey-UserDefaults-Einträge zeigen (T-5-01)
    - grep "refreshProfileHotkeys" VoiceScribe/AppDelegate.swift liefert ≥2 Treffer (Notification.Name + Observer)
    - xcodebuild build gibt BUILD SUCCEEDED
    - xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "Test Suite.*passed" — alle Tests weiterhin grün
  </acceptance_criteria>
  <done>SettingsView hat vollständige Prompt-Profile-Section mit Groq-Banner, Profilliste, Sheet-Integration und isDefault-Invarianz-Enforcement beim Löschen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SecureField → Keychain | API-Key-Eingabe geht direkt via onChange in Keychain — nicht in AppState oder UserDefaults |
| SettingsView → AppState.groqKeyMissing | Bool-Flag — kein Key-Inhalt in AppState (T-5-01/T-5-02) |
| ProfileEditorSheet → Defaults.profiles | Callbacks in SettingsView mutieren Defaults[.profiles] — Sheet schreibt selbst nicht |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Information Disclosure | SecureField + onChange | mitigate | API-Key geht via `keychain["groqApiKey"] = newValue` direkt in Keychain. Kein Zwischenstand in AppState oder UserDefaults. `.textContentType(.password)` verhindert Auto-Fill-Interaktion. |
| T-5-04 | Tampering | isDefault-Invariante bei Löschen | mitigate | onDelete prüft: wenn gelöschtes Profil isDefault war → erstes verbleibendes Profil als neues Default markieren. onSetDefault setzt alle anderen auf false (map). PromptProfileTests Test 4 verifiziert die Invariante. |
</threat_model>

<verification>
`xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "BUILD SUCCEEDED"` — kein Fehler.

`xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "Test Suite.*passed"` — alle Tests (PromptProfileTests + GroqServiceTests) weiterhin grün.
</verification>

<success_criteria>
- ProfileEditorSheet.swift: alle 5 Sektionen, D-06/D-09/D-12/D-13 korrekt
- SettingsView: Groq-Banner (groqKeyMissing), SecureField → Keychain onChange, Profilliste mit ⭐, Sheet-Integration
- isDefault-Invariante beim Löschen und Setzen als Default sichergestellt (T-5-04)
- API-Key nie in UserDefaults, immer Keychain (T-5-01)
- Hotkey-Neuregistrierung nach Profil-Änderung via Notification (Pitfall 2 aus RESEARCH.md)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/05-llm-prompt-profiles/05-06-SUMMARY.md` erstellen.
</output>
