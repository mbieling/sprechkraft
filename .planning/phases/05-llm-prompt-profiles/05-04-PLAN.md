---
phase: 05-llm-prompt-profiles
plan: 04
type: execute
wave: 1
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - VoiceScribe/Services/GroqService.swift
autonomous: true
requirements:
  - PROF-05
  - SET-01

must_haves:
  truths:
    - "GroqService actor existiert mit process(transcript:profile:apiKey:) Methode"
    - "ChatRequest.encode(to:) verwendet encodeIfPresent für reasoning_effort"
    - "GroqServiceTests GREEN nach Wave 1"
    - "Endpoint-URL ist https://api.groq.com/openai/v1/chat/completions (kein HTTP-Fallback)"
  artifacts:
    - path: "VoiceScribe/Services/GroqService.swift"
      provides: "URLSession-basierter Groq-LLM-Client"
      contains: "actor GroqService"
      exports: ["GroqService", "GroqService.shared", "GroqService.ChatRequest", "GroqService.ChatResponse", "GroqService.GroqError"]
  key_links:
    - from: "VoiceScribe/Services/GroqService.swift"
      to: "https://api.groq.com/openai/v1/chat/completions"
      via: "URLSession.shared.data(for:)"
      pattern: "api.groq.com/openai/v1/chat/completions"
    - from: "VoiceScribe/Services/GroqService.swift"
      to: "VoiceScribe/Models/PromptProfile.swift"
      via: "process(transcript:profile:apiKey:)"
      pattern: "profile.isThinkingEnabled"
---

<objective>
Wave 1 Plan B: GroqService actor implementieren.

Parallel zu Plan 03 (keine Datei-Überschneidung). Implementiert den vollständigen Groq-REST-Client mit korrektem reasoning_effort-Encoding, 30s Timeout und stiller Fallback-Semantik (D-10).

Purpose: GroqServiceTests (Plan 02) grün machen und den Service bereitstellen, den AppDelegate in Wave 2 aufruft.
Output: VoiceScribe/Services/GroqService.swift — vollständig implementierter actor.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.planning/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-02-PLAN.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-RESEARCH.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-PATTERNS.md

<interfaces>
<!-- Typ-Abhängigkeit aus Plan 03 -->
PromptProfile (VoiceScribe/Models/PromptProfile.swift, nach Plan 03 vorhanden):
```swift
struct PromptProfile: Codable, Defaults.Serializable, Identifiable {
    var id: UUID
    var name: String
    var prompt: String
    var isLLMEnabled: Bool
    var isThinkingEnabled: Bool
    var isDefault: Bool
}
```

Groq API Endpoint (aus RESEARCH.md Pattern 3, verifiziert via console.groq.com):
- URL: https://api.groq.com/openai/v1/chat/completions
- Method: POST
- Headers: Content-Type: application/json, Authorization: Bearer <key>
- Model: "qwen/qwen3-32b" (D-08, fest kodiert)
- Non-thinking: reasoning_effort: "none", temperature: 0.7, top_p: 0.8
- Thinking: reasoning_effort: nil (Feld fehlt im JSON), temperature: 0.6, top_p: 0.95

GroqServiceTests erwartet (aus Plan 02):
```swift
// Zugreifbar via @testable import:
GroqService.ChatRequest(model:messages:temperature:top_p:reasoning_effort:)
GroqService.ChatResponse — decodierbar von JSON {"choices": [...]}
GroqService.GroqError.emptyResponse
GroqService.shared
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GroqService actor implementieren (PROF-05, SET-01, D-07, D-08, D-09, D-10)</name>
  <files>VoiceScribe/Services/GroqService.swift</files>
  <read_first>
    - .planning/phases/05-llm-prompt-profiles/05-RESEARCH.md (Pattern 3 vollständig, Zeilen 244-306; Pitfall 5 Zeilen 486-494)
    - .planning/phases/05-llm-prompt-profiles/05-PATTERNS.md (GroqService-Abschnitt Zeilen 84-191)
    - VoiceScribeTests/GroqServiceTests.swift (was die Tests von der Struct und dem actor erwarten)
    - VoiceScribe/Transcription/TranscriptionService.swift (Concurrency-Pattern: actor + async throws)
  </read_first>
  <behavior>
    Tests die grün werden müssen (aus GroqServiceTests.swift):
    - testNonThinkingRequestEncodesReasoningEffort: JSON enthält "reasoning_effort":"none"
    - testThinkingRequestOmitsReasoningEffort: JSON enthält KEIN "reasoning_effort"-Feld
    - testEndpointIsHTTPS: GroqService.shared existiert, Endpoint-URL hat scheme=="https"
    - testEmptyChoicesYieldsNil: ChatResponse mit leeren choices → choices.first?.message.content == nil
  </behavior>
  <action>
    Neue Datei `VoiceScribe/Services/GroqService.swift` erstellen.

    KRITISCH — Pitfall 5 (RESEARCH.md Zeilen 486-494):
    Swift kodiert `nil`-Optional-Felder standardmäßig als JSON `null`.
    Groq erwartet das Feld bei Thinking-Mode NICHT im JSON (nicht als null).
    LÖSUNG: `encode(to:)` für ChatRequest custom implementieren mit `encodeIfPresent`.

    Vollständiger Datei-Inhalt:
    ```swift
    // VoiceScribe/Services/GroqService.swift
    // Zweck: URLSession-basierter Groq-LLM-Client fuer qwen3-32b (PROF-05).
    // D-07: URLSession direkt — kein Third-Party-SDK.
    // D-08: Modell qwen/qwen3-32b fest kodiert — kein Modell-Picker.
    // D-09: isThinkingEnabled=false → reasoning_effort: "none"; true → Feld fehlt im JSON.
    // D-10: Stille Fallback — Aufrufer (AppDelegate) faengt Fehler ab und gibt Raw-Text aus.
    // T-5-03: HTTPS erzwungen via fest kodierter URL (kein HTTP-Fallback).
    // T-5-01/T-5-02: API-Key wird als Parameter uebergeben — nie gecacht im actor selbst.

    import Foundation

    actor GroqService {
        static let shared = GroqService()

        // T-5-03: HTTPS erzwungen — URL ist literal, kein Fallback auf http://
        private let endpoint = URL(string: "https://api.groq.com/openai/v1/chat/completions")!

        // Timeout: 30s (Claude's Discretion, RESEARCH.md Open Questions #2)
        // Groq hat typisch <3s Latenz; 30s gibt Buffer fuer hohe Last bei langen Transkripten.
        private let timeoutSeconds: TimeInterval = 30

        // MARK: - Request/Response Typen (internal: @testable import braucht Zugriff)

        struct ChatRequest: Encodable {
            let model: String
            let messages: [Message]
            let temperature: Double
            let top_p: Double
            let reasoning_effort: String?   // nil = Thinking (Feld fehlt im JSON); "none" = non-thinking

            struct Message: Encodable {
                let role: String
                let content: String
            }

            // KRITISCH: Custom encode um encodeIfPresent fuer reasoning_effort zu verwenden.
            // Standard-JSONEncoder wuerde nil als "reasoning_effort":null kodieren —
            // Groq-API erwartet das Feld bei Thinking-Mode gaenzlich wegzulassen (Pitfall 5).
            private enum CodingKeys: String, CodingKey {
                case model, messages, temperature, top_p, reasoning_effort
            }

            func encode(to encoder: Encoder) throws {
                var container = encoder.container(keyedBy: CodingKeys.self)
                try container.encode(model, forKey: .model)
                try container.encode(messages, forKey: .messages)
                try container.encode(temperature, forKey: .temperature)
                try container.encode(top_p, forKey: .top_p)
                try container.encodeIfPresent(reasoning_effort, forKey: .reasoning_effort)
            }
        }

        struct ChatResponse: Decodable {
            let choices: [Choice]

            struct Choice: Decodable {
                let message: Message

                struct Message: Decodable {
                    let content: String
                }
            }
        }

        enum GroqError: Error {
            case emptyResponse
        }

        // MARK: - API-Call

        /// Sendet Transkript mit Profil-Prompt an Groq qwen3-32b und gibt verarbeiteten Text zurueck.
        /// D-10: Stille Fallback liegt beim Aufrufer (AppDelegate) — dieser Service wirft Fehler,
        ///       AppDelegate faengt ab und gibt rawText zurueck.
        /// T-5-01/T-5-02: apiKey wird als Parameter empfangen, nie als Property gecacht.
        ///                  Aufrufer liest Key unmittelbar vor dem Aufruf aus Keychain.
        func process(transcript: String, profile: PromptProfile, apiKey: String) async throws -> String {
            // System-Prompt optional: nur senden wenn Profil einen nicht-leeren Prompt hat
            var messages: [ChatRequest.Message] = []
            if !profile.prompt.isEmpty {
                messages.append(.init(role: "system", content: profile.prompt))
            }
            messages.append(.init(role: "user", content: transcript))

            // D-09: Thinking via reasoning_effort (nicht /no_think Prefix — instabil, RESEARCH.md Zeile 412)
            let request = ChatRequest(
                model: "qwen/qwen3-32b",
                messages: messages,
                temperature: profile.isThinkingEnabled ? 0.6 : 0.7,
                top_p: profile.isThinkingEnabled ? 0.95 : 0.8,
                reasoning_effort: profile.isThinkingEnabled ? nil : "none"
            )

            var urlRequest = URLRequest(url: endpoint, timeoutInterval: timeoutSeconds)
            urlRequest.httpMethod = "POST"
            urlRequest.setValue("application/json", forHTTPHeaderField: "Content-Type")
            // T-5-01: API-Key als Bearer-Token — nie in Log-Output schreiben
            urlRequest.setValue("Bearer \(apiKey)", forHTTPHeaderField: "Authorization")
            urlRequest.httpBody = try JSONEncoder().encode(request)

            let (data, _) = try await URLSession.shared.data(for: urlRequest)
            let response = try JSONDecoder().decode(ChatResponse.self, from: data)

            guard let content = response.choices.first?.message.content else {
                throw GroqError.emptyResponse
            }
            return content
        }
    }
    ```

    KEIN Logging des API-Keys — kein `print("Key: \(apiKey)")` oder ähnliches (T-5-01).
    Der actor cached den Key NICHT als Property — er kommt immer als Parameter (T-5-02).
  </action>
  <verify>
    <automated>xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' -only-testing:VoiceScribeTests/GroqServiceTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - VoiceScribe/Services/GroqService.swift existiert
    - grep "actor GroqService" VoiceScribe/Services/GroqService.swift liefert 1 Treffer
    - grep "https://api.groq.com/openai/v1/chat/completions" VoiceScribe/Services/GroqService.swift liefert 1 Treffer
    - grep "encodeIfPresent" VoiceScribe/Services/GroqService.swift liefert 1 Treffer (Pitfall 5 Schutz)
    - grep "qwen/qwen3-32b" VoiceScribe/Services/GroqService.swift liefert 1 Treffer
    - grep "reasoning_effort" VoiceScribe/Services/GroqService.swift liefert ≥2 Treffer
    - grep "apiKey" VoiceScribe/Services/GroqService.swift liefert Treffer NUR als Parameter, NICHT als stored property
    - grep "print.*apiKey\|NSLog.*apiKey" VoiceScribe/Services/GroqService.swift liefert 0 Treffer (T-5-01)
    - xcodebuild test -only-testing:VoiceScribeTests/GroqServiceTests 2>&1 | grep "Test Suite.*passed" — alle 4 Tests grün
  </acceptance_criteria>
  <done>GroqService.swift kompiliert. GroqServiceTests alle 4 Tests grün (Plan 02 GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Groq API | Netzwerk-Boundary: API-Key im Authorization-Header; Response-Daten werden dekodiert |
| Keychain → GroqService | API-Key wird als Parameter übergeben — nie im actor selbst gespeichert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Information Disclosure | API-Key Logging | mitigate | Key wird als Parameter empfangen, nie in print()/NSLog() ausgegeben. Kein stored property für den Key. acceptance_criteria prüft via grep. |
| T-5-02 | Information Disclosure | API-Key Caching | mitigate | `process(transcript:profile:apiKey:)` empfängt Key per Call — actor hat kein `var apiKey` Property. Aufrufer (AppDelegate) liest Keychain unmittelbar vor jedem Call. |
| T-5-03 | Spoofing | HTTPS Enforcement | mitigate | Endpoint-URL ist literal `"https://api.groq.com/..."` — kein HTTP-Fallback möglich. URLSession respektiert ATS-Policy (kein HTTP zu externen Hosts). GroqServiceTests Test 3 prüft scheme=="https". |
</threat_model>

<verification>
`xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' -only-testing:VoiceScribeTests/GroqServiceTests 2>&1 | grep "Test Suite.*passed"` — alle 4 Tests grün.

`xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "BUILD SUCCEEDED"` — kein Compiler-Fehler.
</verification>

<success_criteria>
- GroqService.swift mit actor-Isolation, shared singleton, 30s Timeout
- ChatRequest.encode(to:) mit encodeIfPresent (Pitfall 5 geschlossen)
- reasoning_effort: nil für Thinking (Feld fehlt im JSON), "none" für non-thinking (D-09)
- API-Key nie gecacht, nie geloggt (T-5-01, T-5-02)
- HTTPS-Endpoint fest kodiert (T-5-03)
- Plan 02 GroqServiceTests: alle 4 Tests grün (GREEN-Phase abgeschlossen)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/05-llm-prompt-profiles/05-04-SUMMARY.md` erstellen.
</output>
