---
phase: 05-llm-prompt-profiles
plan: 07
type: execute
wave: 4
depends_on:
  - "05-06"
files_modified: []
autonomous: false
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - SET-01

must_haves:
  truths:
    - "Profil anlegen, Hotkey setzen und LLM aktivieren funktioniert in der laufenden App"
    - "Profil-Hotkey + ⌥⌘R gleichzeitig aktiviert das korrekte Profil"
    - "Icon wechselt zu lila während Groq-Verarbeitung"
    - "Echter Groq-Call liefert verarbeiteten Text ins aktive Textfeld"
    - "Groq API-Key überlebt App-Neustart"
    - "Roter Banner erscheint ohne API-Key, verschwindet nach Eingabe"
  artifacts: []
  key_links: []
---

<objective>
Wave 4: Manuelle Human-Verify-Checkpoints.

Alle automatisierbaren Tests laufen bereits grün (Wave 0-3). Dieser Plan verifiziert die Verhaltensaspekte, die keine Automatisierung erlauben: simultane Hotkeys, visuelle Icon-Zustände, echter Netzwerk-Call, UI-Fluss.

Purpose: Die fünf Success Criteria aus ROADMAP.md §Phase 5 vollständig bestätigen.
Output: Nutzer bestätigt alle Checkpoints oder meldet Issues für Gap-Closure.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.planning/workflows/execute-plan.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/phases/05-llm-prompt-profiles/05-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 1: Full Test Suite grün (automatisierte Baseline)</name>
  <read_first>
    - .planning/phases/05-llm-prompt-profiles/05-VALIDATION.md (Sampling Rate, Wave-0-Requirements)
  </read_first>
  <what-built>
    Wave 0-3 haben implementiert:
    - PromptProfile struct + Defaults.Keys.profiles + KeyboardShortcuts.Name.profile(_:)
    - GroqService actor mit encodeIfPresent, 30s Timeout, HTTPS
    - AppState: activeProfileID + groqKeyMissing
    - AppDelegate: setupProfileHotkeys (onKeyDown), LLM-Routing in onRecordingComplete, Keychain-Check
    - SettingsView: Section "Prompt-Profile" + Groq-Banner + SecureField
    - ProfileEditorSheet: alle 5 Sektionen
  </what-built>
  <how-to-verify>
    Führe im Terminal aus:
    ```
    xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | tail -30
    ```
    Erwartetes Ergebnis: Alle Tests grün, keine Fehler.
    Insbesondere:
    - SPRECHKRAFTTests/PromptProfileTests: 5 Tests grün (PROF-01, PROF-03, PROF-04)
    - SPRECHKRAFTTests/GroqServiceTests: 4 Tests grün (PROF-05, D-09, T-5-03)
    - Alle Phase-1-bis-4-Tests weiterhin grün
  </how-to-verify>
  <resume-signal>Tippe "grün" wenn alle Tests bestanden, oder beschreibe Fehler.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: Settings-UI — Profilverwaltung und Groq API-Key</name>
  <what-built>
    SettingsView mit Section "Prompt-Profile": Profilliste, Groq-Banner, SecureField, ProfileEditorSheet via .sheet(item:).
  </what-built>
  <how-to-verify>
    1. App starten (oder neu starten). Rechtsklick auf Menu-Bar-Icon → "Einstellungen…"
    2. Scrolle zur Section "Prompt-Profile".
    3. PRÜFEN: Roter Banner "Groq API-Schlüssel fehlt" ist sichtbar (kein Key konfiguriert).
    4. Klicke auf "Schlüssel eingeben" → Fokus springt auf das API-Key-Eingabefeld.
    5. PRÜFEN: Banner verschwindet nach Eingabe eines beliebigen Textes (auch Dummy-Text).
    6. Leere das Feld wieder → Banner erscheint erneut.
    7. PRÜFEN: "Rohe Transkription" ist in der Liste mit ⭐-Symbol.
    8. Klicke auf "Rohe Transkription" → Sheet öffnet sich.
    9. PRÜFEN im Sheet: Name-Feld "Rohe Transkription", LLM-Toggle AUS, Thinking-Toggle nicht sichtbar, Prompt-Editor nicht sichtbar.
    10. Schalte LLM-Toggle EIN → Thinking-Toggle und Prompt-Editor erscheinen.
    11. Klicke "Abbrechen" → Sheet schließt, keine Änderungen.
    12. Klicke "Profil hinzufügen" → Sheet öffnet sich mit leerem Namensfeld, Titel "Neues Profil".
    13. Lasse Name-Feld leer → "Profil sichern"-Button ist ausgegraut (disabled).
    14. Gib Namen ein → Button wird aktiv.
    15. PRÜFEN "Profil löschen": Klicke "Rohe Transkription" → Sheet. "Profil löschen" ist ausgegraut (nur 1 Profil). Klicke "Abbrechen".
    16. Füge 2. Profil hinzu und speichere → Liste hat jetzt 2 Einträge.
    17. Öffne erstes Profil erneut → "Profil löschen" ist jetzt aktiv.
  </how-to-verify>
  <resume-signal>Tippe "ui-ok" wenn alle UI-Checks bestanden, oder beschreibe Issues (z.B. "Banner verschwindet nicht", "Sheet öffnet sich nicht").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Simultaner Hotkey (Profil-Hotkey + ⌥⌘R) aktiviert Profil (D-01, PROF-02)</name>
  <what-built>
    AppDelegate.setupProfileHotkeys() registriert onKeyDown-Handler für alle Profil-Hotkeys.
    AppState.activeProfileID wird beim ersten Profil-Hotkey-Event während der Aufnahme gesetzt.
  </what-built>
  <how-to-verify>
    Vorbereitung:
    1. Neues Profil anlegen: Name "Test-LLM", LLM aktivieren, Prompt "Übersetze den folgenden Text ins Englische.", Hotkey z.B. ⌥1 setzen, speichern.
    2. Zweites Profil anlegen: Name "Rohe Transkription" (falls gelöscht — Initial-Profil als Standard).

    Test simultaner Hotkey:
    3. TextEdit öffnen, Cursor ins Textfeld.
    4. ⌥⌘R drücken (Aufnahme starten) — Icon wird rot.
    5. SOFORT zusätzlich ⌥1 gedrückt halten (oder kurz danach — beide Tasten gleichzeitig halten).
    6. Spreche einen deutschen Satz (z.B. "Hallo, das ist ein Test für die Übersetzungsfunktion.").
    7. ⌥⌘R loslassen (Aufnahme stoppen) — Icon wechselt zu lila (Groq-Verarbeitung).
    8. Warte bis Icon zurück zu grau — Text erscheint in TextEdit.
    9. PRÜFEN: Text ist englisch übersetzt (Groq hat verarbeitet).

    Test Default-Profil (kein Profil-Hotkey):
    10. ⌥⌘R drücken, sprechen, loslassen — OHNE ⌥1 gedrückt zu halten.
    11. PRÜFEN: Icon geht direkt zu blau (Transkription), dann grau — KEIN lila (kein LLM).
    12. Text ist rohe Transkription ohne Übersetzung.

    Test Menü-Häkchen:
    13. Rechtsklick auf Icon → Menü öffnet sich.
    14. PRÜFEN: "Rohe Transkription" hat Häkchen (⌃), "Test-LLM" hat keines.
  </how-to-verify>
  <resume-signal>Tippe "hotkey-ok" wenn simultaner Hotkey korrekt funktioniert, oder beschreibe das Problem (z.B. "Icon bleibt rot ohne lila", "Text ist nicht übersetzt").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 4: Echter Groq API-Call + Icon-State (PROF-05, FEED-01, SET-01)</name>
  <what-built>
    GroqService.shared.process() sendet POST an api.groq.com. AppDelegate setzt .llmProcessing während des Calls und .idle danach. Stille Fallback (D-10) bei fehlendem Key.
  </what-built>
  <how-to-verify>
    Test 1 — Echter API-Call:
    1. Gültigen Groq API-Key in Settings eingeben (aus console.groq.com).
    2. LLM-Profil aktivieren (Checkpoint 3 Vorbereitung).
    3. Kurzen Satz sprechen mit Profil-Hotkey gedrückt.
    4. PRÜFEN: Icon-Sequenz ist Rot → Blau → LILA (pulsierend, 1.2s) → Grau.
    5. PRÜFEN: Der verarbeitete Text erscheint im aktiven Textfeld.

    Test 2 — Stille Fallback (D-10):
    6. API-Key aus Settings löschen (Feld leeren) → roter Banner erscheint.
    7. LLM-Profil-Hotkey halten, sprechen, loslassen.
    8. PRÜFEN: Icon-Sequenz ist Rot → Blau → LILA → Grau (LLM-State trotzdem kurz, dann Fallback).
    9. PRÜFEN: Roher Transkriptions-Text erscheint (keine Fehlermeldung, kein Alert) — D-10 stille Fallback.

    Test 3 — Keychain-Persistenz (SET-01):
    10. Gültigen Groq API-Key eingeben.
    11. App beenden (⌘Q oder Rechtsklick → Beenden).
    12. App neu starten.
    13. Settings öffnen → PRÜFEN: API-Key ist noch vorhanden (SecureField zeigt maskierten Inhalt), kein Banner.
    14. LLM-Profil-Hotkey + Aufnahme → Groq-Call funktioniert noch (Key aus Keychain geladen).
  </how-to-verify>
  <resume-signal>Tippe "groq-ok" wenn alle 3 Tests bestanden, oder beschreibe Issues (z.B. "Icon geht nicht lila", "Stiller Fallback zeigt Alert", "Key nach Neustart verloren").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 5: Phase-5-Success-Criteria aus ROADMAP.md vollständig verifizieren</name>
  <what-built>
    Vollständige Phase-5-Implementierung: Profilverwaltung + Hotkey-Aktivierung + LLM-Toggle + Default-Profil + Keychain-Persistenz.
  </what-built>
  <how-to-verify>
    Prüfe jeden Punkt aus ROADMAP.md §Phase 5 Success Criteria:

    SC-1: "User kann Profil anlegen, umbenennen, Prompt bearbeiten und löschen"
    → Profil anlegen (Checkpoint 2 Step 12-14) ✓/✗
    → Profil umbenennen (Sheet öffnen, Name ändern, sichern, Liste prüfen) ✓/✗
    → Prompt bearbeiten (LLM aktivieren, Prompt eingeben, sichern) ✓/✗
    → Profil löschen (≥2 Profile: Löschen-Button aktiv, Profil verschwindet aus Liste) ✓/✗

    SC-2: "Jedes Profil hat eigenen Hotkey; halten während Aufnahme aktiviert Profil"
    → Profil-Hotkey gesetzt und simultaner Hotkey funktioniert (Checkpoint 3) ✓/✗

    SC-3: "Profil mit LLM disabled → Raw-Transkription; LLM enabled → Groq qwen3-32b"
    → Raw-Transkription ohne Hotkey (Checkpoint 3 Test Default-Profil) ✓/✗
    → LLM-Verarbeitung mit echtem API-Key (Checkpoint 4 Test 1) ✓/✗

    SC-4: "Ein Profil als Standard markiert → verwendet wenn kein Hotkey gehalten"
    → "Als Standard markieren" im Sheet: anderen Profilen ⭐ wegnehmen, diesem geben ✓/✗
    → Aufnahme ohne Hotkey → Standard-Profil aktiv (Menü-Häkchen prüfen) ✓/✗

    SC-5: "Groq API-Key einmal eingeben, Keychain, überlebt App-Neustart"
    → Keychain-Persistenz (Checkpoint 4 Test 3) ✓/✗
  </how-to-verify>
  <resume-signal>Tippe "phase5-complete" wenn alle 5 SC erfüllt sind, oder liste offene Punkte (z.B. "SC-4: isDefault-Invariante gebrochen nach Profil-Löschen").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nutzer → Groq API | Echter Netzwerk-Call mit echtem API-Key — nur in Checkpoint 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Information Disclosure | API-Key in Checkpoint-Anweisungen | accept | Checkpoints zeigen KEINE echten Keys. Nutzer gibt eigenen Key ein. Hinweis: Test-2-Fallback testet explizit den Key-freien Pfad zuerst. |
| T-5-02 | Information Disclosure | API-Key Memory während Checkpoint | accept | Low risk — Test dauert Sekunden; Key liegt als lokale Variable in einem Task{}-Scope. Nach Abschluss kein Retention. |
</threat_model>

<verification>
Alle 5 Checkpoints vom Nutzer bestätigt ("grün", "ui-ok", "hotkey-ok", "groq-ok", "phase5-complete").
</verification>

<success_criteria>
- Full Test Suite: alle Unit-Tests grün (PromptProfileTests + GroqServiceTests + Phase-1-bis-4-Tests)
- Settings-UI: Profil-CRUD funktioniert vollständig, Groq-Banner verhält sich korrekt
- Simultaner Hotkey: Profil-Hotkey während Aufnahme aktiviert korrektes Profil
- Groq-Call: Icon lila während Verarbeitung, verarbeiteter Text im Textfeld
- Stille Fallback: kein Alert, nur Raw-Text wenn Key fehlt (D-10)
- Keychain-Persistenz: Key überlebt App-Neustart (SET-01)
- Alle 5 ROADMAP-Success-Criteria bestätigt
</success_criteria>

<output>
Nach Abschluss `.planning/phases/05-llm-prompt-profiles/05-07-SUMMARY.md` erstellen.
</output>
