---
phase: 06-history
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - VoiceScribe/History/HistoryView.swift
  - VoiceScribe.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - HIST-03
  - HIST-04

must_haves:
  truths:
    - "HistoryView zeigt alle Einträge in Datum-Sektionen (HEUTE / GESTERN / DD.MM.YYYY)"
    - "Suchfeld mit 200ms-Debounce filtert Liste via FTS5"
    - "Klick auf Zeile kopiert richtigen Text (LLM wenn vorhanden, sonst Original)"
    - "Zeilenhintergrund blinkt 0.4s grün nach Kopieren"
    - "Leer-Zustand A: 'Noch keine Einträge' wenn DB leer"
    - "Leer-Zustand B: 'Keine Ergebnisse' wenn Suche keine Treffer liefert"
    - "Kontextmenü (Rechtsklick) auf Zeile mit 'Eintrag löschen'"
    - "Button 'Verlauf leeren…' öffnet Confirm-Alert (T6-DELETE mitigiert)"
    - "ValueObservation via task(id:) — kein Task-Leak"
  artifacts:
    - path: "VoiceScribe/History/HistoryView.swift"
      provides: "HistoryView SwiftUI: Listenzeile, Suchfeld, Datum-Gruppen, Copy-Flash, Delete"
      exports: ["HistoryView"]
      min_lines: 150
  key_links:
    - from: "HistoryView.searchText"
      to: "HistoryStore.search(query:)"
      via: ".onChange(of: searchText) → Task.sleep(200ms) → store.search()"
      pattern: "Task.sleep.*milliseconds.*200"
    - from: "HistoryView"
      to: "HistoryStore.observeAll()"
      via: ".task { for try await in store.observeAll() }"
      pattern: "observeAll\\(\\)"
    - from: "copyEntry(_:)"
      to: "NSPasteboard.general"
      via: "setString(entry.copyText, forType: .string)"
      pattern: "NSPasteboard.*setString.*copyText"
---

<objective>
Wave 2: HistoryView.swift als vollständige SwiftUI-View implementieren.
Listenzeilen mit Datum-Sektionen, Live-Suche (debounced 200ms), Copy-Flash (grün 0.4s),
Leer-Zustände, Kontextmenü-Löschen, Confirm-Alert beim Gesamt-Löschen.

Purpose: Das History-Fenster erhält seinen gesamten UI-Inhalt. Wave 3 verdrahtet nur noch
den Menüpunkt und das Window in VoiceScribeApp/AppDelegate.

Output: VoiceScribe/History/HistoryView.swift — vollständig funktionale SwiftUI-View.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-CONTEXT.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-UI-SPEC.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-PATTERNS.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-02-SUMMARY.md

<interfaces>
<!-- HistoryStore-API (aus Wave 1 — exakt diese Signaturen nutzen): -->
@MainActor final class HistoryStore {
    static let shared: HistoryStore
    func insert(_ entry: HistoryEntry) throws
    func delete(_ entry: HistoryEntry) throws
    func deleteAll() throws
    func search(query: String) throws -> [HistoryEntry]
    func observeAll() -> AsyncThrowingStream<[HistoryEntry], Error>
}

<!-- HistoryEntry-API (aus Wave 1): -->
struct HistoryEntry: Identifiable, Sendable {
    var id: Int64?
    var createdAt: Date
    var originalText: String
    var llmText: String?
    var profileName: String?
    var isLLMProcessed: Bool
    var copyText: String   // D-09: llmText ?? originalText
    var preview: String    // D-03: prefix(80) + "…"
}

<!-- DesignTokens (bestehend in VoiceScribe/Constants/DesignTokens.swift): -->
DesignTokens.Spacing.xs  // 4pt  — Badge-Padding
DesignTokens.Spacing.sm  // 8pt  — Zeilen-Padding vertikal
DesignTokens.Spacing.md  // 16pt — Horizontales Padding
DesignTokens.Spacing.lg  // 24pt — Sektions-Abstände
DesignTokens.Spacing.xl  // 32pt — Fensterkanten

<!-- Analog: VoiceScribe/SettingsView.swift (State, onAppear, List, Section, Alert) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HistoryView.swift — vollständige SwiftUI-View</name>
  <read_first>
    - VoiceScribe/History/HistoryStore.swift (API-Signaturen aus Wave 1)
    - VoiceScribe/History/HistoryEntry.swift (Felder: id, createdAt, copyText, preview, isLLMProcessed, profileName)
    - VoiceScribe/SettingsView.swift (Zeilen 1-50: Imports + State; Zeilen 210-270: List + Section + Alert-Pattern)
    - VoiceScribe/Constants/DesignTokens.swift (Spacing-Werte)
    - .planning/phases/06-history/06-UI-SPEC.md (Abschnitte 4-13: Fenstermaß, Listenzeile, Zustände, Copywriting)
    - .planning/phases/06-history/06-CONTEXT.md (D-05: Sektions-Sortierung, D-06: 200ms-Debounce, D-09: Copy-Logik, D-10: Grün-Flash, D-12: Confirm-Dialog)
  </read_first>
  <files>VoiceScribe/History/HistoryView.swift</files>
  <action>
`VoiceScribe/History/HistoryView.swift` erstellen. Die View ist in drei Teile gegliedert:
HistoryView (Haupt-View), HistoryRowView (Listenzeile), groupedEntries (computed property).

**Exakte Copywriting-Strings** (UI-SPEC Abschnitt 11):
- Suchfeld-Placeholder: `"Verlauf durchsuchen…"`
- Empty State A Titel: `"Noch keine Einträge"`
- Empty State A Body: `"Transkriptionen werden hier gespeichert, sobald du diktierst."`
- Empty State B Titel: `"Keine Ergebnisse"`
- Empty State B Body: `"Keine Einträge für „\(searchText)" gefunden."` (typografische Anführungszeichen)
- Button Gesamt-Löschen: `"Verlauf leeren…"` (U+2026)
- Alert Titel: `"Verlauf leeren?"`
- Alert Nachricht: `"Alle Einträge werden unwiderruflich gelöscht."`
- Alert Bestätigen: `"Löschen"` (role: .destructive)
- Alert Abbrechen: `"Abbrechen"` (role: .cancel)
- Kontextmenü-Item: `"Eintrag löschen"` (role: .destructive)
- KI-Badge: `"KI"`

**Sektions-Sortierung** (D-05): Neueste Sektion zuerst.
groupedEntries computed property gruppiert `entries` (bereits ORDER BY created_at DESC) nach Tagesdatum.
Sektions-Schlüssel: "HEUTE" / "GESTERN" / "DD.MM.YYYY".
Sortierung der Sektionen: erstes Element jeder Gruppe (= neuester Eintrag in der Gruppe) nach createdAt DESC.

**Debounce** (D-06, RESEARCH.md Pattern 4): Task.sleep(for: .milliseconds(200)) — kein Combine.
Bei leerem searchText wird observeAll()-Stream konsumiert (nicht search(query: "")).

**Copy-Flash** (D-10, RESEARCH.md Pattern 8): flashingEntryID: Int64? → Zeilenhintergrund
`Color(.systemGreen).opacity(0.3)` für 400ms → nil.
Accessibility-Announcement nach Kopieren: `AccessibilityNotification.Announcement("Kopiert")` (UI-SPEC Abschnitt 12).

**T6-DELETE-Mitigation** (D-12): showClearConfirm: Bool steuert Alert.
SwiftUI `.alert("Verlauf leeren?", isPresented: $showClearConfirm)` mit destructive Button.
Einzellöschen: `.contextMenu { Button("Eintrag löschen", role: .destructive) { ... } }` — kein onDelete (Pitfall 5).

**KI-Badge** (UI-SPEC Abschnitt 6): `Text("KI")`, 10pt semibold, weiß auf systemPurple, cornerRadius 4,
Padding horizontal + vertikal je DesignTokens.Spacing.xs (4pt).

**Accessibility** (UI-SPEC Abschnitt 12):
- Listenzeile: `.accessibilityLabel("\(timeString), \(entry.preview). Tippen zum Kopieren.")`
- KI-Badge: `.accessibilityLabel("KI-verarbeitet")`
- Suchfeld: `.accessibilityLabel("Verlauf durchsuchen")`
- Gesamt-Löschen Button: `.accessibilityLabel("Verlauf leeren")`

**ValueObservation** (RESEARCH.md Pitfall 7): `.task { for try await in historyStore.observeAll() }` —
SwiftUI bricht Task automatisch ab wenn View entfernt wird. NICHT `onAppear` verwenden.
Wenn searchText nicht leer: `.onChange`-Debounce überschreibt `entries` via `store.search()`.

```swift
// VoiceScribe/History/HistoryView.swift
// History-Fenster: Listenzeilen, Live-Suche, Copy-Flash, Delete.
// Implementiert HIST-03 (FTS5-Suche D-06), HIST-04 (Clipboard D-09, D-10),
// T6-DELETE (Confirm-Dialog D-12).
// Quellen: UI-SPEC §4-13; RESEARCH.md Pattern 4, 7, 8; PATTERNS.md HistoryView.

import SwiftUI
import AppKit  // NSPasteboard
import Accessibility  // AccessibilityNotification

// MARK: - HistoryView

struct HistoryView: View {
    @State private var searchText: String = ""
    @State private var entries: [HistoryEntry] = []
    @State private var flashingEntryID: Int64? = nil
    @State private var showClearConfirm: Bool = false
    @State private var debounceTask: Task<Void, Never>? = nil
    private let historyStore = HistoryStore.shared

    var body: some View {
        VStack(spacing: 0) {
            // Toolbar-Bereich: Suchfeld + Verlauf-leeren-Button (UI-SPEC §5)
            HStack(spacing: DesignTokens.Spacing.sm) {
                TextField("Verlauf durchsuchen…", text: $searchText)
                    .textFieldStyle(.roundedBorder)
                    .accessibilityLabel("Verlauf durchsuchen")

                Button("Verlauf leeren…") {
                    showClearConfirm = true
                }
                .accessibilityLabel("Verlauf leeren")
            }
            .padding(.horizontal, DesignTokens.Spacing.md)
            .padding(.vertical, DesignTokens.Spacing.sm)

            Divider()

            // Hauptinhalt: Liste oder Leer-Zustand
            if entries.isEmpty && searchText.isEmpty {
                // Leer-Zustand A: noch keine Einträge (UI-SPEC §9.3)
                emptyStateA
            } else if entries.isEmpty && !searchText.isEmpty {
                // Leer-Zustand B: Suche ohne Treffer (UI-SPEC §9.4)
                emptyStateB
            } else {
                // Normale Liste mit Datum-Sektionen (D-05)
                List {
                    ForEach(groupedEntries, id: \.0) { (sectionTitle, sectionEntries) in
                        Section(sectionTitle) {
                            ForEach(sectionEntries) { entry in
                                HistoryRowView(
                                    entry: entry,
                                    isFlashing: flashingEntryID == entry.id
                                )
                                .contentShape(Rectangle())
                                .onTapGesture { copyEntry(entry) }
                                .contextMenu {
                                    // T6-DELETE (Einzellöschen): Kontextmenü statt onDelete
                                    // (macOS SwiftUI List.onDelete erfordert Edit-Mode — Pitfall 5)
                                    Button("Eintrag löschen", role: .destructive) {
                                        try? historyStore.delete(entry)
                                    }
                                }
                                .accessibilityLabel(accessibilityLabel(for: entry))
                            }
                        }
                    }
                }
                .listStyle(.inset)
            }
        }
        // T6-DELETE: Confirm-Dialog für Gesamt-Löschen (D-12)
        .alert("Verlauf leeren?", isPresented: $showClearConfirm) {
            Button("Löschen", role: .destructive) {
                try? historyStore.deleteAll()
                entries = []
            }
            Button("Abbrechen", role: .cancel) {}
        } message: {
            Text("Alle Einträge werden unwiderruflich gelöscht.")
        }
        // ValueObservation: task(id:) bricht automatisch ab wenn View entfernt wird (Pitfall 7)
        .task(id: searchText.isEmpty) {
            if searchText.isEmpty {
                do {
                    for try await updated in historyStore.observeAll() {
                        entries = updated
                    }
                } catch { /* Observation-Fehler still schlucken */ }
            }
        }
        // Debounce für Suche (D-06: 200ms — Task.sleep-Pattern, kein Combine)
        .onChange(of: searchText) { _, newValue in
            debounceTask?.cancel()
            if newValue.trimmingCharacters(in: .whitespaces).isEmpty {
                // Leer → zurück zur Observation (task(id:) übernimmt)
                return
            }
            debounceTask = Task {
                try? await Task.sleep(for: .milliseconds(200))
                guard !Task.isCancelled else { return }
                entries = (try? historyStore.search(query: newValue)) ?? []
            }
        }
    }

    // MARK: - Leer-Zustände

    private var emptyStateA: some View {
        VStack(spacing: DesignTokens.Spacing.md) {
            Image(systemName: "clock")
                .font(.system(size: 40))
                .foregroundStyle(.secondary)
            Text("Noch keine Einträge")
                .font(.system(size: 16, weight: .semibold))
            Text("Transkriptionen werden hier gespeichert, sobald du diktierst.")
                .font(.system(size: 13))
                .foregroundStyle(.secondary)
                .multilineTextAlignment(.center)
        }
        .padding(DesignTokens.Spacing.xl)
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }

    private var emptyStateB: some View {
        VStack(spacing: DesignTokens.Spacing.md) {
            Image(systemName: "magnifyingglass")
                .font(.system(size: 40))
                .foregroundStyle(.secondary)
            Text("Keine Ergebnisse")
                .font(.system(size: 16, weight: .semibold))
            // UI-SPEC §9.4: typografische Anführungszeichen „…"
            Text("Keine Einträge für „\(searchText)" gefunden.")
                .font(.system(size: 13))
                .foregroundStyle(.secondary)
                .multilineTextAlignment(.center)
        }
        .padding(DesignTokens.Spacing.xl)
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }

    // MARK: - Datum-Gruppierung (D-05)

    /// Einträge nach Tagesdatum gruppiert, neueste Sektion zuerst.
    private var groupedEntries: [(String, [HistoryEntry])] {
        let calendar = Calendar.current
        // Dictionary grouping (entries sind bereits ORDER BY created_at DESC)
        let grouped = Dictionary(grouping: entries) { entry -> String in
            if calendar.isDateInToday(entry.createdAt) { return "HEUTE" }
            if calendar.isDateInYesterday(entry.createdAt) { return "GESTERN" }
            let formatter = DateFormatter()
            formatter.dateFormat = "dd.MM.yyyy"
            return formatter.string(from: calendar.startOfDay(for: entry.createdAt))
        }
        // Sektionen nach neuestem Eintrag in der Gruppe sortieren (D-05: neueste zuerst)
        return grouped.sorted { a, b in
            let dateA = a.value.first?.createdAt ?? .distantPast
            let dateB = b.value.first?.createdAt ?? .distantPast
            return dateA > dateB
        }
    }

    // MARK: - Copy-Feedback (D-09, D-10)

    private func copyEntry(_ entry: HistoryEntry) {
        NSPasteboard.general.clearContents()
        NSPasteboard.general.setString(entry.copyText, forType: .string)
        withAnimation(.easeOut(duration: 0.4)) {
            flashingEntryID = entry.id
        }
        // UI-SPEC §12: Accessibility-Announcement nach Kopieren
        AccessibilityNotification.Announcement("Kopiert").post()
        Task {
            try? await Task.sleep(for: .milliseconds(400))
            withAnimation(.easeOut(duration: 0.4)) {
                flashingEntryID = nil
            }
        }
    }

    // MARK: - Accessibility

    private func accessibilityLabel(for entry: HistoryEntry) -> String {
        let formatter = DateFormatter()
        formatter.dateFormat = "HH:mm"
        let time = formatter.string(from: entry.createdAt)
        let llm = entry.isLLMProcessed ? ", KI-verarbeitet" : ""
        let profile = entry.profileName.map { ", Profil: \($0)" } ?? ""
        return "\(time), \(entry.preview)\(profile)\(llm). Tippen zum Kopieren."
    }
}

// MARK: - HistoryRowView

/// Kompakte Listenzeile: Zeit + 80-Zeichen-Vorschau + optionaler Profilname + optionales KI-Badge.
/// Hintergrundfarbe blinkt grün wenn isFlashing (D-10).
struct HistoryRowView: View {
    let entry: HistoryEntry
    let isFlashing: Bool

    var body: some View {
        HStack(alignment: .top, spacing: DesignTokens.Spacing.sm) {
            // Zeitstempel (UI-SPEC §6: 11pt, monospacedDigit, secondary, min-width 32pt)
            Text(timeString)
                .font(.system(size: 11).monospacedDigit())
                .foregroundStyle(.secondary)
                .frame(minWidth: 32, alignment: .leading)

            VStack(alignment: .leading, spacing: 2) {
                // Vorschautext (UI-SPEC §6: 13pt, max 80 Zeichen + "…", 1 Zeile)
                Text(entry.preview)
                    .font(.system(size: 13))
                    .lineLimit(1)

                // Metadaten-Zeile: Profilname + KI-Badge
                HStack(spacing: DesignTokens.Spacing.xs) {
                    if let profileName = entry.profileName {
                        Text(profileName)
                            .font(.system(size: 11))
                            .foregroundStyle(.secondary)
                    }
                    if entry.isLLMProcessed {
                        // KI-Badge (UI-SPEC §6: 10pt semibold, systemPurple, cornerRadius 4, Padding xs)
                        Text("KI")
                            .font(.system(size: 10, weight: .semibold))
                            .foregroundStyle(.white)
                            .padding(.horizontal, DesignTokens.Spacing.xs)
                            .padding(.vertical, DesignTokens.Spacing.xs)
                            .background(Color(.systemPurple))
                            .clipShape(RoundedRectangle(cornerRadius: 4))
                            .accessibilityLabel("KI-verarbeitet")
                    }
                }
            }

            Spacer()
        }
        .padding(.horizontal, DesignTokens.Spacing.md)
        .padding(.vertical, DesignTokens.Spacing.sm)
        // D-10: Grün-Flash (UI-SPEC §9.5: systemGreen 30% opacity, easeOut 0.4s)
        .background(isFlashing ? Color(.systemGreen).opacity(0.3) : Color.clear)
        .animation(.easeOut(duration: 0.4), value: isFlashing)
    }

    private var timeString: String {
        let formatter = DateFormatter()
        formatter.dateFormat = "HH:mm"
        return formatter.string(from: entry.createdAt)
    }
}
```

HistoryView.swift in project.pbxproj im App-Target eintragen (Sources-Phase).
  </action>
  <verify>
    <automated>xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep -E "error:|warning:" | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls VoiceScribe/History/HistoryView.swift` gibt die Datei zurück
    - `grep "FTS5Pattern\|synchronize\|String interpolation" VoiceScribe/History/HistoryView.swift` — FTS5Pattern nicht direkt in HistoryView (alle DB-Aufrufe gehen via HistoryStore)
    - `grep "showClearConfirm" VoiceScribe/History/HistoryView.swift` findet den T6-DELETE Alert-State
    - `grep '"Verlauf leeren?"' VoiceScribe/History/HistoryView.swift` findet den Alert-Titel
    - `grep '"Löschen".*role.*destructive\|role.*destructive.*"Löschen"' VoiceScribe/History/HistoryView.swift` findet destructive Button
    - `grep '"Eintrag löschen"' VoiceScribe/History/HistoryView.swift` findet Kontextmenü-Item
    - `grep 'Task.sleep.*milliseconds.*200' VoiceScribe/History/HistoryView.swift` findet D-06-Debounce
    - `grep 'milliseconds(400)\|flashingEntryID' VoiceScribe/History/HistoryView.swift` findet D-10-Flash
    - `grep 'setString.*copyText\|copyText.*setString' VoiceScribe/History/HistoryView.swift` findet D-09-Kopierlogik
    - `grep '"KI-verarbeitet"' VoiceScribe/History/HistoryView.swift` findet Accessibility-Label
    - `grep "observeAll" VoiceScribe/History/HistoryView.swift` findet ValueObservation
    - `grep 'task(id:' VoiceScribe/History/HistoryView.swift` findet Pitfall-7-Mitigation
    - `grep "HistoryView.swift" VoiceScribe.xcodeproj/project.pbxproj` findet Eintrag im Target
    - `xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "error:"` — keine Zeilen
  </acceptance_criteria>
  <done>HistoryView.swift vollständig implementiert. T6-DELETE mitigiert via Confirm-Alert. Projekt kompiliert fehlerfrei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-Input (searchText) → HistoryStore.search() | Suchfeld-Text fließt über Debounce in FTS5-Query |
| User-Action (Button "Verlauf leeren…") → HistoryStore.deleteAll() | Bulk-Delete via UI-Button |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T6-FTS5 | Tampering | HistoryView → HistoryStore.search(query:) | mitigate | FTS5-Injection-Schutz liegt in HistoryStore.search() (Wave 1, FTS5Pattern-Binding). HistoryView übergibt nur den rohen searchText — kein SQL-Kontakt im View-Layer. Verifizierbar: kein `FTS5Pattern` Import in HistoryView.swift. |
| T6-DELETE | Tampering | HistoryView "Verlauf leeren…" → HistoryStore.deleteAll() | mitigate | Confirm-Dialog implementiert (D-12): `showClearConfirm = true` → SwiftUI Alert → Benutzer muss explizit "Löschen" (destructive) tippen. Einzellöschen via `.contextMenu` ist ausreichend explizit (kein zusätzlicher Dialog per D-12). |
</threat_model>

<verification>
Nach Wave 2:
1. `xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "error:"` — leer
2. `grep "showClearConfirm" VoiceScribe/History/HistoryView.swift` — vorhanden
3. `grep '"Löschen"' VoiceScribe/History/HistoryView.swift` — vorhanden
4. `grep 'milliseconds(200)' VoiceScribe/History/HistoryView.swift` — vorhanden
5. `grep 'observeAll\|task(id:' VoiceScribe/History/HistoryView.swift` — beide vorhanden
6. `xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "Test.*passed" | wc -l` — 5 (keine Regression)
</verification>

<success_criteria>
- HistoryView.swift vollständig implementiert mit allen D-01 bis D-12 Anforderungen
- Datum-Sektionen (HEUTE / GESTERN / DD.MM.YYYY) via groupedEntries computed property
- Live-Suche 200ms-Debounce via Task.sleep (kein Combine)
- Grün-Flash 400ms via flashingEntryID + withAnimation(.easeOut(duration: 0.4))
- Leer-Zustand A + B mit korrekten Copywriting-Strings
- T6-DELETE mitigiert: Confirm-Alert vor deleteAll(), contextMenu für Einzellöschen
- ValueObservation via .task(id:) (kein Task-Leak)
- Accessibility-Labels nach UI-SPEC §12
- Alle 5 Unit-Tests bleiben grün (keine Regression)
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/06-history/06-03-SUMMARY.md` mit:
- HistoryView.swift Zeilenzahl
- Bestätigung T6-DELETE-Mitigation: Confirm-Alert vorhanden
- Bestätigung Debounce: Task.sleep 200ms vorhanden
- Bestätigung ValueObservation: task(id:) vorhanden
- pbxproj-Eintrag für HistoryView.swift
</output>
