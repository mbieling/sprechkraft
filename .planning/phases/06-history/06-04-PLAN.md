---
phase: 06-history
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - SPRECHKRAFT/AppDelegate.swift
  - SPRECHKRAFT/SPRECHKRAFTApp.swift
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-04

must_haves:
  truths:
    - "Rechtsklick auf Menüleiste zeigt Eintrag 'Verlauf…' vor 'Einstellungen…'"
    - "Klick auf 'Verlauf…' öffnet History-Fenster (SPRECHKRAFT — Verlauf)"
    - "Nach Diktat erscheint neuer Eintrag automatisch in HistoryView"
    - "LLM-verarbeiteter Text und Original werden beide in DB gespeichert"
    - "History-Fenster öffnet korrekt unter .accessory-Activation-Policy"
  artifacts:
    - path: "SPRECHKRAFT/AppDelegate.swift"
      provides: "openHistoryMenu @objc, openHistory Notification.Name, GRDB-Insert in onRecordingComplete (D-15)"
      contains: "openHistory"
      min_lines: 10
    - path: "SPRECHKRAFT/SPRECHKRAFTApp.swift"
      provides: "Window('SPRECHKRAFT — Verlauf', id: 'history') + onReceive(.openHistory)"
      contains: "history"
      min_lines: 10
  key_links:
    - from: "AppDelegate.openHistoryMenu()"
      to: "NotificationCenter.openHistory"
      via: "NotificationCenter.default.post(name: .openHistory)"
      pattern: "post.*openHistory"
    - from: "SPRECHKRAFTApp.onReceive(.openHistory)"
      to: "openWindow(id: 'history')"
      via: "HiddenActivationView + Activation-Policy-Workaround"
      pattern: "openWindow.*history"
    - from: "AppDelegate.onRecordingComplete"
      to: "HistoryStore.shared.insert"
      via: "nach TextOutputService.shared.output() — D-15"
      pattern: "HistoryStore.shared.insert"
---

<objective>
Wave 3: AppDelegate und SPRECHKRAFTApp verdrahten.
Menüpunkt "Verlauf…" einfügen, Window-Scene für History anlegen,
und GRDB-Insert in onRecordingComplete nach TextOutputService einbauen (D-15).

Purpose: Die vollständige Pipeline ist aktiv — nach jedem Diktat wird gespeichert,
und der Nutzer kann die History über das Rechtsklick-Menü öffnen.

Output: Modifizierte AppDelegate.swift + SPRECHKRAFTApp.swift. Keine neuen Dateien.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-CONTEXT.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-RESEARCH.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-PATTERNS.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-03-SUMMARY.md

<interfaces>
<!-- Bestehendes AppDelegate.swift — relevante Stellen (vor Modifikation lesen!): -->
extension Notification.Name {
    static let openSettings = Notification.Name("com.sprechkraft.openSettings")
    static let refreshProfileHotkeys = Notification.Name("com.sprechkraft.refreshProfileHotkeys")
    // NEU: static let openHistory = ...
}

// showMenu() Zeilen 213-300 — Ziel: historyItem VOR settingsItem einfügen
let settingsItem = NSMenuItem(title: "Einstellungen…", ...)  // bestehend

// onRecordingComplete Zeilen 94-159:
// LLM-Pfad: nach TextOutputService.shared.output(outputText, ...) Zeile 146
// Direkt-Pfad: nach TextOutputService.shared.output(text, ...) Zeile 153

/* HistoryStore.shared.insert API (aus Wave 1): */
func insert(_ entry: HistoryEntry) throws  // throws, nicht async — synchroner Write

<!-- Bestehendes SPRECHKRAFTApp.swift — HiddenActivationView: -->
// Zeilen 53-77: onReceive(.openSettings) Pattern — exakt kopieren für .openHistory
.onReceive(NotificationCenter.default.publisher(for: .openSettings)) { _ in
    Task { @MainActor in
        NSApp.setActivationPolicy(.regular)
        NSApp.activate(ignoringOtherApps: true)
        openWindow(id: "settings")
        if let win = NSApp.windows.first(where: { $0.identifier?.rawValue == "settings" }) {
            win.makeKeyAndOrderFront(nil)
        }
        try? await Task.sleep(for: .milliseconds(300))
        NSApp.setActivationPolicy(.accessory)
    }
}

// Zeilen 25-30: Window("SPRECHKRAFT — Einstellungen", id: "settings") { ... }
// NEU: Window("SPRECHKRAFT — Verlauf", id: "history") { HistoryView() }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppDelegate.swift — Menüpunkt + Notification + GRDB-Insert</name>
  <read_first>
    - SPRECHKRAFT/AppDelegate.swift (vollständig lesen — besonders Zeilen 1-25: Notification.Name Extension; Zeilen 94-160: onRecordingComplete; Zeilen 213-310: showMenu und @objc Selektoren)
    - SPRECHKRAFT/History/HistoryStore.swift (insert Signatur: throws, nicht async)
    - SPRECHKRAFT/History/HistoryEntry.swift (Felder für Insert-Erstellung)
    - .planning/phases/06-history/06-CONTEXT.md (D-02: Menüposition, D-15: Insert-Zeitpunkt)
    - .planning/phases/06-history/06-PATTERNS.md (AppDelegate-Sektion: Notification.Name, historyItem, openHistoryMenu, GRDB-Insert-Code)
  </read_first>
  <files>SPRECHKRAFT/AppDelegate.swift</files>
  <action>
Drei separate Änderungen in AppDelegate.swift:

**Änderung 1 — Notification.Name Extension** (Zeilen 17-22, nach .refreshProfileHotkeys):
```swift
// Bestehendes Ende der Extension:
static let refreshProfileHotkeys = Notification.Name("com.sprechkraft.refreshProfileHotkeys")
// NEU hinzufügen:
static let openHistory = Notification.Name("com.sprechkraft.openHistory")
```

**Änderung 2 — NSMenuItem in showMenu()** (D-02: vor "Einstellungen…"):
Suche den Block der `settingsItem` erstellt und einfügt.
Vor diesem Block einfügen:
```swift
// D-02: Verlauf-Menüpunkt — vor "Einstellungen…" (analog zu openSettingsMenu)
let historyItem = NSMenuItem(
    title: "Verlauf\u{2026}",  // U+2026 ELLIPSIS
    action: #selector(openHistoryMenu),
    keyEquivalent: ""
)
historyItem.target = self
menu.addItem(historyItem)
```

**Änderung 3 — @objc Selektor** (nach @objc func openSettingsMenu()):
```swift
@objc private func openHistoryMenu() {
    NotificationCenter.default.post(name: .openHistory, object: nil)
}
```

**Änderung 4 — GRDB-Insert in onRecordingComplete** (D-15: nach TextOutputService, BEIDE Pfade):

Im **LLM-Pfad** (innerhalb des inneren `Task { }` Blocks, nach `TextOutputService.shared.output(outputText, ...)`):
```swift
// D-15: GRDB-Insert nach TextOutputService — letzter Schritt (HIST-01, HIST-02)
// try? — Insert-Fehler darf Transkription nicht blockieren (RESEARCH.md Open Questions #2)
let historyEntry = HistoryEntry(
    id: nil,
    createdAt: Date(),
    originalText: text,
    llmText: outputText != text ? outputText : nil,  // nil wenn Groq-Fallback = Original
    profileName: activeProfile?.name,
    isLLMProcessed: true
)
try? HistoryStore.shared.insert(historyEntry)
```

Im **Direkt-Pfad** (nach `TextOutputService.shared.output(text, ...)` im else-Branch):
```swift
// D-15: GRDB-Insert — Direkt-Pfad (kein LLM)
let historyEntry = HistoryEntry(
    id: nil,
    createdAt: Date(),
    originalText: text,
    llmText: nil,
    profileName: activeProfile?.name,
    isLLMProcessed: false
)
try? HistoryStore.shared.insert(historyEntry)
```

WICHTIG: `HistoryStore.shared` ist @MainActor — beide Insert-Aufrufe liegen im
`await MainActor.run { }` Block, sind also korrekt isoliert.
Das `activeProfile` ist im bestehenden Code bereits verfügbar.
  </action>
  <verify>
    <automated>xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep -E "error:" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "openHistory" SPRECHKRAFT/AppDelegate.swift | wc -l` — mindestens 3 Zeilen (Notification.Name, NSMenuItem-Action, @objc func)
    - `grep 'static let openHistory' SPRECHKRAFT/AppDelegate.swift` — findet Notification.Name Definition
    - `grep '@objc private func openHistoryMenu' SPRECHKRAFT/AppDelegate.swift` — findet Selektor
    - `grep 'Verlauf' SPRECHKRAFT/AppDelegate.swift` — findet NSMenuItem title
    - `grep 'HistoryStore.shared.insert' SPRECHKRAFT/AppDelegate.swift | wc -l` — genau 2 (LLM-Pfad + Direkt-Pfad)
    - `grep 'isLLMProcessed: true' SPRECHKRAFT/AppDelegate.swift` — findet LLM-Pfad-Insert
    - `grep 'isLLMProcessed: false' SPRECHKRAFT/AppDelegate.swift` — findet Direkt-Pfad-Insert
    - `grep 'TextOutputService.*output.*outputText\|outputText.*TextOutputService' SPRECHKRAFT/AppDelegate.swift` — TextOutputService-Aufruf existiert noch (nicht versehentlich gelöscht)
    - `xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep "error:"` — keine Zeilen
  </acceptance_criteria>
  <done>AppDelegate.swift erweitert: openHistory Notification.Name, NSMenuItem "Verlauf…", @objc openHistoryMenu, GRDB-Insert in beiden onRecordingComplete-Pfaden.</done>
</task>

<task type="auto">
  <name>Task 2: SPRECHKRAFTApp.swift — Window-Scene + onReceive(.openHistory)</name>
  <read_first>
    - SPRECHKRAFT/SPRECHKRAFTApp.swift (vollständig lesen — Zeilen 1-80: Window-Scenes, HiddenActivationView, onReceive-Muster)
    - SPRECHKRAFT/History/HistoryView.swift (View-Name bestätigen: HistoryView, kein Init-Parameter)
    - .planning/phases/06-history/06-UI-SPEC.md (§4: Fenstermaß 640×480, minWidth 480, minHeight 320)
    - .planning/phases/06-history/06-PATTERNS.md (SPRECHKRAFTApp-Sektion: Window-Scene-Muster, HiddenActivationView-Erweiterung)
  </read_first>
  <files>SPRECHKRAFT/SPRECHKRAFTApp.swift</files>
  <action>
Zwei separate Änderungen in SPRECHKRAFTApp.swift:

**Änderung 1 — Neue Window-Scene** (in SPRECHKRAFTApp.body, nach dem "settings"-Window):
```swift
// D-01: Dediziertes History-Fenster (nicht Popover, nicht Settings-Tab)
// UI-SPEC §4: 640×480 initial, minWidth 480, minHeight 320
Window("SPRECHKRAFT — Verlauf", id: "history") {
    HistoryView()
        .frame(minWidth: 480, minHeight: 320)
}
.defaultSize(width: 640, height: 480)
.windowResizability(.contentSize)
```

**Änderung 2 — onReceive in HiddenActivationView** (nach dem bestehenden onReceive(.openSettings) Block):
```swift
// D-02: History-Fenster öffnen via NotificationCenter-Brücke (analog .openSettings)
// Pitfall 4: .accessory-Policy-Workaround — exakt identisch zum openSettings-Muster
.onReceive(NotificationCenter.default.publisher(for: .openHistory)) { _ in
    Task { @MainActor in
        NSApp.setActivationPolicy(.regular)
        NSApp.activate(ignoringOtherApps: true)
        openWindow(id: "history")
        if let win = NSApp.windows.first(where: {
            $0.identifier?.rawValue == "history"
        }) {
            win.makeKeyAndOrderFront(nil)
        }
        // Pitfall 4: 300ms Workaround — identisch zu openSettings (STATE.md)
        try? await Task.sleep(for: .milliseconds(300))
        NSApp.setActivationPolicy(.accessory)
    }
}
```

WICHTIG: `@Environment(\.openWindow) private var openWindow` ist bereits in HiddenActivationView vorhanden —
nicht doppelt deklarieren.
  </action>
  <verify>
    <automated>xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep -E "Test.*passed|Test.*failed|error:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"SPRECHKRAFT — Verlauf"' SPRECHKRAFT/SPRECHKRAFTApp.swift` — findet Fenstertitel
    - `grep 'id: "history"' SPRECHKRAFT/SPRECHKRAFTApp.swift | wc -l` — mindestens 2 (Window-Definition + openWindow-Call)
    - `grep 'HistoryView()' SPRECHKRAFT/SPRECHKRAFTApp.swift` — findet View-Instanziierung
    - `grep 'minWidth: 480' SPRECHKRAFT/SPRECHKRAFTApp.swift` — findet Mindestbreite (UI-SPEC §4)
    - `grep 'minHeight: 320' SPRECHKRAFT/SPRECHKRAFTApp.swift` — findet Mindesthöhe (UI-SPEC §4)
    - `grep 'openHistory' SPRECHKRAFT/SPRECHKRAFTApp.swift` — findet onReceive-Handler
    - `grep 'setActivationPolicy.*regular' SPRECHKRAFT/SPRECHKRAFTApp.swift | wc -l` — 2 (settings + history)
    - `grep '"settings"\|"history"' SPRECHKRAFT/SPRECHKRAFTApp.swift` — beide Window-IDs vorhanden
    - `xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep "Test.*passed" | wc -l` — 5 (alle grün, keine Regression)
    - `xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep "error:"` — keine Zeilen
  </acceptance_criteria>
  <done>SPRECHKRAFTApp.swift erweitert: Window-Scene "history" + onReceive(.openHistory) mit Activation-Policy-Workaround. Alle 5 Tests bleiben grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onRecordingComplete → HistoryStore.insert() | Transkriptions-Daten (text, outputText) fließen in DB |
| AppDelegate NSMenuItem → openHistoryMenu → NotificationCenter | Menüklick → Fenster-Öffnung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T6-FTS5 | Tampering | AppDelegate → HistoryStore.insert() | accept | Insert-Pfad enthält keine FTS5-Query. Daten werden als Bindings via GRDB PersistableRecord geschrieben — kein SQL-Injection-Risiko. Die FTS5-Trigger-Synchronisation läuft automatisch via GRDB-internen After-Insert-Trigger. |
| T6-DELETE | Tampering | SPRECHKRAFTApp Window-Öffnung | accept | Window-Öffnung (openHistory) ist keine destruktive Operation. T6-DELETE-Mitigation (Confirm-Alert) liegt in HistoryView (Wave 2). |
</threat_model>

<verification>
Nach Wave 3 (Abschluss aller Implementierungs-Waves):
1. `xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep "Test.*passed" | wc -l` — 5
2. `xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep "Test.*failed"` — leer
3. `grep "HistoryStore.shared.insert" SPRECHKRAFT/AppDelegate.swift | wc -l` — 2
4. `grep "openHistory" SPRECHKRAFT/AppDelegate.swift SPRECHKRAFT/SPRECHKRAFTApp.swift | wc -l` — mindestens 5
5. `grep '"SPRECHKRAFT — Verlauf"' SPRECHKRAFT/SPRECHKRAFTApp.swift` — vorhanden
6. Alle Phasen-6-Anforderungen erfüllt: HIST-01 (Insert+Zeitstempel), HIST-02 (original+llm), HIST-03 (FTS5-Suche via HistoryView), HIST-04 (Clipboard-Kopieren via HistoryView)
</verification>

<success_criteria>
- AppDelegate: openHistory Notification.Name, NSMenuItem "Verlauf…" vor "Einstellungen…", @objc openHistoryMenu, GRDB-Insert in beiden onRecordingComplete-Pfaden (LLM + Direkt)
- SPRECHKRAFTApp: Window("SPRECHKRAFT — Verlauf", id: "history") mit 640×480 / min 480×320, onReceive(.openHistory) mit Activation-Policy-Workaround
- Vollständige Pipeline aktiv: Diktat → TranscriptionService → TextOutputService → HistoryStore.insert → HistoryView (via ValueObservation)
- Alle 5 Unit-Tests grün (keine Regression durch Wiring)
- HIST-01, HIST-02, HIST-03, HIST-04 erfüllt
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/06-history/06-04-SUMMARY.md` mit:
- Bestätigung der 4 Änderungen in AppDelegate.swift (Zeilen)
- Bestätigung der 2 Änderungen in SPRECHKRAFTApp.swift (Zeilen)
- HistoryStore.insert()-Aufrufe: 2 (LLM-Pfad + Direkt-Pfad)
- Test-Ergebnis: 5/5 grün
- Bereit für Wave 4 (Human-Verify)
</output>
