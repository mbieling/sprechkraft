---
phase: 06-history
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - VoiceScribe/History/HistoryEntry.swift
  - VoiceScribe/History/HistoryStore.swift
  - VoiceScribe.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03

must_haves:
  truths:
    - "HistoryEntry ist ein Sendable-konformes struct mit FetchableRecord + PersistableRecord"
    - "HistoryStore ist eine @MainActor final class mit DatabaseQueue"
    - "Tabelle transcription_entries existiert nach Migration"
    - "FTS5-Virtual-Table transcription_entries_fts ist mit synchronize(withTable:) verknüpft"
    - "FTS5-Suche nutzt FTS5Pattern(matchingAllTokensIn:) — kein String-Interpolation"
    - "HistoryStore(inMemory: true) für Tests verfügbar"
    - "Alle 5 Tests in HistoryStoreTests.swift sind GREEN"
  artifacts:
    - path: "VoiceScribe/History/HistoryEntry.swift"
      provides: "HistoryEntry struct: Codable, Identifiable, Sendable, FetchableRecord, PersistableRecord"
      exports: ["HistoryEntry", "copyText", "preview"]
      min_lines: 40
    - path: "VoiceScribe/History/HistoryStore.swift"
      provides: "HistoryStore @MainActor class: DatabaseQueue, Migration v1, insert, delete, deleteAll, search, observeAll"
      exports: ["HistoryStore", "HistoryStore.shared", "HistoryStore(inMemory:)"]
      min_lines: 80
  key_links:
    - from: "VoiceScribe/History/HistoryStore.swift"
      to: "transcription_entries_fts"
      via: "t.synchronize(withTable: \"transcription_entries\")"
      pattern: "synchronize\\(withTable:"
    - from: "HistoryStore.search()"
      to: "transcription_entries_fts"
      via: "FTS5Pattern(matchingAllTokensIn:) als SQL-Argument"
      pattern: "FTS5Pattern\\(matchingAllTokensIn:"
---

<objective>
Wave 1: HistoryEntry struct und HistoryStore Klasse implementieren.
Alle 5 RED-Tests aus Wave 0 sollen nach dieser Wave GREEN sein.

Purpose: Datenbankschicht vollständig aufbauen — Schema, Migrations, CRUD, FTS5, ValueObservation.
HistoryView (Wave 2) und AppDelegate-Wiring (Wave 3) bauen auf dieser Schicht auf.

Output: VoiceScribe/History/HistoryEntry.swift + VoiceScribe/History/HistoryStore.swift;
xcodebuild test gibt 5 grüne Tests zurück.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-CONTEXT.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-RESEARCH.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-PATTERNS.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-01-SUMMARY.md

<interfaces>
<!-- Ziel-Signaturen (aus RESEARCH.md Pattern 2, PATTERNS.md) -->
<!-- HistoryEntry — muss exakt so aussehen, damit Wave-0-Tests grün werden: -->
struct HistoryEntry: Codable, Identifiable, Sendable {
    var id: Int64?
    var createdAt: Date
    var originalText: String
    var llmText: String?
    var profileName: String?
    var isLLMProcessed: Bool
    var copyText: String { llmText ?? originalText }  // D-09
    var preview: String { /* prefix(80) + "…" */ }    // D-03
}
extension HistoryEntry: FetchableRecord, PersistableRecord {
    static var databaseTableName: String { "transcription_entries" }
    enum CodingKeys: String, CodingKey {
        case id
        case originalText = "original_text"
        case llmText = "llm_text"
        case profileName = "profile_name"
        case isLLMProcessed = "is_llm_processed"
        case createdAt = "created_at"
    }
}

<!-- HistoryStore — öffentliche API die Wave-0-Tests erwarten: -->
@MainActor final class HistoryStore {
    static let shared: HistoryStore            // Produktion: Application Support
    init(inMemory: Bool) throws               // Tests: In-Memory-DatabaseQueue
    func insert(_ entry: HistoryEntry) throws
    func delete(_ entry: HistoryEntry) throws
    func deleteAll() throws
    func search(query: String) throws -> [HistoryEntry]
    func observeAll() -> AsyncThrowingStream<[HistoryEntry], Error>
}

<!-- Analog-Pattern: VoiceScribe/Models/PromptProfile.swift (struct-Pattern) -->
<!-- Analog-Pattern: VoiceScribe/Services/GroqService.swift (Singleton-Pattern) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HistoryEntry.swift — Datenmodell</name>
  <read_first>
    - VoiceScribe/Models/PromptProfile.swift (struct-Pattern: Codable, Identifiable, Felder-Deklaration)
    - VoiceScribeTests/HistoryStoreTests.swift (welche Felder rufen die Tests auf?)
    - .planning/phases/06-history/06-CONTEXT.md (D-13: Schema-Felder, D-09: copyText-Logik)
  </read_first>
  <files>VoiceScribe/History/HistoryEntry.swift</files>
  <behavior>
    - HistoryEntry(id: nil, createdAt: Date(), originalText: "X", llmText: nil, profileName: nil, isLLMProcessed: false) kompiliert
    - withLLM.copyText == "LLM" wenn llmText != nil (D-09)
    - withoutLLM.copyText == "Original" wenn llmText == nil (D-09)
    - preview kürzt auf 80 Zeichen + "…" wenn Text länger (D-03)
    - preview liefert vollen Text wenn <= 80 Zeichen
    - FetchableRecord.databaseTableName == "transcription_entries"
    - CodingKeys mappen camelCase ↔ snake_case korrekt
  </behavior>
  <action>
Neues Verzeichnis `VoiceScribe/History/` anlegen und `VoiceScribe/History/HistoryEntry.swift` erstellen.

```swift
// VoiceScribe/History/HistoryEntry.swift
// Datenmodell für einen Transkriptions-Verlaufseintrag.
// Implementiert HIST-01 (Zeitstempel), HIST-02 (original + LLM Text),
// HIST-04 (copyText: D-09 LLM > Original).
// Quellen: RESEARCH.md Pattern 2; PATTERNS.md HistoryEntry; D-13, D-09, D-03.

import Foundation
import GRDB

/// Ein gespeicherter Transkriptionseintrag.
/// struct (nicht class) — GRDB v7 erzwingt Sendable für Record-Typen in async-Kontexten (Pitfall 3).
struct HistoryEntry: Codable, Identifiable, Sendable {
    // GRDB autoIncrementedPrimaryKey → Int64? (nil vor dem ersten Insert)
    var id: Int64?
    /// Zeitpunkt der Transkription (D-13: created_at DATETIME NOT NULL)
    var createdAt: Date
    /// Rohtext direkt vom Transcription-Service (D-13: original_text TEXT NOT NULL)
    var originalText: String
    /// LLM-verarbeiteter Text; nil wenn LLM nicht aktiv war (D-13: llm_text TEXT nullable)
    var llmText: String?
    /// Name des aktiven Prompt-Profils; nil wenn kein Profil (D-13: profile_name TEXT nullable)
    var profileName: String?
    /// true wenn der Eintrag durch Groq LLM verarbeitet wurde (D-13: is_llm_processed BOOLEAN NOT NULL)
    var isLLMProcessed: Bool

    // MARK: - Computed Properties

    /// D-09: LLM-Text hat Vorrang beim Kopieren — spiegelt tatsächlichen Output.
    var copyText: String {
        llmText ?? originalText
    }

    /// D-03: ~80-Zeichen-Vorschau für die Listenzeile.
    var preview: String {
        let base = copyText
        guard base.count > 80 else { return base }
        return String(base.prefix(80)) + "…"
    }
}

// MARK: - GRDB Conformances

extension HistoryEntry: FetchableRecord, PersistableRecord {
    /// Datenbankname (D-13: Tabelle transcription_entries)
    static var databaseTableName: String { "transcription_entries" }

    /// CodingKeys für snake_case ↔ camelCase Mapping.
    enum CodingKeys: String, CodingKey {
        case id
        case createdAt = "created_at"
        case originalText = "original_text"
        case llmText = "llm_text"
        case profileName = "profile_name"
        case isLLMProcessed = "is_llm_processed"
    }
}
```

Anschließend HistoryEntry.swift in project.pbxproj im VoiceScribe-App-Target (Sources-Phase AA000070 o.ä.)
eintragen — analog zu anderen Swift-Dateien im Projekt. Exakte Stelle: nach den bestehenden Sources-Einträgen.
  </action>
  <verify>
    <automated>xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep -E "error:|HistoryEntry" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls VoiceScribe/History/HistoryEntry.swift` gibt die Datei zurück
    - `grep "FetchableRecord, PersistableRecord" VoiceScribe/History/HistoryEntry.swift` findet die Conformance
    - `grep 'databaseTableName.*transcription_entries' VoiceScribe/History/HistoryEntry.swift` findet die Konstante
    - `grep 'createdAt = "created_at"' VoiceScribe/History/HistoryEntry.swift` findet das CodingKey-Mapping
    - `grep 'llmText ?? originalText' VoiceScribe/History/HistoryEntry.swift` findet D-09-Logik
    - `grep "prefix(80)" VoiceScribe/History/HistoryEntry.swift` findet D-03-Logik
    - `xcodebuild build -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "error:"` — keine Zeilen
  </acceptance_criteria>
  <done>HistoryEntry.swift angelegt und im pbxproj registriert; Projekt kompiliert fehlerfrei.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HistoryStore.swift — Datenbankschicht</name>
  <read_first>
    - VoiceScribe/History/HistoryEntry.swift (gerade erstellt — Felder und Signaturen)
    - VoiceScribeTests/HistoryStoreTests.swift (exakte Signaturen die die Tests erwarten)
    - VoiceScribe/Services/GroqService.swift (Singleton-Pattern als Vorlage)
    - .planning/phases/06-history/06-RESEARCH.md (Pattern 1: Migration, Pattern 3: FTS5-Suche, Pattern 5: ValueObservation)
  </read_first>
  <files>VoiceScribe/History/HistoryStore.swift</files>
  <behavior>
    - HistoryStore.shared ist @MainActor-Singleton
    - HistoryStore(inMemory: true) erzeugt In-Memory-DatabaseQueue (kein Filesystem-State)
    - migrate() erstellt Tabelle "transcription_entries" mit allen 6 Spalten (D-13)
    - migrate() erstellt FTS5-Virtual-Table "transcription_entries_fts" mit synchronize(withTable:) (D-14)
    - insert(_:) schreibt Eintrag via dbQueue.write
    - delete(_:) löscht Eintrag; FTS5-Index wird automatisch via synchronize-Trigger aktualisiert
    - deleteAll() löscht alle Einträge
    - search(query: "") liefert alle Einträge ORDER BY created_at DESC
    - search(query: "Term") nutzt FTS5Pattern(matchingAllTokensIn:) — T6-FTS5-Mitigation
    - search() gibt [] zurück wenn FTS5Pattern nil liefert (ungültiger Query, Pitfall 6)
    - observeAll() gibt AsyncThrowingStream<[HistoryEntry], Error> zurück
    - 1000-Einträge-FTS5-Suche unter 200ms (testSearchPerformance)
  </behavior>
  <action>
`VoiceScribe/History/HistoryStore.swift` erstellen:

```swift
// VoiceScribe/History/HistoryStore.swift
// Service-Klasse für GRDB-Datenbankzugriffe (History-Persistenz).
// Implementiert HIST-01 (Insert mit Zeitstempel), HIST-02 (original + LLM),
// HIST-03 (FTS5-Volltextsuche mit FTS5Pattern-Binding — T6-FTS5).
// @MainActor: alle Aufrufer (AppDelegate, HistoryView) laufen auf dem Main Thread.
// Quellen: RESEARCH.md Pattern 1, 3, 5; PATTERNS.md HistoryStore; D-13, D-14, D-15.

import Foundation
import GRDB

@MainActor
final class HistoryStore {

    // MARK: - Singleton (Produktion)

    static let shared: HistoryStore = {
        try! HistoryStore(productionDB: true)
    }()

    private let dbQueue: DatabaseQueue

    // MARK: - Init

    /// Produktions-Initialisierung: Application Support Directory (Claude's Discretion).
    private init(productionDB: Bool) throws {
        let fileManager = FileManager.default
        let appSupport = try fileManager.url(
            for: .applicationSupportDirectory,
            in: .userDomainMask,
            appropriateFor: nil,
            create: true
        )
        let dir = appSupport.appendingPathComponent("VoiceScribe", isDirectory: true)
        try fileManager.createDirectory(at: dir, withIntermediateDirectories: true)
        let dbURL = dir.appendingPathComponent("history.sqlite")
        dbQueue = try DatabaseQueue(path: dbURL.path)
        try migrate()
    }

    /// Test-Initialisierung: In-Memory-DatabaseQueue (kein Filesystem-Zustand).
    /// Wird ausschließlich in HistoryStoreTests verwendet.
    init(inMemory: Bool) throws {
        dbQueue = try DatabaseQueue()  // ohne Pfad = In-Memory (RESEARCH.md Validation Architecture)
        try migrate()
    }

    // MARK: - Migration

    private func migrate() throws {
        var migrator = DatabaseMigrator()
        migrator.registerMigration("v1_transcription_entries") { db in
            // D-13: Haupt-Tabelle
            try db.create(table: "transcription_entries") { t in
                t.autoIncrementedPrimaryKey("id")
                t.column("created_at", .datetime).notNull()
                t.column("original_text", .text).notNull()
                t.column("llm_text", .text)           // NULL wenn kein LLM-Pfad
                t.column("profile_name", .text)        // NULL wenn kein Profil
                t.column("is_llm_processed", .boolean).notNull()
            }
            // D-14: FTS5-Virtual-Table mit automatischer Trigger-Synchronisation.
            // synchronize(withTable:) erzeugt After-Insert/Update/Delete-Trigger automatisch.
            // Niemals manuell schreiben — GRDB kennt das korrekte FTS5-Trigger-Format (RESEARCH.md Anti-Patterns).
            try db.create(virtualTable: "transcription_entries_fts", using: FTS5()) { t in
                t.synchronize(withTable: "transcription_entries")
                t.tokenizer = .unicode61()  // Unicode-Support für deutsche Umlaute (RESEARCH.md OQ1)
                t.column("original_text")
                t.column("llm_text")
            }
        }
        try migrator.migrate(dbQueue)
    }

    // MARK: - CRUD

    /// Speichert einen neuen HistoryEntry in der Datenbank.
    /// D-15: Wird nach TextOutputService.output() aufgerufen (in AppDelegate.onRecordingComplete).
    func insert(_ entry: HistoryEntry) throws {
        try dbQueue.write { db in
            try entry.insert(db)
        }
    }

    /// Löscht einen einzelnen Eintrag.
    /// FTS5-Index wird automatisch via synchronize-Trigger aktualisiert (Pitfall 2).
    func delete(_ entry: HistoryEntry) throws {
        try dbQueue.write { db in
            try entry.delete(db)
        }
    }

    /// Löscht alle Einträge (D-12: Gesamt-Löschen mit vorhergehendem Confirm-Dialog in HistoryView).
    func deleteAll() throws {
        try dbQueue.write { db in
            try HistoryEntry.deleteAll(db)
        }
    }

    // MARK: - Suche

    /// Sucht Einträge via FTS5.
    /// Leerer Query → alle Einträge ORDER BY created_at DESC.
    /// T6-FTS5-Mitigation: FTS5Pattern(matchingAllTokensIn:) als Binding — kein String-Interpolation.
    func search(query: String) throws -> [HistoryEntry] {
        try dbQueue.read { db in
            if query.trimmingCharacters(in: .whitespaces).isEmpty {
                return try HistoryEntry.order(Column("created_at").desc).fetchAll(db)
            }
            // T6-FTS5: FTS5Pattern ist safe für User-Input — nil bei ungültigem Pattern (Pitfall 6)
            guard let pattern = FTS5Pattern(matchingAllTokensIn: query) else {
                return []  // Sicherer Fallback: kein Absturz bei ungültigem FTS5-Token
            }
            // Explizite SQL-Variante für Content-Table Setup (RESEARCH.md Pattern 3)
            return try HistoryEntry.fetchAll(db,
                sql: """
                    SELECT transcription_entries.*
                    FROM transcription_entries
                    WHERE transcription_entries.rowid IN (
                        SELECT rowid FROM transcription_entries_fts
                        WHERE transcription_entries_fts MATCH ?
                    )
                    ORDER BY created_at DESC
                """,
                arguments: [pattern])
        }
    }

    // MARK: - Observation

    /// AsyncThrowingStream für automatisches List-Update bei neuen Einträgen.
    /// In HistoryView mit task(id:) Modifier konsumieren — verhindert Task-Leak (Pitfall 7).
    func observeAll() -> AsyncThrowingStream<[HistoryEntry], Error> {
        ValueObservation
            .tracking { db in
                try HistoryEntry.order(Column("created_at").desc).fetchAll(db)
            }
            .values(in: dbQueue)
    }
}
```

Anschließend HistoryStore.swift in project.pbxproj im VoiceScribe-App-Target eintragen (Sources-Phase).
  </action>
  <verify>
    <automated>xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep -E "Test.*passed|Test.*failed|error:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls VoiceScribe/History/HistoryStore.swift` gibt die Datei zurück
    - `grep "@MainActor" VoiceScribe/History/HistoryStore.swift` findet die Isolation
    - `grep "synchronize(withTable:" VoiceScribe/History/HistoryStore.swift` findet FTS5-Sync (D-14)
    - `grep "FTS5Pattern(matchingAllTokensIn:" VoiceScribe/History/HistoryStore.swift` findet T6-FTS5-Mitigation
    - `grep 'transcription_entries_fts.*MATCH' VoiceScribe/History/HistoryStore.swift` findet FTS5-SQL
    - `grep 'init(inMemory:' VoiceScribe/History/HistoryStore.swift` findet Test-Initialisierung
    - `grep "HistoryStore.swift" VoiceScribe.xcodeproj/project.pbxproj` findet den Eintrag im Target
    - `xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "Test.*passed"` — 5 Tests grün
    - Kein `grep "String interpolation" VoiceScribe/History/HistoryStore.swift` (kein SQL-Interpolation)
  </acceptance_criteria>
  <done>HistoryStore.swift vollständig implementiert. Alle 5 HistoryStoreTests sind GREEN. T6-FTS5 mitigiert via FTS5Pattern-Binding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-Input → FTS5-Query | Suchtext aus HistoryView.searchText wird an HistoryStore.search() übergeben |
| AppDelegate → HistoryStore.insert() | onRecordingComplete-Daten (text, llmText) werden in DB geschrieben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T6-FTS5 | Tampering | HistoryStore.search(query:) | mitigate | `FTS5Pattern(matchingAllTokensIn: query)` als Binding — User-Input wird niemals via String-Interpolation in MATCH-Clause eingebaut. nil-Rückgabe bei ungültigem Pattern → leere Ergebnisliste (kein Crash, kein Injection-Risiko). In search() Zeile mit `guard let pattern = FTS5Pattern(...)` verifizierbar. |
| T6-DELETE | Tampering | HistoryStore.deleteAll() | mitigate | Confirm-Dialog (Alert mit destructive Button) wird in Wave 2 in HistoryView implementiert (D-12). HistoryStore.deleteAll() selbst hat keine Guard — der Guard liegt im UI-Layer (HistoryView). |
</threat_model>

<verification>
Nach Wave 1:
1. `xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "Test.*passed" | wc -l` — 5
2. `xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "Test.*failed"` — leer
3. `grep "FTS5Pattern(matchingAllTokensIn:" VoiceScribe/History/HistoryStore.swift` — vorhanden
4. `grep "synchronize(withTable:" VoiceScribe/History/HistoryStore.swift` — vorhanden
5. `grep "String interpolation\|MATCH '\\\\(" VoiceScribe/History/HistoryStore.swift` — leer (kein SQL-Injection-Risiko)
</verification>

<success_criteria>
- HistoryEntry: struct, Sendable, Codable, FetchableRecord, PersistableRecord, databaseTableName = "transcription_entries"
- HistoryStore: @MainActor, DatabaseQueue, Migration v1, insert/delete/deleteAll/search/observeAll implementiert
- FTS5-Virtual-Table mit synchronize(withTable:) und unicode61-Tokenizer
- FTS5-Suche via FTS5Pattern-Binding (T6-FTS5 mitigiert)
- In-Memory-Init für Tests verfügbar
- Alle 5 HistoryStoreTests grün
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/06-history/06-02-SUMMARY.md` mit:
- Datei-Pfade und Zeilenzahlen (HistoryEntry.swift, HistoryStore.swift)
- Test-Ergebnis (5/5 grün)
- Bestätigung T6-FTS5-Mitigation: `FTS5Pattern(matchingAllTokensIn:)` vorhanden
- pbxproj-Einträge für History/HistoryEntry.swift und History/HistoryStore.swift
</output>
