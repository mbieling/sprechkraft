---
phase: 06-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SPRECHKRAFT.xcodeproj/project.pbxproj
  - SPRECHKRAFTTests/HistoryStoreTests.swift
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03

must_haves:
  truths:
    - "Das Projekt kompiliert fehlerfrei nach GRDB-SPM-Integration"
    - "xcodebuild test schlägt fehl (RED) weil HistoryStore noch nicht existiert"
    - "GRDB ist in allen drei pbxproj-Pflichtstellen eingetragen"
  artifacts:
    - path: "SPRECHKRAFTTests/HistoryStoreTests.swift"
      provides: "RED-Stubs für HIST-01, HIST-02, HIST-03 (testInsertPersists, testBothTextsStored, testFTS5SearchFindsMatch, testSearchPerformance, testCopyPreference)"
      min_lines: 60
    - path: "SPRECHKRAFT.xcodeproj/project.pbxproj"
      provides: "GRDB SPM-Dependency in 3 Pflicht-Stellen"
      contains: "GR060600"
  key_links:
    - from: "project.pbxproj packageReferences"
      to: "GR060600 XCRemoteSwiftPackageReference"
      via: "PBXProject-Eintrag"
      pattern: "GR060600"
    - from: "project.pbxproj packageProductDependencies"
      to: "GR060601 GRDB"
      via: "Target-Eintrag"
      pattern: "GR060601"
---

<objective>
Wave-0-Build-Gate: GRDB als SPM-Dependency in project.pbxproj eintragen (3 Pflichtstellen)
und HistoryStoreTests.swift mit failing RED-Stubs anlegen. Dieses Paket blockiert Wave 1.

Purpose: Verhindert, dass Wave-1-Implementierung gegen falsche Interfaces gebaut wird.
Die Tests definieren die erwarteten HistoryStore-Signaturen, bevor HistoryStore existiert.

Output: Compiliertes Projekt mit GRDB-Import + 5 failing Tests.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/PROJECT.md
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/STATE.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-CONTEXT.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-RESEARCH.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-PATTERNS.md

<interfaces>
<!-- Existierende pbxproj-Pattern für SPM (KeychainAccess als Vorlage) -->
<!-- project.pbxproj Zeilen 226-232: packageProductDependencies im Target -->
packageProductDependencies = (
    AA000021 /* KeyboardShortcuts */,
    AA000023 /* LaunchAtLogin */,
    AA000025 /* Defaults */,
    BEEF0028 /* WhisperKit */,
    KC050501 /* KeychainAccess */,
);

<!-- project.pbxproj Zeilen 283-289: packageReferences in PBXProject -->
packageReferences = (
    AA000100 /* XCRemoteSwiftPackageReference "KeyboardShortcuts" */,
    AA000101 /* XCRemoteSwiftPackageReference "LaunchAtLogin-modern" */,
    AA000102 /* XCRemoteSwiftPackageReference "Defaults" */,
    DEAD0103 /* XCRemoteSwiftPackageReference "argmax-oss-swift" */,
    KC050500 /* XCRemoteSwiftPackageReference "KeychainAccess" */,
);

<!-- project.pbxproj Zeilen 623-658: XCRemoteSwiftPackageReference + XCSwiftPackageProductDependency -->
KC050500 /* XCRemoteSwiftPackageReference "KeychainAccess" */ = {
    isa = XCRemoteSwiftPackageReference;
    repositoryURL = "https://github.com/kishikawakatsumi/KeychainAccess";
    requirement = { kind = upToNextMajorVersion; minimumVersion = 4.0.0; };
};
KC050501 /* KeychainAccess */ = {
    isa = XCSwiftPackageProductDependency;
    package = KC050500 /* XCRemoteSwiftPackageReference "KeychainAccess" */;
    productName = KeychainAccess;
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GRDB SPM-Dependency in project.pbxproj (3 Pflichtstellen)</name>
  <read_first>
    - SPRECHKRAFT.xcodeproj/project.pbxproj (vollständige aktuelle Struktur lesen, besonders Zeilen 220-295 und 590-664)
  </read_first>
  <files>SPRECHKRAFT.xcodeproj/project.pbxproj</files>
  <action>
GRDB.swift v7.5.0 in project.pbxproj an exakt drei Stellen eintragen.
Verwende die Object-IDs GR060600 (XCRemoteSwiftPackageReference) und GR060601 (XCSwiftPackageProductDependency).

**Stelle 1 — XCRemoteSwiftPackageReference** (nach KC050500-Block, vor "End XCRemoteSwiftPackageReference section"):
```
GR060600 /* XCRemoteSwiftPackageReference "GRDB.swift" */ = {
    isa = XCRemoteSwiftPackageReference;
    repositoryURL = "https://github.com/groue/GRDB.swift";
    requirement = {
        kind = upToNextMajorVersion;
        minimumVersion = 7.5.0;
    };
};
```

**Stelle 2 — packageReferences in PBXProject** (in der Liste nach KC050500):
```
GR060600 /* XCRemoteSwiftPackageReference "GRDB.swift" */,
```

**Stelle 3a — XCSwiftPackageProductDependency** (nach KC050501-Block, vor "End XCSwiftPackageProductDependency section"):
```
GR060601 /* GRDB */ = {
    isa = XCSwiftPackageProductDependency;
    package = GR060600 /* XCRemoteSwiftPackageReference "GRDB.swift" */;
    productName = GRDB;
};
```

**Stelle 3b — packageProductDependencies im SPRECHKRAFT Target** (nach KC050501):
```
GR060601 /* GRDB */,
```

NICHT in den SPRECHKRAFTTests-Target eintragen — Tests importieren GRDB via @testable import SPRECHKRAFT (HistoryStore bringt GRDB mit).
  </action>
  <verify>
    <automated>grep -c "GR060600\|GR060601" SPRECHKRAFT.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GR060600" SPRECHKRAFT.xcodeproj/project.pbxproj` gibt 4 zurück (1× Definition + 1× packageReferences + 1× package-Referenz in GR060601 + 1× Kommentar in packageProductDependencies)
    - `grep "groue/GRDB.swift" SPRECHKRAFT.xcodeproj/project.pbxproj` gibt genau eine Zeile zurück
    - `grep "minimumVersion = 7.5.0" SPRECHKRAFT.xcodeproj/project.pbxproj` gibt eine Zeile zurück
    - `grep "GR060601" SPRECHKRAFT.xcodeproj/project.pbxproj` gibt mindestens 3 Zeilen zurück (Definition + packageProductDependencies + package-Referenz innerhalb)
    - `xcodebuild -scheme SPRECHKRAFT -destination 'platform=macOS' build 2>&1 | tail -5` endet ohne "error:"
  </acceptance_criteria>
  <done>GRDB ist in allen 3 Pflicht-Stellen von project.pbxproj eingetragen; Projekt kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: HistoryStoreTests.swift — RED-Stubs anlegen</name>
  <read_first>
    - SPRECHKRAFTTests/ (ls: welche Test-Dateien existieren bereits?)
    - SPRECHKRAFT.xcodeproj/project.pbxproj (Zeilen 237-253: SPRECHKRAFTTests-Target-Struktur, welche Quell-Dateien sind gelistet?)
  </read_first>
  <files>SPRECHKRAFTTests/HistoryStoreTests.swift</files>
  <action>
Neue Datei `SPRECHKRAFTTests/HistoryStoreTests.swift` anlegen mit 5 RED-Stubs (Swift Testing).
Die Stubs rufen HistoryStore und HistoryEntry auf, die noch nicht existieren — Test-Kompilation
schlägt bewusst fehl (oder Tests schlagen zur Laufzeit fehl, wenn HistoryStore als leere Klasse
vorübergehend existiert).

Strategie: Stubs kompilieren gegen zukünftige HistoryStore-API — wenn Wave 1 HistoryStore anlegt,
müssen alle Stubs grün werden ohne Änderung an dieser Datei.

```swift
// SPRECHKRAFTTests/HistoryStoreTests.swift
// RED-Stubs für Phase 6 History (HIST-01, HIST-02, HIST-03, HIST-04)
// Wave 0: Tests MÜSSEN fehlschlagen bis Wave 1 HistoryStore implementiert.

import Testing
import Foundation
@testable import SPRECHKRAFT

@MainActor
struct HistoryStoreTests {

    // In-Memory-Instanz für Tests — kein Filesystem-Zustand (RESEARCH.md Validation Architecture)
    private func makeStore() throws -> HistoryStore {
        try HistoryStore(inMemory: true)
    }

    // HIST-01: Insert speichert Eintrag mit created_at
    @Test func testInsertPersists() throws {
        let store = try makeStore()
        let entry = HistoryEntry(
            id: nil,
            createdAt: Date(),
            originalText: "Hallo Welt",
            llmText: nil,
            profileName: nil,
            isLLMProcessed: false
        )
        try store.insert(entry)
        let all = try store.search(query: "")
        #expect(all.count == 1)
        #expect(all.first?.originalText == "Hallo Welt")
    }

    // HIST-02: Beide Texte (original + LLM) werden gespeichert
    @Test func testBothTextsStored() throws {
        let store = try makeStore()
        let entry = HistoryEntry(
            id: nil,
            createdAt: Date(),
            originalText: "Rohtext",
            llmText: "Verarbeiteter Text",
            profileName: "Notizen",
            isLLMProcessed: true
        )
        try store.insert(entry)
        let all = try store.search(query: "")
        #expect(all.first?.llmText == "Verarbeiteter Text")
        #expect(all.first?.profileName == "Notizen")
        #expect(all.first?.isLLMProcessed == true)
    }

    // HIST-03: FTS5-Suche findet Eintrag der den Term enthält
    @Test func testFTS5SearchFindsMatch() throws {
        let store = try makeStore()
        let entry = HistoryEntry(
            id: nil,
            createdAt: Date(),
            originalText: "Besprechungsnotizen Quartalsbericht",
            llmText: nil,
            profileName: nil,
            isLLMProcessed: false
        )
        try store.insert(entry)
        let results = try store.search(query: "Quartalsbericht")
        #expect(results.count == 1)
        #expect(results.first?.originalText == "Besprechungsnotizen Quartalsbericht")
    }

    // HIST-03: FTS5-Suche liefert Ergebnis unter 200ms bei 1000 Einträgen
    @Test func testSearchPerformance() throws {
        let store = try makeStore()
        for i in 0..<1000 {
            let entry = HistoryEntry(
                id: nil,
                createdAt: Date().addingTimeInterval(TimeInterval(-i * 60)),
                originalText: "Eintrag Nummer \(i) mit verschiedenem Inhalt",
                llmText: i % 2 == 0 ? "LLM Text \(i)" : nil,
                profileName: nil,
                isLLMProcessed: i % 2 == 0
            )
            try store.insert(entry)
        }
        let start = Date()
        let results = try store.search(query: "Inhalt")
        let elapsed = Date().timeIntervalSince(start)
        #expect(results.count > 0)
        #expect(elapsed < 0.2, "FTS5-Suche dauerte \(elapsed)s — Limit: 200ms")
    }

    // HIST-04: copyText liefert LLM-Text wenn vorhanden, sonst Original (D-09)
    @Test func testCopyPreference() throws {
        let withLLM = HistoryEntry(
            id: nil, createdAt: Date(),
            originalText: "Original", llmText: "LLM",
            profileName: nil, isLLMProcessed: true
        )
        #expect(withLLM.copyText == "LLM")

        let withoutLLM = HistoryEntry(
            id: nil, createdAt: Date(),
            originalText: "Original", llmText: nil,
            profileName: nil, isLLMProcessed: false
        )
        #expect(withoutLLM.copyText == "Original")
    }
}
```

Anschließend muss die neue Datei im SPRECHKRAFTTests-Target in project.pbxproj eingetragen werden.
Prüfe zuerst mit grep, ob die pbxproj bereits eine "Sources"-Phase für SPRECHKRAFTTests hat und trage
`HistoryStoreTests.swift` dort analog zu bestehenden Test-Dateien ein.
  </action>
  <verify>
    <automated>xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep -E "error:|HistoryStore|HIST" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls SPRECHKRAFTTests/HistoryStoreTests.swift` gibt die Datei zurück
    - `grep "@Test func testInsertPersists" SPRECHKRAFTTests/HistoryStoreTests.swift` findet den Test
    - `grep "@Test func testFTS5SearchFindsMatch" SPRECHKRAFTTests/HistoryStoreTests.swift` findet den Test
    - `grep "@Test func testSearchPerformance" SPRECHKRAFTTests/HistoryStoreTests.swift` findet den Test
    - `grep "@Test func testCopyPreference" SPRECHKRAFTTests/HistoryStoreTests.swift` findet den Test
    - `grep "HistoryStoreTests.swift" SPRECHKRAFT.xcodeproj/project.pbxproj` findet den Eintrag im Target
    - `xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep "error:"` gibt keine Zeilen zurück (App kompiliert — Tests scheitern zur Laufzeit, nicht zur Compile-Zeit, weil HistoryStore noch fehlt)
    - Hinweis: Falls HistoryStore noch fehlt und Kompilation scheitert, ist ein minimaler Stub-File akzeptabel bis Wave 1
  </acceptance_criteria>
  <done>HistoryStoreTests.swift mit 5 Tests angelegt und im pbxproj registriert. Tests sind RED (scheitern bei fehlender HistoryStore-Implementation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keine in Wave 0 | Wave 0 berührt keine User-Input-Verarbeitung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T6-FTS5 | Tampering | HistoryStore.search() | mitigate | FTS5Pattern(matchingAllTokensIn:) als Binding — Wave 0 definiert den Test-Contract; Wave 1 Implementierung muss diesen Binding-Pfad nutzen. Kein String-Interpolation in MATCH-Clauses. |
| T6-DELETE | Tampering | HistoryStore.deleteAll() | mitigate | Confirm-Dialog (Alert mit destructive Button, D-12) — Wave 0 legt testCopyPreference an; Wave 2 implementiert den Alert. |
</threat_model>

<verification>
Nach Wave 0:
1. `xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS'` — 0 errors (App-Target kompiliert)
2. `grep "GR060600\|GR060601" SPRECHKRAFT.xcodeproj/project.pbxproj | wc -l` — mindestens 6 Zeilen
3. `grep "groue/GRDB.swift" SPRECHKRAFT.xcodeproj/project.pbxproj` — genau 1 Zeile
4. `ls SPRECHKRAFTTests/HistoryStoreTests.swift` — Datei existiert
5. `grep "@Test" SPRECHKRAFTTests/HistoryStoreTests.swift | wc -l` — 5 Tests
</verification>

<success_criteria>
- GRDB v7.5.0 ist in project.pbxproj an 3 Pflichtstellen eingetragen (GR060600 + GR060601)
- Projekt kompiliert fehlerfrei (App-Target)
- SPRECHKRAFTTests/HistoryStoreTests.swift mit 5 failing Tests angelegt
- Tests definieren erwartete Signaturen: HistoryStore(inMemory:), insert(_:), search(query:), HistoryEntry.copyText
- Keine Änderung an produktiven Swift-Dateien in Wave 0
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/06-history/06-01-SUMMARY.md` mit:
- Welche pbxproj-Zeilen geändert wurden (mit Zeilennummern)
- Object-IDs GR060600 / GR060601 bestätigt
- Test-Datei-Pfad und Anzahl Tests
- Kompilierungs-Status (grün / Fehler)
</output>
