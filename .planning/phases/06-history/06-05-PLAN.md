---
phase: 06-history
plan: 05
type: execute
wave: 5
depends_on:
  - 06-04
files_modified: []
autonomous: false
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04

must_haves:
  truths:
    - "Menüpunkt 'Verlauf…' sichtbar im Rechtsklick-Menü"
    - "History-Fenster öffnet sich via Menüklick"
    - "Nach Diktat erscheint Eintrag mit Zeitstempel in HistoryView"
    - "Datum-Sektionen HEUTE / GESTERN korrekt angezeigt"
    - "FTS5-Suche liefert Ergebnisse bei Eingabe in Suchfeld"
    - "Klick auf Zeile kopiert Text und zeigt grünen Flash"
    - "Leer-Zustand 'Noch keine Einträge' sichtbar bei leerer DB"
    - "Suche ohne Treffer zeigt 'Keine Ergebnisse für …'"
    - "Einzellöschen via Rechtsklick-Kontextmenü funktioniert"
    - "Gesamt-Löschen zeigt Confirm-Alert mit 'Löschen' / 'Abbrechen'"
  artifacts: []
  key_links: []
---

<objective>
Wave 4: Manuelle Human-Verify-Checkpoints für alle visuellen und interaktiven Verhaltensweisen,
die nicht per Unit-Test verifizierbar sind.

Purpose: Sicherstellen dass HIST-01 bis HIST-04 aus Nutzersicht vollständig funktionieren.
Phase 6 gilt erst nach bestandenen Checkpoints als abgeschlossen.

Output: Benutzer bestätigt jede Verification. Anschließend Phase-6-SUMMARY erstellen.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-CONTEXT.md
@/Users/mbieling/claude/voice/.planning/phases/06-history/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automatische Abschluss-Verifikation vor Human-Verify</name>
  <read_first>
    - .planning/phases/06-history/06-04-SUMMARY.md (bestätigt alle Wires gesetzt)
  </read_first>
  <files></files>
  <action>
Vor den manuellen Checkpoints die automatische Test-Suite ausführen
und sicherstellen dass alle 5 Unit-Tests grün sind:

```bash
xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep -E "Test.*passed|Test.*failed|error:"
```

Wenn Tests fehlschlagen: Abbruch — Wiring aus Wave 3 (Plan 04) korrigieren, nicht fortfahren.
  </action>
  <verify>
    <automated>xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep -E "Test.*passed|Test.*failed" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "Test.*failed"` — leer (keine fehlgeschlagenen Tests)
    - `xcodebuild test -scheme VoiceScribe -destination 'platform=macOS' 2>&1 | grep "Test.*passed" | wc -l` — 5
  </acceptance_criteria>
  <done>5/5 Tests grün. Bereit für manuelle Verifikation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 1: Menüpunkt und Fenster-Öffnung</name>
  <what-built>
    AppDelegate: NSMenuItem "Verlauf…" vor "Einstellungen…".
    VoiceScribeApp: Window-Scene "history" mit onReceive(.openHistory) + Activation-Policy-Workaround.
  </what-built>
  <how-to-verify>
    1. App starten (oder neu starten falls bereits läuft)
    2. Rechtsklick auf Menüleisten-Icon
    3. **Prüfen:** Eintrag "Verlauf…" erscheint vor "Einstellungen…"
    4. Auf "Verlauf…" klicken
    5. **Prüfen:** Fenster "VoiceScribe — Verlauf" öffnet sich im Vordergrund
    6. **Prüfen:** Fenster ist mindestens 480×320pt groß und resizierbar
    7. **Prüfen:** Suchfeld "Verlauf durchsuchen…" ist oben sichtbar
    8. **Prüfen:** Bei leerer DB zeigt Fenster "Noch keine Einträge" (Leer-Zustand A)
    9. Fenster schließen — Menüleisten-Icon bleibt erhalten (kein Dock-Icon erscheint)
  </how-to-verify>
  <resume-signal>Tippe "ok" wenn alle 9 Punkte bestätigt, oder beschreibe das Problem</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: Diktat → History-Eintrag erscheint</name>
  <what-built>
    AppDelegate.onRecordingComplete: HistoryStore.shared.insert() nach TextOutputService (D-15).
    HistoryView: ValueObservation via observeAll() aktualisiert Liste automatisch.
  </what-built>
  <how-to-verify>
    1. History-Fenster öffnen (bleibt offen)
    2. Hotkey drücken (⌥⌘R), etwas sprechen (z.B. "Test Eintrag für die History"), Hotkey erneut drücken
    3. **Prüfen (HIST-01):** Neuer Eintrag erscheint automatisch in HistoryView ohne manuelles Aktualisieren
    4. **Prüfen:** Eintrag zeigt Zeitstempel (HH:MM), z.B. "14:32"
    5. **Prüfen:** Eintrag erscheint unter Sektion "HEUTE"
    6. **Prüfen (HIST-02):** Eintrag zeigt korrekte Textvorschau (~80 Zeichen)
    7. Zweiten Diktat-Durchgang machen mit aktivem LLM-Profil (falls verfügbar)
    8. **Prüfen (HIST-02):** Eintrag mit LLM zeigt KI-Badge (lila "KI") und Profil-Name
  </how-to-verify>
  <resume-signal>Tippe "ok" wenn Einträge korrekt erscheinen, oder beschreibe das Problem</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: FTS5-Suche und Leer-Zustände</name>
  <what-built>
    HistoryView: TextField mit 200ms-Debounce → HistoryStore.search() via FTS5Pattern.
    Leer-Zustand B bei keinen Treffern.
  </what-built>
  <how-to-verify>
    1. History-Fenster mit mindestens 1 Eintrag öffnen
    2. Einen Begriff aus dem Eintrag in das Suchfeld eingeben (z.B. erstes Wort des Transkripts)
    3. **Prüfen (HIST-03):** Liste filtert sich nach ~200ms auf passende Einträge
    4. Suchbegriff ändern, der keine Einträge matcht (z.B. "xyzabc123")
    5. **Prüfen:** Leer-Zustand B erscheint: "Keine Ergebnisse" + "Keine Einträge für „xyzabc123" gefunden."
    6. **Prüfen:** Typografische Anführungszeichen „…" korrekt (nicht ASCII "…")
    7. Suchfeld leeren (× klicken oder Text löschen)
    8. **Prüfen:** Vollständige Liste kehrt zurück (Leer-Zustand B verschwindet)
  </how-to-verify>
  <resume-signal>Tippe "ok" wenn Suche korrekt funktioniert, oder beschreibe das Problem</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 4: Kopieren und Grün-Flash</name>
  <what-built>
    HistoryView.copyEntry(): NSPasteboard + flashingEntryID + withAnimation(.easeOut(duration: 0.4)).
    D-09: LLM-Text hat Vorrang beim Kopieren.
  </what-built>
  <how-to-verify>
    1. History-Fenster mit mindestens 1 Eintrag öffnen
    2. Auf einen Eintrag klicken (gesamte Zeile tappable)
    3. **Prüfen (HIST-04):** Zeilenhintergrund blinkt kurz grün (~0.4s) und kehrt zurück
    4. In eine App wechseln (z.B. TextEdit) und ⌘V drücken
    5. **Prüfen (HIST-04):** Korrekter Text wird eingefügt
    6. Falls ein LLM-verarbeiteter Eintrag (KI-Badge) vorhanden:
       - Auf diesen Eintrag klicken
       - **Prüfen (D-09):** LLM-Text (nicht Original-Transkript) wird eingefügt
  </how-to-verify>
  <resume-signal>Tippe "ok" wenn Kopieren und Flash korrekt funktionieren, oder beschreibe das Problem</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 5: Löschen — Einzeln und Gesamt</name>
  <what-built>
    Einzellöschen: .contextMenu "Eintrag löschen" (role: .destructive).
    Gesamt-Löschen: Button "Verlauf leeren…" → SwiftUI Alert mit destructive "Löschen" (T6-DELETE).
  </what-built>
  <how-to-verify>
    **Einzellöschen (Rechtsklick-Kontextmenü):**
    1. History-Fenster mit mindestens 2 Einträgen öffnen
    2. Rechtsklick auf einen Eintrag
    3. **Prüfen:** Kontextmenü erscheint mit "Eintrag löschen" (rot/destructive)
    4. "Eintrag löschen" klicken
    5. **Prüfen:** Eintrag verschwindet sofort aus der Liste
    6. **Prüfen:** Kein Confirm-Dialog für Einzellöschen (erwartetes Verhalten)

    **Gesamt-Löschen (Confirm-Alert):**
    1. Noch mindestens 1 Eintrag in der Liste
    2. Button "Verlauf leeren…" klicken
    3. **Prüfen (T6-DELETE):** Alert erscheint mit Titel "Verlauf leeren?"
    4. **Prüfen:** Alert zeigt Nachricht "Alle Einträge werden unwiderruflich gelöscht."
    5. **Prüfen:** Alert hat "Löschen" (rot/destructive) + "Abbrechen" (neutral)
    6. "Abbrechen" klicken — Liste unverändert
    7. Wieder "Verlauf leeren…" klicken → Alert → "Löschen" klicken
    8. **Prüfen:** Alle Einträge gelöscht; Leer-Zustand A "Noch keine Einträge" erscheint
  </how-to-verify>
  <resume-signal>Tippe "ok" wenn Löschen korrekt funktioniert, oder beschreibe das Problem</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human-Verify | Manueller Checkpoint — keine Code-Änderungen in Wave 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T6-FTS5 | Tampering | FTS5-Suche via Suchfeld (Checkpoint 3) | mitigate | Visuell verifiziert: Suche liefert korrekte Ergebnisse ohne Injection. Die technische Mitigation (FTS5Pattern-Binding) ist in Wave 1 implementiert und via Unit-Test bestätigt. |
| T6-DELETE | Tampering | "Verlauf leeren…" Button (Checkpoint 5) | mitigate | Visuell verifiziert: Alert mit "Löschen" / "Abbrechen" erscheint. Destructive-Farbe korrekt. Kein Bulk-Delete ohne explizite Bestätigung möglich. |
</threat_model>

<verification>
Human-Verify-Ergebnisse:
- [ ] Checkpoint 1: Menüpunkt + Fenster-Öffnung — bestätigt
- [ ] Checkpoint 2: Diktat → Eintrag erscheint — bestätigt
- [ ] Checkpoint 3: FTS5-Suche + Leer-Zustände — bestätigt
- [ ] Checkpoint 4: Kopieren + Grün-Flash — bestätigt
- [ ] Checkpoint 5: Löschen (Einzeln + Gesamt mit Confirm) — bestätigt
</verification>

<success_criteria>
- Alle 5 Checkpoints vom Benutzer als "ok" bestätigt
- HIST-01: Eintrag erscheint nach Diktat mit Zeitstempel und Datum-Sektion ✓
- HIST-02: Original + LLM-Text korrekt gespeichert; KI-Badge bei LLM-Einträgen ✓
- HIST-03: FTS5-Suche mit 200ms-Debounce; Leer-Zustand B bei keinen Treffern ✓
- HIST-04: Klick kopiert LLM-Text (D-09) mit grünem Flash (D-10) ✓
- T6-DELETE: Confirm-Alert vor Gesamt-Löschen ✓
- Phase 6 vollständig abgeschlossen
</success_criteria>

<output>
Nach Abschluss aller Checkpoints erstelle `.planning/phases/06-history/06-05-SUMMARY.md` mit:
- Alle Checkpoints bestätigt (Datum + "ok")
- HIST-01 bis HIST-04: alle erfüllt
- T6-FTS5 + T6-DELETE: beide mitigiert und verifiziert

Danach ROADMAP.md aktualisieren:
- Phase 6 als [x] markieren (completed YYYY-MM-DD)
- Plans: 5 plans (alle 5 als [x])

STATE.md aktualisieren:
- stopped_at: Phase 6 complete — History (DATUM)
- completed_phases: 6
- completed_plans: 28 (23 + 5)
</output>
