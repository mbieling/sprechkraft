---
phase: 03-transcription
plan: "02"
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - SPRECHKRAFT/Audio/AudioController.swift
  - SPRECHKRAFT/AppState.swift
  - SPRECHKRAFTTests/AppStateTests.swift
autonomous: true
requirements:
  - RECORD-04
  - RECORD-05

must_haves:
  truths:
    - "AudioController akkumuliert Float-Samples waehrend der Aufnahme im installTap-Callback"
    - "AudioController.stopRecording() uebergibt akkumuliertes [Float]-Array via onRecordingComplete-Callback"
    - "AppState besitzt isModelReady: Bool = false Property auf @MainActor"
    - "AppState-Tests sind gruen"
  artifacts:
    - path: "SPRECHKRAFT/Audio/AudioController.swift"
      provides: "Float-Sample-Akkumulation + onRecordingComplete Callback"
      contains: "var onRecordingComplete: (([Float], Double) -> Void)?"
    - path: "SPRECHKRAFT/AppState.swift"
      provides: "isModelReady: Bool Property"
      contains: "var isModelReady: Bool = false"
    - path: "SPRECHKRAFTTests/AppStateTests.swift"
      provides: "Test fuer isModelReady-Initialzustand"
      contains: "testIsModelReadyInitiallyFalse"
  key_links:
    - from: "SPRECHKRAFT/Audio/AudioController.swift"
      to: "SPRECHKRAFT/AppDelegate.swift"
      via: "onRecordingComplete callback"
      pattern: "onRecordingComplete"
    - from: "SPRECHKRAFT/AppState.swift"
      to: "SPRECHKRAFT/AppDelegate.swift"
      via: "appState.isModelReady"
      pattern: "isModelReady"
---

<objective>
Erweitert AudioController um Float-Sample-Akkumulation und den `onRecordingComplete`-Callback,
und fuegt `isModelReady: Bool` zu AppState hinzu. Beides sind die Empfaenger- und Sender-Enden
der Transkriptions-Pipeline — kein TranscriptionService-Code in diesem Plan.

Purpose: AudioController liefert nach stopRecording() das vollstaendige Audio-Array. AppState
stellt die Download-Blockierlogik bereit. Beide Erweiterungen sind isoliert testbar.

Output: AudioController mit Sample-Buffer, AppState mit isModelReady, AppStateTests gruen.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transcription/03-RESEARCH.md
@.planning/phases/03-transcription/03-PATTERNS.md

<interfaces>
<!-- Bestehende AudioController.swift Struktur — Erweiterungspunkte -->

Aus SPRECHKRAFT/Audio/AudioController.swift (Lines 20-45):
```swift
final class AudioController: @unchecked Sendable {
    private let engine = AVAudioEngine()
    private var silenceAccumulator: TimeInterval = 0
    private var lastDispatchedLevel: CGFloat = -1
    private let silenceThresholdRMS: Float = 0.01
    private weak var appState: AppState?
    var onAutoStop: (() -> Void)?
    var onLevelUpdate: (() -> Void)?
```

Aus SPRECHKRAFT/Audio/AudioController.swift installTap (Lines 97-118):
```swift
engine.inputNode.installTap(onBus: 0, bufferSize: 1024, format: format) { [weak self] buffer, _ in
    guard let self else { return }
    let rms = self.calculateRMS(buffer: buffer)
    let bufferDuration = Double(buffer.frameLength) / buffer.format.sampleRate
    self.updateSilenceDetection(rms: rms, bufferDuration: bufferDuration)
    let clampedLevel = CGFloat(min(1.0, rms * 4.0))
    guard abs(clampedLevel - self.lastDispatchedLevel) > 0.05 else { return }
    self.lastDispatchedLevel = clampedLevel
    Task { @MainActor [weak self] in
        self?.appState?.audioLevel = clampedLevel
        self?.onLevelUpdate?()
    }
}
```

Aus SPRECHKRAFT/Audio/AudioController.swift stopRecording (Lines 127-133):
```swift
func stopRecording() {
    engine.inputNode.removeTap(onBus: 0)
    engine.stop()
    silenceAccumulator = 0
    lastDispatchedLevel = -1
}
```

Aus SPRECHKRAFT/AppState.swift (Lines 58-71):
```swift
@MainActor
@Observable
final class AppState {
    var recordingState: RecordingState = .idle
    var audioLevel: CGFloat = 0.0
    var micPermissionDenied: Bool = false
    init() {}
```

<!-- onAutoStop-Dispatch-Pattern (AudioController.swift Lines 171-173) als Vorbild fuer onRecordingComplete: -->
```swift
Task { @MainActor [weak self] in
    self?.onAutoStop?()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AudioController — Float-Sample-Akkumulation + onRecordingComplete</name>
  <files>SPRECHKRAFT/Audio/AudioController.swift</files>

  <read_first>
    - SPRECHKRAFT/Audio/AudioController.swift — vollstaendige aktuelle Datei lesen; exakte Zeilennummern fuer die drei Erweiterungspunkte verstehen (Properties-Block, installTap-Closure, stopRecording)
    - .planning/phases/03-transcription/03-PATTERNS.md §AudioController — Pattern 3 (Erweiterung): exakte Code-Snippets fuer recordedSamples, installTap-Ergaenzung, stopRecording-Ergaenzung
    - .planning/phases/03-transcription/03-RESEARCH.md §Pitfall 5: Memory-Spike-Erklaerung warum recordedSamples = [] VOR Task-Dispatch erfolgen muss
  </read_first>

  <behavior>
    - AudioController hat `private var recordedSamples: [Float] = []` Property
    - AudioController hat `var onRecordingComplete: (([Float], Double) -> Void)?` Property
    - Nach startRecording() wird recordedSamples bei jedem installTap-Callback befuellt
    - Nach stopRecording(): Array-Kopie extrahiert, recordedSamples = [] geleert, dann onRecordingComplete via Task { @MainActor } aufgerufen
    - sampleRate im onRecordingComplete-Callback ist die Hardware-Rate aus `engine.inputNode.outputFormat(forBus: 0).sampleRate`
  </behavior>

  <action>
Erweitere AudioController.swift an drei Punkten (NICHT umschreiben — nur Ergaenzungen):

**Punkt 1: Zwei neue Properties im Properties-Block** (nach `var onLevelUpdate: (() -> Void)?`, Zeile ~45):
```swift
/// Akkumulierte Float-Samples waehrend der Aufnahme. Nur auf Audio-Render-Thread geschrieben
/// (@unchecked Sendable Invariante). Wird in stopRecording() entleert.
private var recordedSamples: [Float] = []

/// Callback nach stopRecording() mit dem vollstaendigen Sample-Array.
/// Parameter: ([Float] samples, Double sampleRate) — sampleRate ist Hardware-Rate (z.B. 44100 oder 48000).
/// Wird via Task { @MainActor } aufgerufen (konsistent mit onAutoStop-Pattern).
var onRecordingComplete: (([Float], Double) -> Void)?
```

**Punkt 2: Sample-Akkumulation im installTap-Callback** (nach updateSilenceDetection-Aufruf, VOR dem
lastDispatchedLevel-Guard — wichtig: IMMER akkumulieren, nicht nur wenn Level-Dispatch feuert):
```swift
// Phase 3: Float-Sample-Akkumulation fuer Transkription (D-04, D-05)
// Render-Thread-safe: recordedSamples nur hier geschrieben (@unchecked Sendable)
if let channelData = buffer.floatChannelData?[0] {
    let count = Int(buffer.frameLength)
    let newSamples = Array(UnsafeBufferPointer(start: channelData, count: count))
    recordedSamples.append(contentsOf: newSamples)
}
```

**Punkt 3: stopRecording() erweitern** (VOR engine.inputNode.removeTap(onBus: 0) — Array extrahieren
BEVOR der Tap gestoppt wird, damit kein Sample verloren geht):
```swift
// Phase 3: Sample-Array extrahieren und Callback ausloesen (D-05)
// recordedSamples zuerst leeren (vor Task-Dispatch) um Memory-Spike zu vermeiden (RESEARCH.md Pitfall 5)
let capturedSamples = recordedSamples
let capturedSampleRate = engine.inputNode.outputFormat(forBus: 0).sampleRate
recordedSamples = []
Task { @MainActor [weak self] in
    self?.onRecordingComplete?(capturedSamples, capturedSampleRate)
}
```

Danach folgen die bestehenden Zeilen: `engine.inputNode.removeTap(onBus: 0)`, `engine.stop()`,
`silenceAccumulator = 0`, `lastDispatchedLevel = -1`.

WICHTIG: `recordedSamples = []` muss VOR dem `Task { @MainActor in }` stehen (Pitfall 5:
Original freigeben bevor Kopie im Task verarbeitet wird).

NICHT aendern: calculateRMS, updateSilenceDetection, startRecording, requestPermissionIfNeeded.
  </action>

  <verify>
    <automated>xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep -E "error:|Build succeeded"</automated>
  </verify>

  <acceptance_criteria>
    - AudioController.swift enthaelt `private var recordedSamples: [Float] = []`
    - AudioController.swift enthaelt `var onRecordingComplete: (([Float], Double) -> Void)?`
    - AudioController.swift enthaelt `recordedSamples.append(contentsOf: newSamples)` im installTap-Block
    - AudioController.swift enthaelt `let capturedSamples = recordedSamples` in stopRecording()
    - AudioController.swift enthaelt `recordedSamples = []` VOR `Task { @MainActor` in stopRecording()
    - `xcodebuild build -scheme SPRECHKRAFT` exitiert mit Code 0 (kein Kompilierfehler)
    - SWIFT_STRICT_CONCURRENCY=complete: Keine neuen Concurrency-Warnungen
  </acceptance_criteria>

  <done>
    AudioController akkumuliert Float-Samples waehrend der Aufnahme und ruft onRecordingComplete
    nach stopRecording() mit dem vollstaendigen Array und der Hardware-Samplerate auf.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppState.isModelReady + AppStateTests-Erweiterung</name>
  <files>SPRECHKRAFT/AppState.swift, SPRECHKRAFTTests/AppStateTests.swift</files>

  <read_first>
    - SPRECHKRAFT/AppState.swift — vollstaendige aktuelle Datei lesen; insbesondere Properties-Block (Zeilen 61-69) als Vorlage fuer neue Property
    - SPRECHKRAFTTests/AppStateTests.swift — vollstaendige aktuelle Datei lesen; bestehende Tests nicht beschaedigen; Teststruktur fuer neuen Test verstehen
    - .planning/phases/03-transcription/03-PATTERNS.md §AppState.swift: exakte Property-Deklaration als Analog
    - .planning/phases/03-transcription/03-CONTEXT.md §D-11: isModelReady wird von startRecordingWithCue() geprueft
  </read_first>

  <behavior>
    - AppState hat `var isModelReady: Bool = false` Property
    - isModelReady startet als false (kein Modell geladen bei App-Start)
    - AppStateTests enthaelt Test `testIsModelReadyInitiallyFalse` der prueft dass isModelReady == false nach init()
    - Bestehende AppState-Tests laufen unveraendert gruen
  </behavior>

  <action>
**AppState.swift — eine Zeile einfuegen:**

Nach `var micPermissionDenied: Bool = false` (aktuell Zeile ~69), neue Property ergaenzen:
```swift
/// true wenn TranscriptionService.downloadAndLoad() erfolgreich abgeschlossen hat.
/// Gesetzt von AppDelegate nach abgeschlossenem Download (D-09, D-11).
/// Pruefung in startRecordingWithCue(): guard appState.isModelReady — blockiert Aufnahme waehrend Download.
var isModelReady: Bool = false
```

@MainActor gilt fuer die gesamte Klasse — keine zusaetzliche Annotation noetig.

**AppStateTests.swift — einen neuen Test am Ende der Struct einfuegen** (vor der letzten `}`):
```swift
@Test("isModelReady startet als false (RECORD-05)")
func testIsModelReadyInitiallyFalse() {
    let state = AppState()
    #expect(state.isModelReady == false,
            "isModelReady muss false sein bevor Download abgeschlossen ist")
}
```

NICHT aendern: bestehende Tests in AppStateTests.swift, AppState-Methoden toggleRecording() und resetToIdle().
  </action>

  <verify>
    <automated>xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' -only-testing SPRECHKRAFTTests/AppStateTests 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - AppState.swift enthaelt `var isModelReady: Bool = false`
    - AppStateTests.swift enthaelt `func testIsModelReadyInitiallyFalse()`
    - AppStateTests.swift enthaelt `#expect(state.isModelReady == false`
    - `xcodebuild test -only-testing SPRECHKRAFTTests/AppStateTests` exitiert mit Code 0 (alle Tests gruen, inklusive neuer)
    - `xcodebuild build -scheme SPRECHKRAFT` exitiert mit Code 0
  </acceptance_criteria>

  <done>
    AppState.isModelReady: Bool = false ist deklariert. AppStateTests hat neuen gruen laufenden
    Test testIsModelReadyInitiallyFalse. Alle bestehenden AppState-Tests unveraendert gruen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audio-Render-Thread → Main Thread | recordedSamples wird auf Render-Thread geschrieben, via Task { @MainActor } uebergeben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | recordedSamples Akkumulation (unbegrenzt) | accept | Phase 3: Maximale Aufnahmedauer ist durch Silence-Detection begrenzt (~30s typisch). Explizite Maximaldauer-Guard ist als Phase-N-Item in RESEARCH.md §Security erfasst. |
| T-03-04 | Tampering | onRecordingComplete Callback-Pointer | accept | Callback wird nur in setupAudioController() gesetzt (@MainActor AppDelegate); kein externer Zugriff moeglich |
</threat_model>

<verification>
- `xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS'` exitiert mit Code 0
- AudioController.swift: grep "onRecordingComplete" gibt mindestens 2 Treffer (Deklaration + Aufruf)
- AppState.swift: grep "isModelReady" gibt mindestens 1 Treffer
- `xcodebuild test -only-testing SPRECHKRAFTTests/AppStateTests` exitiert mit Code 0
</verification>

<success_criteria>
AudioController akkumuliert Float-Samples und liefert sie via onRecordingComplete-Callback.
AppState hat isModelReady: Bool = false. AppStateTests enthalten testIsModelReadyInitiallyFalse (gruen).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/03-transcription/03-02-SUMMARY.md` erstellen.
</output>
