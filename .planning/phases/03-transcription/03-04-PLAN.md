---
phase: 03-transcription
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - SPRECHKRAFT/AppDelegate.swift
autonomous: true
requirements:
  - RECORD-04
  - RECORD-05

must_haves:
  truths:
    - "AppDelegate startet den Modell-Download beim App-Launch"
    - "NSStatusItem-Title zeigt '↓ XX%' waehrend des Downloads"
    - "Aufnahme ist waehrend Download blockiert (isModelReady-Check)"
    - "Nach stopRecording() wird appState auf .transcribing gesetzt und TranscriptionService aufgerufen"
    - "Nach Transkription wird print('Transkription: <text>') auf Konsole ausgegeben und State auf .idle zurueckgesetzt"
  artifacts:
    - path: "SPRECHKRAFT/AppDelegate.swift"
      provides: "Download-Kickoff, onRecordingComplete-Callback, isModelReady-Guard"
      contains: "private var transcriptionService = TranscriptionService()"
  key_links:
    - from: "SPRECHKRAFT/AppDelegate.swift"
      to: "SPRECHKRAFT/Transcription/TranscriptionService.swift"
      via: "transcriptionService.downloadAndLoad() in setupTranscription()"
      pattern: "transcriptionService.downloadAndLoad"
    - from: "SPRECHKRAFT/AppDelegate.swift"
      to: "SPRECHKRAFT/AppState.swift"
      via: "appState?.isModelReady = true nach Download"
      pattern: "appState.*isModelReady"
    - from: "SPRECHKRAFT/Audio/AudioController.swift"
      to: "SPRECHKRAFT/AppDelegate.swift"
      via: "onRecordingComplete callback → transcribeWithResampling"
      pattern: "onRecordingComplete"
---

<objective>
Verdrahtet TranscriptionService in AppDelegate: Download-Kickoff bei App-Start, Fortschrittsanzeige
im NSStatusItem-Title, Download-Block in startRecordingWithCue(), und onRecordingComplete-Callback
der die Transkription ausloest und den State zuruecksetzt.

Purpose: Ohne dieses Wiring laeuft TranscriptionService isoliert — keine Integration mit dem
bestehenden Recording-Flow. Dieser Plan schliesst Phase 3 ab und liefert den vollstaendigen
Pipeline-Stub (Audio → Transkript → Konsole).

Output: AppDelegate mit vollstaendiger Phase-3-Pipeline. print("Transkription: ...") sichtbar
nach Aufnahme.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transcription/03-RESEARCH.md
@.planning/phases/03-transcription/03-PATTERNS.md
@.planning/phases/03-transcription/03-02-SUMMARY.md
@.planning/phases/03-transcription/03-03-SUMMARY.md

<interfaces>
<!-- TranscriptionService API (aus 03-03) -->
```swift
actor TranscriptionService {
    private(set) var isModelReady: Bool

    func downloadAndLoad(
        progressHandler: @MainActor @escaping (Double) -> Void
    ) async

    func transcribeWithResampling(
        _ samples: [Float],
        sampleRate: Double
    ) async -> String?
}
```

<!-- AudioController onRecordingComplete (aus 03-02) -->
```swift
// AudioController.swift — neu in 03-02
var onRecordingComplete: (([Float], Double) -> Void)?
// Aufruf: Task { @MainActor in self?.onRecordingComplete?(capturedSamples, capturedSampleRate) }
```

<!-- AppState (aus 03-02) -->
```swift
// AppState.swift — neu in 03-02
var isModelReady: Bool = false
// Existierende Methoden:
func resetToIdle()     // .transcribing → .idle
func toggleRecording() // .idle → .recording ODER .recording → .transcribing
```

<!-- Bestehende AppDelegate.swift Struktur (aktuelle Datei) -->
```swift
@MainActor
final class AppDelegate: NSObject, NSApplicationDelegate {
    private lazy var statusItem: NSStatusItem = ...
    var appState: AppState?
    private var audioController: AudioController?

    func applicationDidFinishLaunching(_ notification: Notification) {
        // ... bestehender Code ...
        updateIcon()
        setupHotkey()
        // NEU: setupTranscription() hier einfuegen
    }

    func setupAudioController() {
        // ... bestehend ...
        audioController?.onAutoStop = { ... }
        audioController?.onLevelUpdate = { ... }
        // NEU: audioController?.onRecordingComplete = { ... } hier einfuegen
    }

    private func startRecordingWithCue() {
        guard appState?.recordingState == .idle else { return }
        // NEU: guard appState?.isModelReady == true else { return } hier einfuegen
        // ... Rest unveraendert ...
    }

    private func stopRecordingWithCue() {
        guard appState?.recordingState == .recording else { return }
        audioController?.stopRecording()
        appState?.toggleRecording()  // .recording → .transcribing
        NSSound(named: NSSound.Name("Pop"))?.play()
        updateIcon()
        // ALT (Platzhalter entfernen):
        // appState?.resetToIdle()   ← DIESE ZEILE LOESCHEN
        // updateIcon()               ← DIESE ZEILE LOESCHEN
        // NEU: kein resetToIdle() hier — kommt via onRecordingComplete-Callback
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppDelegate — TranscriptionService-Property + Download-Kickoff</name>
  <files>SPRECHKRAFT/AppDelegate.swift</files>

  <read_first>
    - SPRECHKRAFT/AppDelegate.swift — vollstaendige aktuelle Datei lesen; exakte Zeilennummern verstehen (applicationDidFinishLaunching, setupHotkey, updateIcon) bevor irgendwas editiert wird
    - .planning/phases/03-transcription/03-PATTERNS.md §AppDelegate.swift — Pattern 4: setupTranscription()-Methode, exakte Task-Closure-Struktur
    - .planning/phases/03-transcription/03-RESEARCH.md §Pattern 4: vollstaendige Code-Beispiele fuer AppDelegate-Integration
    - .planning/phases/03-transcription/03-03-SUMMARY.md — TranscriptionService-API bestätigen
  </read_first>

  <action>
Fuehre vier Aenderungen in AppDelegate.swift durch (Datei vorher vollstaendig lesen):

**Aenderung 1: TranscriptionService-Property** (nach `private var audioController: AudioController?`, aktuell ~Zeile 24):
```swift
/// TranscriptionService — Download und Transkription. Actor-isoliert.
private let transcriptionService = TranscriptionService()
```

**Aenderung 2: setupTranscription()-Aufruf in applicationDidFinishLaunching** (nach `setupHotkey()`, aktuell ~Zeile 39):
```swift
setupTranscription()
```

**Aenderung 3: Neue private Methode setupTranscription()** (neue MARK-Sektion nach dem MARK - Global Hotkey-Block):
```swift
// MARK: - Transcription Setup (RECORD-05)

/// Startet Modell-Download beim App-Start (D-09).
/// Fortschritt via NSStatusItem-Title "↓ XX%" (D-10).
/// isModelReady wird nach Abschluss auf true gesetzt (D-11).
private func setupTranscription() {
    Task {
        await transcriptionService.downloadAndLoad { [weak self] fraction in
            // @MainActor (Closure deklariert in downloadAndLoad als @MainActor)
            let pct = Int(fraction * 100)
            self?.statusItem.button?.title = pct < 100 ? "↓ \(pct)%" : ""
        }
        // Download abgeschlossen (oder fehlgeschlagen — isModelReady bleibt dann false, D-13)
        statusItem.button?.title = ""
        appState?.isModelReady = await transcriptionService.isModelReady
        updateIcon()
    }
}
```

HINWEIS zu `await transcriptionService.isModelReady`: Da `transcriptionService` ein `actor` ist,
muss der Zugriff auf actor-isolierte Properties via `await` erfolgen (Swift 6 strict concurrency).

**Aenderung 4: isModelReady-Guard in startRecordingWithCue()** (nach dem bestehenden
`guard appState?.recordingState == .idle else { return }`, aktuell ~Zeile 67):
```swift
// D-11: Aufnahme waehrend Download blockiert — kein Audio-Cue, kein State-Wechsel
guard appState?.isModelReady == true else { return }
```

NICHT aendern: handleClick, showMenu, updateIcon, toggleLoginItem, openSettingsMenu,
setupHotkey, stopRecordingWithCue (wird in Task 2 behandelt).
  </action>

  <verify>
    <automated>xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep -E "error:|Build succeeded"</automated>
  </verify>

  <acceptance_criteria>
    - AppDelegate.swift enthaelt `private let transcriptionService = TranscriptionService()`
    - AppDelegate.swift enthaelt `setupTranscription()` in applicationDidFinishLaunching
    - AppDelegate.swift enthaelt `func setupTranscription()` private Methode
    - AppDelegate.swift enthaelt `self?.statusItem.button?.title = pct < 100 ? "↓ \(pct)%" : ""`
    - AppDelegate.swift enthaelt `appState?.isModelReady = await transcriptionService.isModelReady`
    - AppDelegate.swift enthaelt zweiten `guard` fuer `isModelReady == true` in startRecordingWithCue()
    - `xcodebuild build -scheme SPRECHKRAFT` exitiert mit Code 0
  </acceptance_criteria>

  <done>
    AppDelegate initialisiert TranscriptionService, startet Download beim App-Start, zeigt Fortschritt
    im NSStatusItem-Title und blockiert Aufnahme waehrend Download laeuft.
  </done>
</task>

<task type="auto">
  <name>Task 2: AppDelegate — onRecordingComplete-Callback + stopRecordingWithCue-Platzhalter entfernen</name>
  <files>SPRECHKRAFT/AppDelegate.swift</files>

  <read_first>
    - SPRECHKRAFT/AppDelegate.swift — aktuelle Datei nach Task 1 lesen (Zustand nach Task 1 verstehen bevor Task 2 beginnt)
    - .planning/phases/03-transcription/03-PATTERNS.md §AppDelegate.swift — onRecordingComplete-Callback-Pattern und stopRecordingWithCue()-Aenderung
    - .planning/phases/03-transcription/03-RESEARCH.md §Pattern 4: onRecordingComplete-Closure mit Task { await transcribeWithResampling }
  </read_first>

  <action>
Fuehre zwei Aenderungen in AppDelegate.swift durch (nach Task 1):

**Aenderung 1: onRecordingComplete-Callback in setupAudioController()** (nach dem bestehenden
`audioController?.onLevelUpdate = { [weak self] in ... }` Block):
```swift
// Phase 3: Transkription nach Aufnahme-Ende (RECORD-04, D-05)
// Callback laeuft auf @MainActor (Task { @MainActor } in AudioController.stopRecording())
audioController?.onRecordingComplete = { [weak self] samples, sampleRate in
    guard let self else { return }
    Task {
        // Resampling (D-06) und Transkription (D-07) im actor — kein Main-Thread-Block
        let text = await self.transcriptionService.transcribeWithResampling(samples, sampleRate: sampleRate)
        await MainActor.run {
            if let text {
                print("Transkription: \(text)")  // D-07: Pipeline-Stub, Phase 4 ersetzt dies
            }
            self.appState?.resetToIdle()  // D-08: .transcribing → .idle
            self.updateIcon()
        }
    }
}
```

**Aenderung 2: Platzhalter in stopRecordingWithCue() entfernen**

Aktuell in stopRecordingWithCue() (nach `updateIcon()`, ~Zeilen 90-93):
```swift
        // Phase 3 wird hier Transkription starten. Bis dahin: sofort idle.
        appState?.resetToIdle()
        updateIcon()
```

Diese drei Zeilen entfernen. Der resetToIdle() Aufruf wandert in den onRecordingComplete-Callback
(Aenderung 1). stopRecordingWithCue() endet nach `updateIcon()` — der State bleibt auf
`.transcribing` bis der Transcription-Callback fertig ist.

Nach der Aenderung sieht stopRecordingWithCue() so aus:
```swift
private func stopRecordingWithCue() {
    guard appState?.recordingState == .recording else { return }
    audioController?.stopRecording()           // loest onRecordingComplete aus
    appState?.toggleRecording()                // .recording → .transcribing
    NSSound(named: NSSound.Name("Pop"))?.play()
    updateIcon()
    // resetToIdle() kommt via onRecordingComplete-Callback (nicht mehr hier)
}
```

NICHT aendern: startRecordingWithCue, handleClick, showMenu, updateIcon, setupTranscription.
  </action>

  <verify>
    <automated>xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep -E "error:|Build succeeded"</automated>
  </verify>

  <acceptance_criteria>
    - AppDelegate.swift enthaelt `audioController?.onRecordingComplete = { [weak self] samples, sampleRate in`
    - AppDelegate.swift enthaelt `await self.transcriptionService.transcribeWithResampling(samples, sampleRate: sampleRate)`
    - AppDelegate.swift enthaelt `print("Transkription: \(text)")`
    - AppDelegate.swift enthaelt `self.appState?.resetToIdle()` im onRecordingComplete-Callback (NICHT in stopRecordingWithCue())
    - AppDelegate.swift enthaelt KEINEN `appState?.resetToIdle()` Aufruf mehr in stopRecordingWithCue()
    - AppDelegate.swift enthaelt KEINEN Kommentar "Phase 3 wird hier Transkription starten" mehr
    - `xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS'` exitiert mit Code 0
    - `xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS'` exitiert mit Code 0 (alle bestehenden Tests gruen)
  </acceptance_criteria>

  <done>
    Vollstaendige Phase-3-Pipeline aktiv: Aufnahme → onRecordingComplete → transcribeWithResampling
    → print("Transkription: ...") → resetToIdle(). Platzhalter entfernt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @MainActor AppDelegate → actor TranscriptionService | Cross-isolation-boundary: await erforderlich |
| Audio-Render-Thread → @MainActor onRecordingComplete | Task { @MainActor } in AudioController ueberbrueckt Grenze |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Denial of Service | startRecordingWithCue() waehrend Download | mitigate | guard appState?.isModelReady == true (D-11) — Hotkey und Klick werden ignoriert bis Download fertig |
| T-03-10 | Repudiation | print("Transkription: ...") Konsolen-Output | accept | Phase 3 Stub; kein persistenter Audit-Trail noetig. Phase 6 (History) fuegt SQLite-Persistenz hinzu |
| T-03-11 | Denial of Service | Doppel-Hotkey waehrend .transcribing | accept | startRecordingWithCue() prueft guard recordingState == .idle — .transcribing ist nicht .idle, zweiter Start wird geblockt (RESEARCH.md Pitfall 3) |
</threat_model>

<verification>
- `xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS'` exitiert mit Code 0
- `xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS'` exitiert mit Code 0
- AppDelegate.swift: grep "transcriptionService" gibt mindestens 3 Treffer (Property, setupTranscription, onRecordingComplete)
- AppDelegate.swift: grep "isModelReady" gibt mindestens 2 Treffer (Guard + Setzen)
- AppDelegate.swift: grep "resetToIdle" gibt genau 1 Treffer (im onRecordingComplete-Callback, NICHT in stopRecordingWithCue)
- AppDelegate.swift: grep "Transkription:" gibt 1 Treffer (print-Statement)
</verification>

<success_criteria>
Vollstaendige Phase-3-Pipeline: App-Start loest Modell-Download aus, NSStatusItem-Title zeigt
Fortschritt, Aufnahme ist waehrend Download blockiert, nach Aufnahme-Ende wird TranskriptionService
aufgerufen und Ergebnis auf Konsole gedruckt, State wechselt zu .idle.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/03-transcription/03-04-SUMMARY.md` erstellen.
</output>
