---
phase: 03-transcription
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - SPRECHKRAFTTests/TranscriptionServiceTests.swift
autonomous: true
requirements:
  - RECORD-04
  - RECORD-05

must_haves:
  truths:
    - "Xcode-Build mit WhisperKit-Dependency schlaegt nicht fehl"
    - "TranscriptionServiceTests enthaelt failing RED-Tests fuer RECORD-04 und RECORD-05"
    - "Alle bestehenden Tests laufen weiterhin gruen"
  artifacts:
    - path: "SPRECHKRAFTTests/TranscriptionServiceTests.swift"
      provides: "RED-Test-Stubs fuer Resampling, MinSample-Guard und isModelReady"
      exports:
        - testResamplingProducesCorrectLength
        - testMinimumSampleGuardReturnsNil
        - testTranscribeReturnsNilWhenNotReady
        - testInitialStateNotReady
  key_links:
    - from: "SPRECHKRAFTTests/TranscriptionServiceTests.swift"
      to: "SPRECHKRAFT/Transcription/TranscriptionService.swift"
      via: "@testable import SPRECHKRAFT"
      pattern: "@testable import SPRECHKRAFT"
---

<objective>
Baut das Build-Fundament fuer Phase 3: WhisperKit als SPM-Dependency im Xcode-Projekt eintragen
und die Failing-Test-Stubs (RED-Phase) fuer TranscriptionService erstellen. Kein Produktionscode
in diesem Plan — nur das Geruest, das nachfolgende Pläne implementieren koennen.

Purpose: Ohne WhisperKit im Build schlaegt Wave 1 sofort fehl. Test-Stubs geben den nachfolgenden
Implementierungsplaenen eine klare Zielscheibe (Nyquist-Konformitaet).

Output: WhisperKit im xcodeproj, TranscriptionServiceTests.swift mit 4 RED-Tests.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transcription/03-RESEARCH.md
@.planning/phases/03-transcription/03-VALIDATION.md

<interfaces>
<!-- Bestehende Test-Patterns aus AudioControllerTests.swift -->

Aus SPRECHKRAFTTests/AudioControllerTests.swift (Lines 9-16):
```swift
import Testing
import AVFoundation
import Defaults
@testable import SPRECHKRAFT

@Suite("AudioController (RECORD-01, RECORD-02)")
struct AudioControllerTests {
```

Async-Test-Pattern (Lines 35-37):
```swift
@Test("calculateRMS gibt ~0.0 fuer stille Buffer (alle Samples 0.0)")
func testRMSCalculation_silentBuffer() async throws {
    let appState = await MainActor.run { AppState() }
```

<!-- TranscriptionService wird in Wave 1 erstellt — Tests referenzieren Typen die noch nicht existieren -->
<!-- Das ist gewollt: RED-Tests kompilieren NICHT bis Wave 1 fertig ist -->
<!-- xcodebuild build -scheme SPRECHKRAFT muss dagegen gruen bleiben -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WhisperKit SPM-Dependency im Xcode-Projekt eintragen</name>
  <files>SPRECHKRAFT.xcodeproj/project.pbxproj</files>

  <read_first>
    - SPRECHKRAFT.xcodeproj/project.pbxproj — vollstaendige aktuelle Struktur lesen um bestehende XCRemoteSwiftPackageReference-Eintraege zu verstehen und das Pattern korrekt zu replizieren. NICHT mit Annahmen arbeiten.
    - CLAUDE.md §Technology Stack §Supporting Libraries — bestehende Pakete (KeyboardShortcuts, LaunchAtLogin, Defaults) als strukturelle Vorlage
  </read_first>

  <action>
Trage WhisperKit als XCRemoteSwiftPackageReference im pbxproj ein. Das Projekt verwaltet SPM-Pakete
direkt im pbxproj (kein separates Package.swift), analog zu den bestehenden Paketen.

Exakte Werte:
- SPM URL: `https://github.com/argmaxinc/argmax-oss-swift`
- Version requirement: upToNextMajorVersion from "0.18.0"
- Product name: "WhisperKit"
- Target: SPRECHKRAFT (Haupttarget, NICHT SPRECHKRAFTTests)

Schritt 1: pbxproj lesen und bestehende XCRemoteSwiftPackageReference-Struktur verstehen
(mindestens einen bestehenden Eintrag als Vorlage: z.B. KeyboardShortcuts oder Defaults).

Schritt 2: Drei neue Sektionen im pbxproj einfuegen (exaktes Pattern wie bestehende Pakete):
  a) XCRemoteSwiftPackageReference-Eintrag fuer argmax-oss-swift
  b) XCSwiftPackageProductDependency-Eintrag fuer WhisperKit Product
  c) packageProductDependencies-Referenz im SPRECHKRAFT-Target

Verwende neue UUID-Strings (8-4-4-4-12 Hex, Grossbuchstaben), die sich von bestehenden
UUIDs im pbxproj unterscheiden. Generiere sie deterministisch (z.B. beginnend mit DEAD, BEEF,
CAFE etc. um Eindeutigkeit zu gewaehrleisten).

WICHTIG (aus RESEARCH.md): Die URL ist `argmaxinc/argmax-oss-swift`, NICHT `argmaxinc/WhisperKit`.
Das alte Repo wurde umgezogen. Falsche URL fuehrt zu SPM-Resolve-Fehler.
  </action>

  <verify>
    <automated>xcodebuild -resolvePackageDependencies -scheme SPRECHKRAFT 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `xcodebuild -resolvePackageDependencies -scheme SPRECHKRAFT` exitiert mit Code 0
    - pbxproj enthaelt String `argmaxinc/argmax-oss-swift`
    - pbxproj enthaelt String `WhisperKit` im packageProductDependencies-Block des SPRECHKRAFT-Targets
    - `xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS'` exitiert mit Code 0 (WhisperKit importierbar)
  </acceptance_criteria>

  <done>WhisperKit ist im Xcode-Build aufgeloest und das Haupttarget kompiliert ohne Fehler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TranscriptionServiceTests.swift — RED-Test-Stubs</name>
  <files>SPRECHKRAFTTests/TranscriptionServiceTests.swift</files>

  <read_first>
    - SPRECHKRAFTTests/AudioControllerTests.swift — vollstaendige Datei lesen als Struktur-Vorlage (Import-Pattern, @Suite, async throws, #expect)
    - SPRECHKRAFTTests/AppStateTests.swift — @MainActor-Test-Pattern als Referenz
    - .planning/phases/03-transcription/03-VALIDATION.md — Validation Architecture §Phase Requirements → Test Map: exakte Testnamen und erwartetes Verhalten
    - .planning/phases/03-transcription/03-RESEARCH.md — §Code Examples §Minimum-Sample-Guard: 1600 Samples-Schwellwert
  </read_first>

  <behavior>
    - testResamplingProducesCorrectLength: resampleTo16kHz([Float](repeating:0.5, count:48000), fromSampleRate:48000.0) soll Array mit Laenge ~16000 zurueckgeben (Toleranz ±160, d.h. 1%)
    - testMinimumSampleGuardReturnsNil: TranscriptionService.transcribe() mit Array < 1600 Samples soll nil zurueckgeben (ohne Modell-Laden)
    - testTranscribeReturnsNilWhenNotReady: TranscriptionService.transcribe() wenn isModelReady == false soll nil zurueckgeben
    - testInitialStateNotReady: Frisch initialisierter TranscriptionService hat isModelReady == false
  </behavior>

  <action>
Erstelle `SPRECHKRAFTTests/TranscriptionServiceTests.swift` mit 4 RED-Test-Stubs.

Diese Tests kompilieren ERST wenn Wave 1 (TranscriptionService.swift) erstellt wurde.
Das ist die erwuenschte RED-Phase — kein Workaround, kein Dummy-Typ erstellen.

Dateistruktur (analog AudioControllerTests.swift):
```swift
// SPRECHKRAFTTests/TranscriptionServiceTests.swift
// Zweck: Unit-Tests fuer TranscriptionService — Resampling, MinSample-Guard, Model-Ready-State.
// RECORD-04: Resampling korrekte Laenge, Guard gegen zu kurze Aufnahmen
// RECORD-05: isModelReady-Initialzustand

import Testing
import AVFoundation
@testable import SPRECHKRAFT

@Suite("TranscriptionService (RECORD-04, RECORD-05)")
struct TranscriptionServiceTests {

    // MARK: - Hilfsmethoden

    private func makeSamples(count: Int, value: Float = 0.5) -> [Float] {
        Array(repeating: value, count: count)
    }

    // MARK: - RECORD-05: Model-Ready-State

    @Test("isModelReady ist false nach Initialisierung (RECORD-05)")
    func testInitialStateNotReady() async {
        let service = TranscriptionService()
        let ready = await service.isModelReady
        #expect(ready == false, "isModelReady muss false sein bevor downloadAndLoad() aufgerufen wurde")
    }

    // MARK: - RECORD-04: Resampling

    @Test("resampleTo16kHz: 48kHz Input liefert korrekte Ausgangslaenge (RECORD-04)")
    func testResamplingProducesCorrectLength() async {
        let service = TranscriptionService()
        let input = makeSamples(count: 48000)  // 1s @ 48kHz
        let output = await service.resampleTo16kHz(input, fromSampleRate: 48000.0)
        // Erwartete Laenge: 16000 ± 1% (160 Samples Toleranz fuer Converter-Rundung)
        #expect(abs(output.count - 16000) <= 160,
                "Resampling 48kHz->16kHz: erwartet ~16000, bekommen \(output.count)")
    }

    @Test("resampleTo16kHz: Identitaet wenn sampleRate bereits 16kHz (RECORD-04)")
    func testResamplingIdentityAt16kHz() async {
        let service = TranscriptionService()
        let input = makeSamples(count: 16000)
        let output = await service.resampleTo16kHz(input, fromSampleRate: 16000.0)
        #expect(output.count == 16000, "Kein Resampling noetig bei 16kHz — Laenge unveraendert")
    }

    // MARK: - RECORD-04: Minimum-Sample-Guard

    @Test("transcribe gibt nil fuer Audio < 1600 Samples zurueck (RECORD-04)")
    func testMinimumSampleGuardReturnsNil() async {
        let service = TranscriptionService()
        let shortAudio = makeSamples(count: 800)  // 0.05s @ 16kHz — zu kurz
        let result = await service.transcribe(shortAudio)
        #expect(result == nil, "transcribe() muss nil zurueckgeben fuer Arrays < 1600 Samples")
    }

    @Test("transcribe gibt nil zurueck wenn Modell nicht geladen (RECORD-04)")
    func testTranscribeReturnsNilWhenNotReady() async {
        let service = TranscriptionService()
        let audio = makeSamples(count: 16000)  // Laenge OK, aber Modell nicht geladen
        let result = await service.transcribe(audio)
        #expect(result == nil, "transcribe() muss nil zurueckgeben wenn isModelReady == false")
    }
}
```

NICHT hinzufuegen:
- Kein `import WhisperKit` in der Test-Datei (nicht noetig — @testable import SPRECHKRAFT reicht)
- Keinen Dummy-TranscriptionService-Typ (Wave 1 liefert den echten)
- Keine XCTest-Imports (Projekt nutzt Swift Testing / import Testing)
  </action>

  <verify>
    <automated>xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' -only-testing SPRECHKRAFTTests/AudioControllerTests 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - SPRECHKRAFTTests/TranscriptionServiceTests.swift existiert
    - Datei enthaelt `@Suite("TranscriptionService (RECORD-04, RECORD-05)")`
    - Datei enthaelt alle 5 Test-Methoden: testInitialStateNotReady, testResamplingProducesCorrectLength, testResamplingIdentityAt16kHz, testMinimumSampleGuardReturnsNil, testTranscribeReturnsNilWhenNotReady
    - Bestehende Tests (AudioControllerTests, AppStateTests) laufen weiterhin gruen: `xcodebuild test -only-testing SPRECHKRAFTTests/AudioControllerTests` exitiert mit Code 0
    - Datei enthaelt KEIN `import XCTest` (Swift Testing wird verwendet)
  </acceptance_criteria>

  <done>
    5 RED-Test-Stubs existieren in TranscriptionServiceTests.swift. Bestehende Tests sind gruen.
    TranscriptionServiceTests kompilieren noch NICHT (TranscriptionService fehlt) — das ist korrekt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pbxproj → SPM-Resolver | URL-String in pbxproj bestimmt welches Paket geladen wird |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | pbxproj SPM URL | mitigate | URL explizit auf `argmaxinc/argmax-oss-swift` gesetzt (verifiziert in RESEARCH.md); xcodebuild -resolvePackageDependencies prueft Integritaet |
| T-03-02 | Information Disclosure | WhisperKit-Cache in ~/Library/Caches | accept | Standard macOS-Caches-Verzeichnis; keine PII; App ist nicht sandboxed per Architekturentscheidung (CONTEXT.md) |
</threat_model>

<verification>
- `xcodebuild -resolvePackageDependencies -scheme SPRECHKRAFT` exitiert mit Code 0
- `xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS'` exitiert mit Code 0
- SPRECHKRAFTTests/TranscriptionServiceTests.swift enthaelt 5 Test-Methoden mit `#expect`
- `xcodebuild test -only-testing SPRECHKRAFTTests/AudioControllerTests` exitiert mit Code 0
</verification>

<success_criteria>
WhisperKit ist im Xcode-Build verfuegbar und importierbar. Test-Stubs definieren die Zielscheibe
fuer Wave 1. Bestehende Tests sind unveraendert gruen.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/03-transcription/03-01-SUMMARY.md` erstellen.
</output>
