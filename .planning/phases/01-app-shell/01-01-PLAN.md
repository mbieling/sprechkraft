---
phase: 01-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SPRECHKRAFT.xcodeproj/project.pbxproj
  - SPRECHKRAFT/Info.plist
  - SPRECHKRAFT/SPRECHKRAFT.entitlements
  - Package.swift
  - SPRECHKRAFTTests/RecordingStateTests.swift
  - SPRECHKRAFTTests/AppStateTests.swift
  - SPRECHKRAFTTests/HotkeyTests.swift
  - .gitignore
  - .planning/phases/01-app-shell/01-VALIDATION.md
autonomous: false
requirements:
  - SET-06
user_setup:
  - service: xcode
    why: "Xcode.app wird zwingend benötigt, um eine signierbare .app-Bundle zu bauen. Command Line Tools allein erzeugen nur ein unsigniertes CLI-Binary ohne Info.plist/Entitlements."
    dashboard_config:
      - task: "Xcode 26 aus dem Mac App Store installieren"
        location: "Mac App Store → Suche 'Xcode'"
      - task: "Nach Installation: xcodebuild -license accept"
        location: "Terminal (einmalig)"

must_haves:
  truths:
    - "Xcode ist installiert und `xcodebuild -version` liefert eine Version ≥ 26.0"
    - "Ein Xcode-Projekt 'SPRECHKRAFT.xcodeproj' existiert mit App-Target 'SPRECHKRAFT' und Test-Target 'SPRECHKRAFTTests'"
    - "Info.plist enthält LSUIElement=YES (verhindert Dock-Icon — SET-06)"
    - "Package.swift listet KeyboardShortcuts, LaunchAtLogin-modern und Defaults als Dependencies"
    - "Test-Scaffolds existieren für RecordingState, AppState und Hotkey — Tests schlagen absichtlich fehl (RED-Phase), da Implementierung in Plan 02/03 folgt"
    - "Build Setting GENERATE_INFOPLIST_FILE = NO, damit manuelle Info.plist nicht überschrieben wird"
    - "01-VALIDATION.md Frontmatter ist nach Task 5 auf nyquist_compliant: true und wave_0_complete: true gesetzt (Nyquist-Sign-Off)"
  artifacts:
    - path: "SPRECHKRAFT.xcodeproj/project.pbxproj"
      provides: "Xcode-Projektstruktur mit App- und Test-Target"
      contains: "SPRECHKRAFT"
    - path: "SPRECHKRAFT/Info.plist"
      provides: "LSUIElement=YES für Menu-Bar-Only-App"
      contains: "LSUIElement"
    - path: "SPRECHKRAFT/SPRECHKRAFT.entitlements"
      provides: "Entitlements-Datei OHNE com.apple.security.app-sandbox"
      contains: "plist"
    - path: "Package.swift"
      provides: "SPM-Dependencies KeyboardShortcuts, LaunchAtLogin-modern, Defaults"
      contains: "KeyboardShortcuts"
    - path: "SPRECHKRAFTTests/RecordingStateTests.swift"
      provides: "Test-Scaffold für RecordingState (FEED-01)"
      min_lines: 20
    - path: "SPRECHKRAFTTests/AppStateTests.swift"
      provides: "Test-Scaffold für AppState.toggleRecording Zustandsmaschine"
      min_lines: 20
    - path: "SPRECHKRAFTTests/HotkeyTests.swift"
      provides: "Test-Scaffold für KeyboardShortcuts-Integration (SET-02)"
      min_lines: 15
  key_links:
    - from: "SPRECHKRAFT.xcodeproj"
      to: "SPRECHKRAFT/Info.plist"
      via: "INFOPLIST_FILE Build Setting"
      pattern: "INFOPLIST_FILE"
    - from: "SPRECHKRAFT.xcodeproj"
      to: "Package.swift"
      via: "SPM Package Dependency auf lokales Package"
      pattern: "KeyboardShortcuts"
    - from: "SPRECHKRAFTTests/RecordingStateTests.swift"
      to: "SPRECHKRAFT.xcodeproj/project.pbxproj"
      via: "PBXFileReference + PBXBuildFile am SPRECHKRAFTTests-Target"
      pattern: "RecordingStateTests.swift"
---

<objective>
Legt das Xcode-Projekt und die SPM-Dependencies als Fundament für alle weiteren Phasen an. Stellt sicher, dass Xcode installiert ist (andernfalls blockiert), legt Info.plist mit LSUIElement=YES an (erfüllt SET-06), und erzeugt leere Test-Scaffolds für die Wave-0-Gaps aus VALIDATION.md. Beinhaltet keinen App-Code — nur Projektstruktur, Abhängigkeiten und Test-Skelette.

Purpose: Ohne Xcode und korrekte Info.plist kann die App weder als Menu-Bar-Only-App gestartet werden, noch ist ein Code-Signing-Ablauf möglich. Tests müssen existieren (auch wenn sie zunächst fehlschlagen), damit spätere Plans gegen sie verifizieren können.
Output: Build-fähiges Xcode-Projekt mit leerer App + Tests-Target + SPM-Abhängigkeiten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-app-shell/01-CONTEXT.md
@.planning/phases/01-app-shell/01-RESEARCH.md
@.planning/phases/01-app-shell/01-PATTERNS.md
@.planning/phases/01-app-shell/01-UI-SPEC.md
@.planning/phases/01-app-shell/01-VALIDATION.md

<interfaces>
<!-- Downstream-Contracts, die Plans 02 und 03 gegen diese Test-Skelette erfüllen MÜSSEN -->

Test-Erwartungen (werden in Plan 02/03 grün):

From SPRECHKRAFTTests/RecordingStateTests.swift:
```swift
// Erwartet (Plan 02):
// - RecordingState hat genau 4 Fälle: .idle, .recording, .transcribing, .llmProcessing
// - .idle.color entspricht grau (#8E8E93: 0.557/0.557/0.576)
// - .recording.color == Color(.systemRed)
// - .transcribing.color == Color(.systemBlue)
// - .llmProcessing.color == Color(.systemPurple)
// - .recording.isPulsing == true, .llmProcessing.isPulsing == true
// - .idle.isPulsing == false, .transcribing.isPulsing == false
// - .recording.pulseSpeed == 0.8, .llmProcessing.pulseSpeed == 1.2
// - accessibilityLabel nicht leer für alle 4 Zustände
```

From SPRECHKRAFTTests/AppStateTests.swift:
```swift
// Erwartet (Plan 02):
// - AppState startet mit recordingState == .idle
// - toggleRecording() cycelt: .idle → .recording → .transcribing → .llmProcessing → .idle
```

From SPRECHKRAFTTests/HotkeyTests.swift:
```swift
// Erwartet (Plan 03):
// - KeyboardShortcuts.Name.toggleRecording hat initial: ⌥⌘R
// - KeyboardShortcuts.Name.toggleRecording.rawValue == "toggleRecording"
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Xcode-Verfügbarkeit prüfen (blockiert bei Fehlen)</name>
  <files>(keine Dateiänderungen — Checkpoint)</files>
  <read_first>
    - .planning/phases/01-app-shell/01-RESEARCH.md (Abschnitt "Environment Availability" — Xcode.app fehlt laut Research)
    - .planning/phases/01-app-shell/01-RESEARCH.md (Open Questions §1 — Xcode nicht installiert, blockiert)
  </read_first>
  <action>Checkpoint — User muss bestätigen, dass Xcode installiert ist oder installieren. Verifikationsbefehl: `xcodebuild -version` liefert eine Version. Bei Option "abort" endet der Plan hier.</action>
  <verify>xcodebuild -version 2>&amp;1 | head -2</verify>
  <decision>
    Xcode.app muss installiert sein, um die Phase 1-App zu bauen. Research hat bestätigt, dass nur Command Line Tools vorhanden sind. Der Entwickler muss Xcode 26 aus dem Mac App Store installieren (einmalig, ca. 10-15 GB Download, 30-60 Minuten) bevor fortgefahren werden kann.
  </decision>
  <context>
    Ohne Xcode:
    - kann kein .app-Bundle erstellt werden (swift build liefert nur CLI-Executable)
    - keine Info.plist-Einbettung
    - keine Entitlements
    - keine Code-Signierung
    - Menu-Bar-App (LSUIElement=YES) funktioniert nicht als CLI
  </context>
  <options>
    <option id="installed">
      <name>Xcode ist bereits installiert</name>
      <pros>Sofort weiter mit Task 2</pros>
      <cons>Muss zur Validierung `xcodebuild -version` liefern (mind. Xcode 26.x erwartet laut Research)</cons>
    </option>
    <option id="will-install">
      <name>Ich installiere Xcode jetzt und melde mich zurück</name>
      <pros>Einzig valider Weg, falls Xcode fehlt</pros>
      <cons>Installation dauert — die Phase pausiert</cons>
    </option>
    <option id="abort">
      <name>Phase 1 abbrechen — ich kann Xcode nicht installieren</name>
      <pros>Ehrliches Stoppen</pros>
      <cons>Keine weitere Entwicklung möglich; alle nachfolgenden Phasen blockiert</cons>
    </option>
  </options>
  <resume-signal>Type "installed" und paste `xcodebuild -version` Output, ODER "will-install" nach Abschluss, ODER "abort"</resume-signal>
  <acceptance_criteria>
    - User-Antwort liegt vor und entspricht einer der drei Optionen
    - Bei "installed" / "will-install": `xcodebuild -version` liefert Version >= 16.0 (oder Xcode 26.x falls verfügbar)
    - Bei "abort": Dieser Plan endet hier, kein weiterer Task läuft
  </acceptance_criteria>
  <done>User-Entscheidung dokumentiert; bei Fortfahren ist Xcode verfügbar.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Xcode-Projekt anlegen mit App- und Test-Target</name>
  <files>
    SPRECHKRAFT.xcodeproj/project.pbxproj,
    SPRECHKRAFT/SPRECHKRAFT.entitlements,
    SPRECHKRAFT/Info.plist,
    .gitignore
  </files>
  <read_first>
    - .planning/phases/01-app-shell/01-RESEARCH.md (Abschnitt "Empfohlene Projektstruktur" — Dateilayout)
    - .planning/phases/01-app-shell/01-RESEARCH.md (Pitfall 4 — LSUIElement und GENERATE_INFOPLIST_FILE)
    - .planning/phases/01-app-shell/01-RESEARCH.md (Security Domain — kein App Sandbox)
    - .planning/phases/01-app-shell/01-PATTERNS.md (SPRECHKRAFT/Info.plist — kritische Einträge)
    - CLAUDE.md (GSD Workflow Enforcement, Technology Stack)
  </read_first>
  <behavior>
    - SPRECHKRAFT.xcodeproj existiert und lässt sich mit `xcodebuild -list` öffnen
    - Target "SPRECHKRAFT" (macOS app), Target "SPRECHKRAFTTests" (unit test bundle)
    - Deployment Target: macOS 14.0
    - Swift Language Version: Swift 6 (SWIFT_VERSION = 6.0)
    - Build Setting GENERATE_INFOPLIST_FILE = NO
    - Build Setting INFOPLIST_FILE = SPRECHKRAFT/Info.plist
    - Build Setting CODE_SIGN_ENTITLEMENTS = SPRECHKRAFT/SPRECHKRAFT.entitlements
    - Info.plist enthält LSUIElement=true
    - Entitlements-Datei enthält KEIN com.apple.security.app-sandbox
  </behavior>
  <action>
    Erstelle ein macOS Xcode-Projekt für SPRECHKRAFT. Zwei Wege sind akzeptabel:

    (A) Xcode GUI: File → New → Project → macOS → App, Product Name "SPRECHKRAFT", Interface: SwiftUI, Language: Swift, Team: None (kein Signieren in Phase 1), Include Tests: ja. Dann Build Settings manuell anpassen.

    (B) Programmatisch via xcodegen oder direktes pbxproj-Template. Falls xcodegen nicht installiert, nutze Weg (A) + direkte pbxproj-Bearbeitung mit dem `ruby xcodeproj` Gem oder Hand-Edit.

    Setze explizit folgende Build Settings am App-Target "SPRECHKRAFT":
    - PRODUCT_NAME = SPRECHKRAFT
    - PRODUCT_BUNDLE_IDENTIFIER = com.sprechkraft.app
    - MACOSX_DEPLOYMENT_TARGET = 14.0
    - SWIFT_VERSION = 6.0
    - SWIFT_STRICT_CONCURRENCY = complete
    - GENERATE_INFOPLIST_FILE = NO
    - INFOPLIST_FILE = SPRECHKRAFT/Info.plist
    - CODE_SIGN_ENTITLEMENTS = SPRECHKRAFT/SPRECHKRAFT.entitlements
    - CODE_SIGN_STYLE = Automatic
    - DEVELOPMENT_TEAM = "" (leer, Phase 1 lokal)
    - CODE_SIGN_IDENTITY = "-" (Ad-hoc signing)
    - LD_RUNPATH_SEARCH_PATHS = "$(inherited) @executable_path/../Frameworks"

    Erstelle SPRECHKRAFT/Info.plist (XML plist) mit MINIMUM diesen Keys:
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
        <key>CFBundleDevelopmentRegion</key>
        <string>de</string>
        <key>CFBundleExecutable</key>
        <string>$(EXECUTABLE_NAME)</string>
        <key>CFBundleIdentifier</key>
        <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
        <key>CFBundleInfoDictionaryVersion</key>
        <string>6.0</string>
        <key>CFBundleName</key>
        <string>$(PRODUCT_NAME)</string>
        <key>CFBundlePackageType</key>
        <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
        <key>CFBundleShortVersionString</key>
        <string>1.0</string>
        <key>CFBundleVersion</key>
        <string>1</string>
        <key>LSMinimumSystemVersion</key>
        <string>14.0</string>
        <key>LSUIElement</key>
        <true/>
        <key>NSHumanReadableCopyright</key>
        <string>Copyright © 2026 SPRECHKRAFT. All rights reserved.</string>
    </dict>
    </plist>
    ```

    Erstelle SPRECHKRAFT/SPRECHKRAFT.entitlements (leeres plist-Dict — KEIN Sandbox, wie in STATE.md initial entschieden):
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
    </dict>
    </plist>
    ```

    Erstelle .gitignore im Repository-Root mit MINIMUM diesen Einträgen:
    ```
    # Xcode
    build/
    DerivedData/
    *.xcuserstate
    xcuserdata/
    *.xcscmblueprint
    *.xccheckout
    .swiftpm/
    Package.resolved

    # macOS
    .DS_Store

    # SPM
    .build/
    ```

    Erstelle per Default-Xcode-Template auch die beiden Start-Swift-Dateien (leeres `SPRECHKRAFT/SPRECHKRAFTApp.swift` mit minimalem `@main struct SPRECHKRAFTApp: App { var body: some Scene { Settings { EmptyView() } } }` damit das Projekt builded — wird in Plan 03 komplett überschrieben). Dies ist KEIN Pattern-Code, nur ein Placeholder damit Task 5 `xcodebuild` grün wird.
  </action>
  <verify>
    <automated>xcodebuild -list -project SPRECHKRAFT.xcodeproj 2>&amp;1 | grep -E "SPRECHKRAFT|SPRECHKRAFTTests"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild -list -project SPRECHKRAFT.xcodeproj` listet Target "SPRECHKRAFT" UND "SPRECHKRAFTTests"
    - `plutil -lint SPRECHKRAFT/Info.plist` exit code 0
    - `plutil -extract LSUIElement raw SPRECHKRAFT/Info.plist` liefert "true"
    - `grep -q "com.apple.security.app-sandbox" SPRECHKRAFT/SPRECHKRAFT.entitlements` exit code 1 (NICHT vorhanden — bewusste Entscheidung)
    - `grep -q "GENERATE_INFOPLIST_FILE = NO" SPRECHKRAFT.xcodeproj/project.pbxproj` exit code 0
    - `grep -q "SWIFT_VERSION = 6.0" SPRECHKRAFT.xcodeproj/project.pbxproj` exit code 0
    - `grep -q "MACOSX_DEPLOYMENT_TARGET = 14.0" SPRECHKRAFT.xcodeproj/project.pbxproj` exit code 0
    - `.gitignore` enthält die Zeile `DerivedData/`
  </acceptance_criteria>
  <done>Xcode-Projekt existiert mit korrekter Info.plist (LSUIElement=YES), keiner Sandbox, Swift 6 und macOS 14 als Mindestziel.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Swift Package Manager Dependencies anbinden</name>
  <files>
    Package.swift,
    SPRECHKRAFT.xcodeproj/project.pbxproj
  </files>
  <read_first>
    - .planning/phases/01-app-shell/01-RESEARCH.md (Abschnitt "Standard Stack" — Library-Versionen verifiziert)
    - .planning/phases/01-app-shell/01-RESEARCH.md (Abschnitt "Installation (SPM)" — fertige Package.swift-Konfig)
    - CLAUDE.md (Supporting Libraries Tabelle)
    - Package.swift (aktueller Stand, falls existent)
    - SPRECHKRAFT.xcodeproj/project.pbxproj (zum Hinzufügen von Package Product Dependencies)
  </read_first>
  <behavior>
    - Drei SPM-Pakete sind an das App-Target gebunden:
      * sindresorhus/KeyboardShortcuts (from: 2.0.0)
      * sindresorhus/LaunchAtLogin-modern (from: 1.0.0)
      * sindresorhus/Defaults (from: 8.0.0)
    - Pakete werden als "remote Swift Package References" im Xcode-Projekt geführt, nicht via lokalem Package.swift allein
    - `xcodebuild -resolvePackageDependencies -project SPRECHKRAFT.xcodeproj -scheme SPRECHKRAFT` löst alle Abhängigkeiten auf
  </behavior>
  <action>
    Binde drei SPM-Dependencies an das App-Target "SPRECHKRAFT" im Xcode-Projekt:

    1. KeyboardShortcuts — URL: https://github.com/sindresorhus/KeyboardShortcuts, Rule: Up to next major from "2.0.0", Product: "KeyboardShortcuts"
    2. LaunchAtLogin-modern — URL: https://github.com/sindresorhus/LaunchAtLogin-modern, Rule: Up to next major from "1.0.0", Product: "LaunchAtLogin"
    3. Defaults — URL: https://github.com/sindresorhus/Defaults, Rule: Up to next major from "8.0.0", Product: "Defaults"

    Vorgehen (Xcode GUI):
    Project Navigator → Projekt auswählen → Package Dependencies Tab → "+" Button → URL eingeben → "Add Package" → Product am Target "SPRECHKRAFT" auswählen.

    Vorgehen (pbxproj direkt): Füge `XCRemoteSwiftPackageReference` und `XCSwiftPackageProductDependency` Objekte mit obigen URLs und Produktnamen ein.

    Erstelle ZUSÄTZLICH eine Package.swift im Repository-Root für SPM-CLI-Builds (wird nicht von Xcode genutzt, ermöglicht aber `swift build` für Kommandozeilen-Tests). Inhalt:

    ```swift
    // swift-tools-version: 6.0
    import PackageDescription

    let package = Package(
        name: "SPRECHKRAFT",
        platforms: [.macOS(.v14)],
        dependencies: [
            .package(url: "https://github.com/sindresorhus/KeyboardShortcuts", from: "2.0.0"),
            .package(url: "https://github.com/sindresorhus/LaunchAtLogin-modern", from: "1.0.0"),
            .package(url: "https://github.com/sindresorhus/Defaults", from: "8.0.0"),
        ],
        targets: []  // Xcode-Projekt definiert die Targets; dies dient nur zur Resolver-Kompatibilität
    )
    ```

    HINWEIS: Product-Name von LaunchAtLogin-modern ist "LaunchAtLogin" (nicht "LaunchAtLogin-modern"). Verifiziere via Context7 oder README des Pakets.
  </action>
  <verify>
    <automated>xcodebuild -resolvePackageDependencies -project SPRECHKRAFT.xcodeproj -scheme SPRECHKRAFT 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild -resolvePackageDependencies -project SPRECHKRAFT.xcodeproj -scheme SPRECHKRAFT` exit code 0
    - `grep -q "KeyboardShortcuts" SPRECHKRAFT.xcodeproj/project.pbxproj` exit code 0
    - `grep -q "LaunchAtLogin" SPRECHKRAFT.xcodeproj/project.pbxproj` exit code 0
    - `grep -q "Defaults" SPRECHKRAFT.xcodeproj/project.pbxproj` exit code 0
    - `grep -q "sindresorhus/KeyboardShortcuts" Package.swift` exit code 0
    - `grep -q "sindresorhus/LaunchAtLogin-modern" Package.swift` exit code 0
    - `grep -q "sindresorhus/Defaults" Package.swift` exit code 0
    - `grep -q "platforms: \[.macOS(.v14)\]" Package.swift` exit code 0
  </acceptance_criteria>
  <done>Alle drei SPM-Dependencies sind aufgelöst und am App-Target gebunden. `xcodebuild -resolvePackageDependencies` läuft erfolgreich durch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Test-Scaffolds für RecordingState, AppState und Hotkey anlegen (RED-Phase)</name>
  <files>
    SPRECHKRAFTTests/RecordingStateTests.swift,
    SPRECHKRAFTTests/AppStateTests.swift,
    SPRECHKRAFTTests/HotkeyTests.swift
  </files>
  <read_first>
    - .planning/phases/01-app-shell/01-VALIDATION.md (Wave 0 Requirements — Test-Dateien die fehlen)
    - .planning/phases/01-app-shell/01-RESEARCH.md (Abschnitt "Anforderungen → Test-Map")
    - .planning/phases/01-app-shell/01-UI-SPEC.md (State Machine Contract + Color-Tabelle)
    - .planning/phases/01-app-shell/01-PATTERNS.md (AppState.swift Pattern — Erwartungen an die spätere Implementierung)
    - CLAUDE.md (Swift 6, Testing-Konventionen)
  </read_first>
  <behavior>
    Tests laufen, aber schlagen fehl (RED), weil die Types RecordingState und AppState noch nicht existieren und KeyboardShortcuts.Name.toggleRecording noch nicht deklariert ist. Die Test-Dateien müssen kompilieren, sobald Plan 02 (RecordingState + AppState) und Plan 03 (Hotkey-Extension) die entsprechenden Typen liefern. Zusätzlich: Die drei Test-Dateien sind am SPRECHKRAFTTests-Target im pbxproj registriert (PBXFileReference + PBXBuildFile Einträge), damit xcodebuild sie beim Test-Build kompiliert.
  </behavior>
  <action>
    Erstelle drei Swift-Testdateien mit Swift Testing (import Testing; @Test Makro — NICHT XCTest). Alle Tests verwenden `@testable import SPRECHKRAFT`.

    **SPRECHKRAFTTests/RecordingStateTests.swift:**
    ```swift
    import Testing
    import SwiftUI
    @testable import SPRECHKRAFT

    @Suite("RecordingState (FEED-01)")
    struct RecordingStateTests {
        @Test("hat genau 4 Fälle")
        func caseCount() {
            let all: [RecordingState] = [.idle, .recording, .transcribing, .llmProcessing]
            #expect(all.count == 4)
        }

        @Test(".idle.color entspricht grau #8E8E93")
        func idleColor() {
            let expected = Color(red: 0.557, green: 0.557, blue: 0.576)
            #expect(RecordingState.idle.color == expected)
        }

        @Test(".recording.color == Color(.systemRed)")
        func recordingColor() {
            #expect(RecordingState.recording.color == Color(.systemRed))
        }

        @Test(".transcribing.color == Color(.systemBlue)")
        func transcribingColor() {
            #expect(RecordingState.transcribing.color == Color(.systemBlue))
        }

        @Test(".llmProcessing.color == Color(.systemPurple)")
        func llmColor() {
            #expect(RecordingState.llmProcessing.color == Color(.systemPurple))
        }

        @Test("isPulsing nur für recording und llmProcessing")
        func isPulsing() {
            #expect(RecordingState.idle.isPulsing == false)
            #expect(RecordingState.recording.isPulsing == true)
            #expect(RecordingState.transcribing.isPulsing == false)
            #expect(RecordingState.llmProcessing.isPulsing == true)
        }

        @Test("pulseSpeed: 0.8s recording, 1.2s llm")
        func pulseSpeed() {
            #expect(RecordingState.recording.pulseSpeed == 0.8)
            #expect(RecordingState.llmProcessing.pulseSpeed == 1.2)
        }

        @Test("accessibilityLabel nicht leer für alle Zustände")
        func accessibilityLabels() {
            for state: RecordingState in [.idle, .recording, .transcribing, .llmProcessing] {
                #expect(!state.accessibilityLabel.isEmpty)
            }
        }
    }
    ```

    **SPRECHKRAFTTests/AppStateTests.swift:**
    ```swift
    import Testing
    @testable import SPRECHKRAFT

    @Suite("AppState Zustandsmaschine (Phase 1 Demo-Cycle)")
    @MainActor
    struct AppStateTests {
        @Test("startet im Idle-Zustand")
        func initialState() {
            let state = AppState()
            #expect(state.recordingState == .idle)
        }

        @Test("toggleRecording cycelt idle → recording → transcribing → llmProcessing → idle")
        func cyclesThroughAllStates() {
            let state = AppState()
            state.toggleRecording()
            #expect(state.recordingState == .recording)
            state.toggleRecording()
            #expect(state.recordingState == .transcribing)
            state.toggleRecording()
            #expect(state.recordingState == .llmProcessing)
            state.toggleRecording()
            #expect(state.recordingState == .idle)
        }
    }
    ```

    **SPRECHKRAFTTests/HotkeyTests.swift:**
    ```swift
    import Testing
    import KeyboardShortcuts
    @testable import SPRECHKRAFT

    @Suite("KeyboardShortcuts Integration (SET-02)")
    struct HotkeyTests {
        @Test("toggleRecording ist als Name registriert")
        func nameIsDeclared() {
            let name = KeyboardShortcuts.Name.toggleRecording
            #expect(name.rawValue == "toggleRecording")
        }

        @Test("toggleRecording hat initial-Shortcut ⌥⌘R")
        func initialShortcut() {
            let name = KeyboardShortcuts.Name.toggleRecording
            // initialShortcut ist der von KeyboardShortcuts bereitgestellte Getter
            let initial = name.defaultShortcut
            #expect(initial != nil)
            #expect(initial?.key == .r)
            #expect(initial?.modifiers.contains(.option) == true)
            #expect(initial?.modifiers.contains(.command) == true)
        }
    }
    ```

    **WICHTIG — Target-Membership im pbxproj:**
    Nach dem Erstellen der drei Dateien im Dateisystem müssen sie dem SPRECHKRAFTTests-Target im Xcode-Projekt zugeordnet werden. Zwei Wege:

    (A) Xcode GUI: Dateien per Drag&Drop in die Gruppe "SPRECHKRAFTTests" im Project Navigator ziehen; im Dialog "Add to Targets" HAKEN bei "SPRECHKRAFTTests" setzen (App-Target DEAKTIVIERT lassen).

    (B) pbxproj direkt: Füge für jede Testdatei einen `PBXFileReference` mit `lastKnownFileType = sourcecode.swift` und einen `PBXBuildFile` im `PBXSourcesBuildPhase` des SPRECHKRAFTTests-Targets ein.

    Die Acceptance-Criteria prüfen mit grep explizit, dass alle drei Dateinamen im pbxproj auftauchen. Ohne diesen Schritt kompiliert xcodebuild die Tests nicht und spätere Plans (02/03) können die TDD-Gates nicht verifizieren.

    WICHTIG: Die Tests werden NICHT grün sein, bis Plan 02 (RecordingState, AppState) und Plan 03 (KeyboardShortcuts.Name.toggleRecording) die Typen bereitstellen. Das ist die RED-Phase der TDD-Schleife. Kompilieren werden sie ebenfalls noch nicht — das ist gewollt und dient als "failing scaffold" im Sinne der Nyquist-Regel aus VALIDATION.md.
  </action>
  <verify>
    <automated>test -f SPRECHKRAFTTests/RecordingStateTests.swift &amp;&amp; test -f SPRECHKRAFTTests/AppStateTests.swift &amp;&amp; test -f SPRECHKRAFTTests/HotkeyTests.swift &amp;&amp; grep -q "RecordingStateTests.swift" SPRECHKRAFT.xcodeproj/project.pbxproj &amp;&amp; grep -q "AppStateTests.swift" SPRECHKRAFT.xcodeproj/project.pbxproj &amp;&amp; grep -q "HotkeyTests.swift" SPRECHKRAFT.xcodeproj/project.pbxproj &amp;&amp; echo "All test files exist and registered in pbxproj"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f SPRECHKRAFTTests/RecordingStateTests.swift` exit 0
    - `test -f SPRECHKRAFTTests/AppStateTests.swift` exit 0
    - `test -f SPRECHKRAFTTests/HotkeyTests.swift` exit 0
    - `grep -q "import Testing" SPRECHKRAFTTests/RecordingStateTests.swift` exit 0
    - `grep -q "@testable import SPRECHKRAFT" SPRECHKRAFTTests/RecordingStateTests.swift` exit 0
    - `grep -q "RecordingState.idle" SPRECHKRAFTTests/RecordingStateTests.swift` exit 0
    - `grep -q "RecordingState.recording" SPRECHKRAFTTests/RecordingStateTests.swift` exit 0
    - `grep -q "AppState()" SPRECHKRAFTTests/AppStateTests.swift` exit 0
    - `grep -q "toggleRecording" SPRECHKRAFTTests/AppStateTests.swift` exit 0
    - `grep -q "KeyboardShortcuts.Name.toggleRecording" SPRECHKRAFTTests/HotkeyTests.swift` exit 0
    - Target-Membership automatisiert verifiziert: `grep -q "RecordingStateTests.swift" SPRECHKRAFT.xcodeproj/project.pbxproj` exit 0
    - Target-Membership automatisiert verifiziert: `grep -q "AppStateTests.swift" SPRECHKRAFT.xcodeproj/project.pbxproj` exit 0
    - Target-Membership automatisiert verifiziert: `grep -q "HotkeyTests.swift" SPRECHKRAFT.xcodeproj/project.pbxproj` exit 0
  </acceptance_criteria>
  <done>Alle drei Test-Scaffolds existieren auf der Platte UND sind im pbxproj am SPRECHKRAFTTests-Target registriert (durch grep im `<automated>` verify nachgewiesen). Tests werden in Plan 02/03 grün, wenn die entsprechenden Typen implementiert sind.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Projekt-Build verifizieren und Nyquist-Sign-Off in VALIDATION.md</name>
  <files>
    .planning/phases/01-app-shell/01-VALIDATION.md
  </files>
  <read_first>
    - SPRECHKRAFT.xcodeproj/project.pbxproj (aktuelle Einstellungen)
    - Package.swift
    - SPRECHKRAFT/Info.plist
    - .planning/phases/01-app-shell/01-VALIDATION.md (Frontmatter-Flags)
  </read_first>
  <behavior>
    - App-Target builded ohne Fehler
    - SPM-Dependencies sind aufgelöst
    - Test-Target-Build-Fehler sind akzeptabel, solange sie AUSSCHLIESSLICH fehlende Typen betreffen (RecordingState / AppState / KeyboardShortcuts.Name.toggleRecording) — das ist die dokumentierte RED-Phase
    - VALIDATION.md Frontmatter wird am Ende des Tasks auf `nyquist_compliant: true` und `wave_0_complete: true` umgestellt (Sign-Off der Wave 0)
  </behavior>
  <action>
    Führe einen sauberen Projekt-Build durch, um sicherzustellen, dass:
    1. SPM-Dependencies auflösbar sind
    2. Das Xcode-Projekt kompiliert (trotz leerem SPRECHKRAFTApp.swift-Placeholder aus Task 2)
    3. Die Test-Scaffolds aus Task 4 werden von xcodebuild erkannt (Target-Membership stimmt) — die Tests in SPRECHKRAFTTests werden NICHT kompilieren (fehlende Typen in App-Target), das ist erwartet

    Falls Tests nicht kompilieren: Das ist der gewollte Zustand. Die Plans 02 und 03 machen den Test-Build grün. Dokumentiere die genauen Fehlermeldungen im SUMMARY.md dieses Plans.

    Befehle ausführen:
    ```bash
    xcodebuild -resolvePackageDependencies -project SPRECHKRAFT.xcodeproj -scheme SPRECHKRAFT
    xcodebuild -project SPRECHKRAFT.xcodeproj -scheme SPRECHKRAFT -destination 'platform=macOS' -configuration Debug build 2>&amp;1 | tail -30
    ```

    Build-Fehler des App-Targets MUSS 0 sein. Test-Target-Build-Fehler sind akzeptabel (RED-Phase), müssen aber dokumentiert werden.

    **Nyquist-Sign-Off (abschließender Schritt dieses Tasks):**
    Nach erfolgreichem App-Target-Build und dokumentierter RED-Phase der Tests: Öffne `.planning/phases/01-app-shell/01-VALIDATION.md` und aktualisiere das Frontmatter:

    ```yaml
    # Vorher:
    nyquist_compliant: false
    wave_0_complete: false

    # Nachher:
    nyquist_compliant: true
    wave_0_complete: true
    ```

    Zusätzlich: Die Checkliste am Ende von VALIDATION.md aktualisieren — alle bis dahin erfüllten Punkte abhaken (insbesondere "All tasks have `<automated>` verify or Wave 0 dependencies", "Wave 0 covers all MISSING references", "`nyquist_compliant: true` set in frontmatter"). Der Abschnitt "Approval" auf "signed off by Plan 01-01 Task 5, {Datum}" setzen.

    Begründung: Nach Plan 01-01 existieren alle Test-Scaffolds am Test-Target (Wave 0 erfüllt), alle Auto-Tasks haben `<automated>`-Blöcke (Nyquist-Regel), und die Sampling-Continuity ist gegeben. Der Validator braucht dieses Flag explizit gesetzt, bevor `/gsd-verify-work` grün werden kann.
  </action>
  <verify>
    <automated>xcodebuild -project SPRECHKRAFT.xcodeproj -scheme SPRECHKRAFT -destination 'platform=macOS' -configuration Debug build 2>&amp;1 | grep -E "(BUILD SUCCEEDED|BUILD FAILED)" | head -1 &amp;&amp; grep -q "nyquist_compliant: true" .planning/phases/01-app-shell/01-VALIDATION.md &amp;&amp; grep -q "wave_0_complete: true" .planning/phases/01-app-shell/01-VALIDATION.md &amp;&amp; echo "Build OK + VALIDATION.md signed off"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild ... -scheme SPRECHKRAFT ... build` enthält "BUILD SUCCEEDED" für das App-Target
    - Der Output erwähnt KeyboardShortcuts, LaunchAtLogin und Defaults als aufgelöste Pakete
    - Wenn Test-Build fehlschlägt (erwartet): Fehlermeldungen betreffen ausschließlich fehlende Typen RecordingState / AppState / KeyboardShortcuts.Name.toggleRecording — das ist die dokumentierte RED-Phase und wird im SUMMARY.md vermerkt
    - VALIDATION.md Frontmatter enthält `nyquist_compliant: true` (`grep -q "nyquist_compliant: true" .planning/phases/01-app-shell/01-VALIDATION.md` exit 0)
    - VALIDATION.md Frontmatter enthält `wave_0_complete: true` (`grep -q "wave_0_complete: true" .planning/phases/01-app-shell/01-VALIDATION.md` exit 0)
    - VALIDATION.md Approval-Zeile ist von "pending" auf einen signierten Vermerk umgestellt
  </acceptance_criteria>
  <done>App-Target builded erfolgreich. Projekt-Fundament steht. VALIDATION.md ist auf nyquist_compliant: true / wave_0_complete: true gesetzt und damit als Wave-0-Sign-Off abgeschlossen. Plan 02 kann aufsetzen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-Prozess → Dateisystem | `xcodebuild` liest/schreibt DerivedData, Package-Cache in ~/Library |
| SPM Remote-Dependencies → lokales Repo | SPM lädt Pakete von github.com/sindresorhus herunter (TOFU auf Package.resolved) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | SPM-Dependencies | mitigate | Konkrete `from:`-Versionen in Package.swift festpinnen (2.0.0, 1.0.0, 8.0.0); Package.resolved commiten sobald stabil; nur sindresorhus/* und bereits verifizierte Quellen zulassen |
| T-01-02 | I (Information Disclosure) | Entitlements-Datei | mitigate | Entitlements bleibt leer (kein Sandbox, aber auch keine zusätzlichen Berechtigungen); kein Hinzufügen von Netzwerk-/Dateisystem-Entitlements in Phase 1 |
| T-01-03 | E (Elevation of Privilege) | LSUIElement fehlt irrtümlich | mitigate | `plutil -extract LSUIElement raw SPRECHKRAFT/Info.plist` in acceptance_criteria; `GENERATE_INFOPLIST_FILE = NO` um Überschreibung zu verhindern (Pitfall 4 aus RESEARCH.md) |
| T-01-04 | I (Information Disclosure) | DerivedData im Repo | mitigate | `.gitignore` listet DerivedData/, build/, xcuserdata/, .swiftpm/ — verhindert Leck von User-spezifischen Pfaden und Build-Cache |
| T-01-05 | S (Spoofing) | Bundle Identifier | accept | `com.sprechkraft.app` ist noch nicht auf Apple Developer registriert; akzeptiertes Risiko für Phase 1 (lokaler Dev-Build ohne Distribution) |
</threat_model>

<verification>
- Xcode-Projekt öffnet in Xcode GUI ohne Fehlerdialoge
- `xcodebuild -list -project SPRECHKRAFT.xcodeproj` listet beide Targets
- `xcodebuild -resolvePackageDependencies` erfolgreich
- `xcodebuild ... build` für App-Target: BUILD SUCCEEDED
- Info.plist: LSUIElement=true, korrekt als plist validiert
- Entitlements: enthält KEIN com.apple.security.app-sandbox
- Test-Scaffold-Dateien existieren UND sind im pbxproj am SPRECHKRAFTTests-Target registriert
- VALIDATION.md Frontmatter auf nyquist_compliant: true und wave_0_complete: true gesetzt
</verification>

<success_criteria>
Plan 01 ist fertig, wenn:
1. Xcode ist nachweislich installiert und nutzbar
2. SPRECHKRAFT.xcodeproj existiert mit App- und Test-Target
3. Alle drei SPM-Dependencies sind gebunden und aufgelöst
4. Info.plist setzt LSUIElement=YES; Entitlements enthält keine Sandbox
5. App-Target builded ohne Fehler
6. Drei Test-Dateien existieren als RED-Phase-Scaffolds und sind im pbxproj registriert
7. VALIDATION.md ist auf nyquist_compliant: true / wave_0_complete: true signiert
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/01-app-shell/01-01-SUMMARY.md` mit:
- Xcode-Version und Deployment-Target
- Bundle Identifier
- SPM-Dependency-Versionen (aus Package.resolved)
- Liste der erstellten Dateien
- Status der Test-Scaffolds (RED, dokumentiert) inkl. exakter Compiler-Fehlermeldungen
- Nyquist-Sign-Off: nyquist_compliant + wave_0_complete auf true gesetzt, inkl. Datum
- Nächste Wave: Plan 02 (AppState + RecordingState)
</output>
</output>
