---
phase: 01-app-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - SPRECHKRAFT/AppState.swift
  - SPRECHKRAFT/StatusBarIconView.swift
  - SPRECHKRAFT/Constants/DesignTokens.swift
autonomous: true
requirements:
  - FEED-01

must_haves:
  truths:
    - "Enum RecordingState existiert mit genau 4 Fällen: .idle, .recording, .transcribing, .llmProcessing"
    - "RecordingState.color liefert die UI-SPEC-konformen Farben (grau, systemRed, systemBlue, systemPurple)"
    - "RecordingState.isPulsing ist true genau für .recording und .llmProcessing"
    - "RecordingState.pulseSpeed ist 0.8 für .recording, 1.2 für .llmProcessing"
    - "RecordingState.accessibilityLabel liefert für jeden Zustand einen deutschen Text"
    - "AppState ist @MainActor @Observable mit recordingState: RecordingState"
    - "AppState.toggleRecording() cycelt durch alle 4 Zustände in Reihenfolge idle → recording → transcribing → llmProcessing → idle"
    - "StatusBarIconView bindet an RecordingState und animiert opacity 1.0 → 0.5 für pulsierende Zustände"
    - "StatusBarIconView nutzt renderingMode(.alwaysOriginal) für D-03"
    - "DesignTokens-Enum existiert mit Spacing-Skala xs/sm/md/lg/xl (4/8/16/24/32)"
    - "Alle Tests aus Plan 01 (RecordingStateTests, AppStateTests) laufen grün — GREEN-Phase"
  artifacts:
    - path: "SPRECHKRAFT/AppState.swift"
      provides: "RecordingState-Enum und AppState @Observable Source of Truth"
      contains: "enum RecordingState"
      min_lines: 60
    - path: "SPRECHKRAFT/StatusBarIconView.swift"
      provides: "SwiftUI View für Menu-Bar-Icon mit Pulse-Animation"
      contains: "mic.fill"
      min_lines: 25
    - path: "SPRECHKRAFT/Constants/DesignTokens.swift"
      provides: "Design-Token-Konstanten (Spacing)"
      contains: "enum Spacing"
      min_lines: 15
  key_links:
    - from: "SPRECHKRAFT/StatusBarIconView.swift"
      to: "SPRECHKRAFT/AppState.swift"
      via: "state: RecordingState Parameter + onChange(of:)"
      pattern: "state: RecordingState"
---

<objective>
Implementiert die Domain-Schicht und die View-Ebene für das Menu-Bar-Icon. Legt das RecordingState-Enum mit Farben, Pulse-Eigenschaften und Accessibility-Labels gemäß UI-SPEC an. Erstellt AppState als @MainActor @Observable Source of Truth, die in Phase 1 im Demo-Modus zyklisch durch alle 4 Zustände tastet. Die SwiftUI-View StatusBarIconView rendert das mic.fill-Symbol mit Zustandsfarbe und Pulse-Animation. Die Tests aus Plan 01 werden durch diese Implementierung grün (GREEN-Phase der TDD-Schleife).

Purpose: Diese Schicht ist die Source of Truth für alle späteren Phasen. Phase 2 (Audio), 3 (Transcription) und 5 (LLM) setzen Zustandsänderungen an AppState. Die Trennung zwischen State (AppState) und View (StatusBarIconView) ermöglicht Tests ohne UI und Austausch der View ohne Logik-Änderung.
Output: 3 Swift-Dateien — reiner Swift-Code, keine AppKit-Anbindung (das erledigt Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-app-shell/01-CONTEXT.md
@.planning/phases/01-app-shell/01-RESEARCH.md
@.planning/phases/01-app-shell/01-PATTERNS.md
@.planning/phases/01-app-shell/01-UI-SPEC.md
@SPRECHKRAFTTests/RecordingStateTests.swift
@SPRECHKRAFTTests/AppStateTests.swift

<interfaces>
<!-- Dieses Plan DEFINIERT diese Contracts — Plan 03 KONSUMIERT sie -->

Public Types (Plan 03 importiert diese):

```swift
// SPRECHKRAFT/AppState.swift
public enum RecordingState: Equatable {
    case idle
    case recording
    case transcribing
    case llmProcessing

    var color: Color { get }
    var isPulsing: Bool { get }
    var pulseSpeed: Double { get }
    var accessibilityLabel: String { get }
}

@MainActor
@Observable
public final class AppState {
    public var recordingState: RecordingState { get set }
    public init()
    public func toggleRecording()
}
```

```swift
// SPRECHKRAFT/StatusBarIconView.swift
public struct StatusBarIconView: View {
    public init(state: RecordingState)
    public var body: some View { get }
}
```

```swift
// SPRECHKRAFT/Constants/DesignTokens.swift
public enum DesignTokens {
    public enum Spacing {
        static let xs: CGFloat  // 4
        static let sm: CGFloat  // 8
        static let md: CGFloat  // 16
        static let lg: CGFloat  // 24
        static let xl: CGFloat  // 32
    }
}
```

Test-Erwartungen (aus Plan 01, die diese Implementierung grün machen MUSS):
- Alle 4 RecordingState-Fälle, exakte Farben, isPulsing/pulseSpeed-Werte, non-empty accessibilityLabels
- AppState startet bei .idle, toggleRecording cycelt durch 4 Zustände
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecordingState-Enum und AppState @Observable implementieren</name>
  <files>SPRECHKRAFT/AppState.swift</files>
  <read_first>
    - .planning/phases/01-app-shell/01-PATTERNS.md (Abschnitt "SPRECHKRAFT/AppState.swift" — vollständiger Pattern-Code)
    - .planning/phases/01-app-shell/01-UI-SPEC.md (State Machine Contract + Color-Tabelle + Accessibility Contract)
    - .planning/phases/01-app-shell/01-RESEARCH.md (Pattern 2 — @Observable Source of Truth; Pitfall 3 — Swift 6 Concurrency)
    - SPRECHKRAFTTests/RecordingStateTests.swift (Test-Erwartungen, die grün werden müssen)
    - SPRECHKRAFTTests/AppStateTests.swift (Test-Erwartungen für toggleRecording-Cycle)
    - SPRECHKRAFT/AppState.swift (falls bereits als leeres Template existent)
  </read_first>
  <behavior>
    - RecordingStateTests läuft vollständig grün
    - AppStateTests läuft vollständig grün
    - Swift 6 strict concurrency kompiliert ohne Warnungen
    - Kein Import von AppKit — diese Datei ist UI-Framework-agnostisch (SwiftUI Color ist erlaubt)
  </behavior>
  <action>
    Erstelle SPRECHKRAFT/AppState.swift mit folgendem Inhalt (exakt, da dies die in Plan 01 getestete Contract erfüllt):

    ```swift
    // SPRECHKRAFT/AppState.swift
    // Zweck: Source of Truth für den Aufnahme-Zustand der App.
    // Phase 1: Demo-Cycle durch alle 4 Zustände — echte Audio-Logik folgt Phase 2.
    // Implementiert Requirement FEED-01 (4 Icon-Zustände) und UI-SPEC D-02/D-04.

    import Foundation
    import Observation
    import SwiftUI

    /// 4-Zustands-Modell für das Menu-Bar-Icon.
    /// Quelle: CONTEXT.md D-01 bis D-04, UI-SPEC State Machine Contract
    enum RecordingState: Equatable {
        case idle          // Icon: grau (#8E8E93), statisch
        case recording     // Icon: systemRed,     pulsierend 0.8s
        case transcribing  // Icon: systemBlue,    statisch
        case llmProcessing // Icon: systemPurple,  pulsierend 1.2s

        /// Farbe laut UI-SPEC Color Contract (D-02).
        /// Hinweis: idle verwendet exakte sRGB-Komponenten #8E8E93 = (0.557, 0.557, 0.576),
        /// die anderen Zustände nutzen System-Accent-Farben (Dark/Light-Mode-aware).
        var color: Color {
            switch self {
            case .idle:          return Color(red: 0.557, green: 0.557, blue: 0.576)
            case .recording:     return Color(.systemRed)
            case .transcribing:  return Color(.systemBlue)
            case .llmProcessing: return Color(.systemPurple)
            }
        }

        /// Pulse-Animation Contract (D-04): aktiv für Recording und LLM.
        var isPulsing: Bool {
            self == .recording || self == .llmProcessing
        }

        /// Pulse-Geschwindigkeit (D-04): 0.8s Recording, 1.2s LLM.
        var pulseSpeed: Double {
            self == .recording ? 0.8 : 1.2
        }

        /// Accessibility Contract laut UI-SPEC: deutscher Text pro Zustand.
        var accessibilityLabel: String {
            switch self {
            case .idle:          return "SPRECHKRAFT — Bereit"
            case .recording:     return "SPRECHKRAFT — Aufnahme läuft"
            case .transcribing:  return "SPRECHKRAFT — Transkribiert"
            case .llmProcessing: return "SPRECHKRAFT — KI verarbeitet"
            }
        }
    }

    /// Zentrale Source of Truth. Swift 6: @MainActor, damit alle UI-Zugriffe
    /// auf dem Main Thread erfolgen. @Observable ermöglicht SwiftUI-Reaktivität.
    @MainActor
    @Observable
    final class AppState {
        var recordingState: RecordingState = .idle

        init() {}

        /// Phase 1 Demo: cycelt zyklisch durch alle 4 Zustände.
        /// Phase 2 ersetzt dies durch echte Audio-Capture-Events.
        func toggleRecording() {
            switch recordingState {
            case .idle:          recordingState = .recording
            case .recording:     recordingState = .transcribing
            case .transcribing:  recordingState = .llmProcessing
            case .llmProcessing: recordingState = .idle
            }
        }
    }
    ```

    Datei in SPRECHKRAFT/AppState.swift speichern. Keine weiteren Typen, keine Extensions in diese Datei.

    Anschließend in Xcode: Datei zum App-Target "SPRECHKRAFT" hinzufügen (Project Navigator → Rechtsklick auf SPRECHKRAFT-Gruppe → New Group für logische Gruppierung "Domain" → Datei dort einfügen), falls sie nicht schon Teil des Targets ist.
  </action>
  <verify>
    <automated>xcodebuild -project SPRECHKRAFT.xcodeproj -scheme SPRECHKRAFT -destination 'platform=macOS' test -only-testing:SPRECHKRAFTTests/RecordingStateTests -only-testing:SPRECHKRAFTTests/AppStateTests 2>&amp;1 | grep -E "(Test Suite|passed|failed)" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f SPRECHKRAFT/AppState.swift` exit 0
    - `grep -qE "enum RecordingState: Equatable" SPRECHKRAFT/AppState.swift` exit 0
    - `grep -q "case idle" SPRECHKRAFT/AppState.swift` exit 0
    - `grep -q "case recording" SPRECHKRAFT/AppState.swift` exit 0
    - `grep -q "case transcribing" SPRECHKRAFT/AppState.swift` exit 0
    - `grep -q "case llmProcessing" SPRECHKRAFT/AppState.swift` exit 0
    - `grep -q "@MainActor" SPRECHKRAFT/AppState.swift` exit 0
    - `grep -q "@Observable" SPRECHKRAFT/AppState.swift` exit 0
    - `grep -q "func toggleRecording" SPRECHKRAFT/AppState.swift` exit 0
    - `grep -q 'Color(red: 0.557, green: 0.557, blue: 0.576)' SPRECHKRAFT/AppState.swift` exit 0
    - `xcodebuild test -only-testing:SPRECHKRAFTTests/RecordingStateTests` exit code 0 (Tests grün)
    - `xcodebuild test -only-testing:SPRECHKRAFTTests/AppStateTests` exit code 0 (Tests grün)
  </acceptance_criteria>
  <done>RecordingState und AppState sind implementiert, beide Testsuites laufen grün. Swift 6 strict concurrency ohne Warnungen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusBarIconView mit Pulse-Animation</name>
  <files>SPRECHKRAFT/StatusBarIconView.swift</files>
  <read_first>
    - .planning/phases/01-app-shell/01-PATTERNS.md (Abschnitt "SPRECHKRAFT/StatusBarIconView.swift" — vollständiger Pattern-Code)
    - .planning/phases/01-app-shell/01-UI-SPEC.md (Icon-Design Contract — 18×18 pt, SF Symbol mic.fill, Animationen)
    - .planning/phases/01-app-shell/01-RESEARCH.md (Pattern 3 — NSHostingView; Pitfall 5 — Liquid Glass)
    - SPRECHKRAFT/AppState.swift (RecordingState-Contract, muss existieren aus Task 1)
  </read_first>
  <behavior>
    - View nimmt RecordingState als Input, zeigt mic.fill in der Zustandsfarbe
    - `.renderingMode(.alwaysOriginal)` erzwingt D-03 (keine Template-Umfärbung)
    - Für pulsierende Zustände: opacity animiert easeInOut zwischen 1.0 und 0.5 mit pulseSpeed
    - Für statische Zustände: opacity 1.0, keine Animation
    - Frame: 18×18 pt (macOS Menu-Bar-Icon-Standardgröße)
    - SwiftUI-Preview vorhanden, damit Entwickler die 4 Zustände im Canvas sehen kann
  </behavior>
  <action>
    Erstelle SPRECHKRAFT/StatusBarIconView.swift mit folgendem Inhalt:

    ```swift
    // SPRECHKRAFT/StatusBarIconView.swift
    // Zweck: SwiftUI-Darstellung des Menu-Bar-Icons. Wird via NSHostingView
    // (in Plan 03 / AppDelegate) in NSStatusItem.button eingebettet.
    // Implementiert UI-SPEC Icon-Design Contract (D-01 bis D-04) und FEED-01.

    import SwiftUI

    /// Darstellung des Menu-Bar-Icons mit Zustandsfarbe und optionaler Pulse-Animation.
    /// - Note: Die tatsächliche Einbettung in NSStatusItem übernimmt AppDelegate (Plan 03)
    ///         via NSHostingView.
    struct StatusBarIconView: View {
        let state: RecordingState
        @State private var opacity: Double = 1.0

        var body: some View {
            Image(systemName: "mic.fill")
                // D-03: .alwaysOriginal — Farben bleiben sichtbar auch auf
                // macOS 26 Tahoe mit transparentem Menu-Bar (Liquid Glass).
                .renderingMode(.alwaysOriginal)
                .foregroundStyle(state.color)
                .font(.system(size: 16, weight: .medium))
                .frame(width: 18, height: 18)
                .opacity(opacity)
                .onAppear { applyAnimation(for: state) }
                .onChange(of: state) { _, newState in
                    applyAnimation(for: newState)
                }
        }

        /// Wendet die Pulse-Animation an (D-04):
        /// - .recording → easeInOut 0.8s, opacity 1.0 ↔ 0.5, repeatForever
        /// - .llmProcessing → easeInOut 1.2s, opacity 1.0 ↔ 0.5, repeatForever
        /// - .idle / .transcribing → keine Animation, opacity 1.0
        private func applyAnimation(for state: RecordingState) {
            if state.isPulsing {
                withAnimation(
                    .easeInOut(duration: state.pulseSpeed)
                        .repeatForever(autoreverses: true)
                ) {
                    opacity = 0.5
                }
            } else {
                withAnimation(nil) {
                    opacity = 1.0
                }
            }
        }
    }

    #Preview("Idle") {
        StatusBarIconView(state: .idle).padding()
    }

    #Preview("Recording") {
        StatusBarIconView(state: .recording).padding()
    }

    #Preview("Transcribing") {
        StatusBarIconView(state: .transcribing).padding()
    }

    #Preview("LLM") {
        StatusBarIconView(state: .llmProcessing).padding()
    }
    ```

    Datei im Xcode-Projekt an das App-Target "SPRECHKRAFT" anhängen (Gruppe "Views").
  </action>
  <verify>
    <automated>xcodebuild -project SPRECHKRAFT.xcodeproj -scheme SPRECHKRAFT -destination 'platform=macOS' -configuration Debug build 2>&amp;1 | grep -E "(BUILD SUCCEEDED|error:)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f SPRECHKRAFT/StatusBarIconView.swift` exit 0
    - `grep -q 'Image(systemName: "mic.fill")' SPRECHKRAFT/StatusBarIconView.swift` exit 0
    - `grep -q ".renderingMode(.alwaysOriginal)" SPRECHKRAFT/StatusBarIconView.swift` exit 0
    - `grep -q ".foregroundStyle(state.color)" SPRECHKRAFT/StatusBarIconView.swift` exit 0
    - `grep -q ".frame(width: 18, height: 18)" SPRECHKRAFT/StatusBarIconView.swift` exit 0
    - `grep -qE "repeatForever\(autoreverses: true\)" SPRECHKRAFT/StatusBarIconView.swift` exit 0
    - `grep -q "let state: RecordingState" SPRECHKRAFT/StatusBarIconView.swift` exit 0
    - `xcodebuild ... build` enthält "BUILD SUCCEEDED"
  </acceptance_criteria>
  <done>StatusBarIconView kompiliert und zeigt in Preview alle 4 Zustände korrekt. Pulse-Animation ist für recording/llmProcessing aktiv, statische Anzeige für idle/transcribing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DesignTokens-Enum für Spacing-Konstanten</name>
  <files>SPRECHKRAFT/Constants/DesignTokens.swift</files>
  <read_first>
    - .planning/phases/01-app-shell/01-UI-SPEC.md (Spacing Scale Abschnitt — Werte xs/sm/md/lg/xl)
    - .planning/phases/01-app-shell/01-PATTERNS.md (Abschnitt "SPRECHKRAFT/Constants/DesignTokens.swift")
  </read_first>
  <behavior>
    - Enum DesignTokens mit verschachteltem enum Spacing
    - Alle 5 Spacing-Konstanten entsprechen exakt der UI-SPEC-Tabelle
    - Datei wird von SettingsView (Plan 03) konsumiert für Fensterpadding
  </behavior>
  <action>
    Erstelle SPRECHKRAFT/Constants/DesignTokens.swift mit folgendem Inhalt:

    ```swift
    // SPRECHKRAFT/Constants/DesignTokens.swift
    // Zweck: Zentrale Design-Token-Konstanten.
    // Quelle: UI-SPEC Spacing Scale (Vielfache von 4).

    import Foundation
    import SwiftUI

    /// Design-Tokens für SPRECHKRAFT — ausschließlich Swift-Konstanten, kein CSS.
    /// Accent-Farben für Icon-Zustände sind in RecordingState.color (AppState.swift) deklariert,
    /// NICHT hier — Icon-State ist Domain, nicht Design.
    enum DesignTokens {
        /// Spacing-Skala laut UI-SPEC: Vielfache von 4.
        enum Spacing {
            /// 4 pt — Icon-interne Abstände
            static let xs: CGFloat = 4
            /// 8 pt — Menüpunkt-Innenabstand (vertikal)
            static let sm: CGFloat = 8
            /// 16 pt — Standard-Element-Abstand
            static let md: CGFloat = 16
            /// 24 pt — Abschnittstrennungen im Menü
            static let lg: CGFloat = 24
            /// 32 pt — Fensterkanten-Padding (Einstellungsfenster)
            static let xl: CGFloat = 32
        }
    }
    ```

    Datei zum App-Target "SPRECHKRAFT" hinzufügen (Gruppe "Constants" im Xcode-Navigator).
  </action>
  <verify>
    <automated>grep -c "static let" SPRECHKRAFT/Constants/DesignTokens.swift</automated>
  </verify>
  <acceptance_criteria>
    - `test -f SPRECHKRAFT/Constants/DesignTokens.swift` exit 0
    - `grep -q "enum DesignTokens" SPRECHKRAFT/Constants/DesignTokens.swift` exit 0
    - `grep -q "enum Spacing" SPRECHKRAFT/Constants/DesignTokens.swift` exit 0
    - `grep -q "static let xs: CGFloat = 4" SPRECHKRAFT/Constants/DesignTokens.swift` exit 0
    - `grep -q "static let sm: CGFloat = 8" SPRECHKRAFT/Constants/DesignTokens.swift` exit 0
    - `grep -q "static let md: CGFloat = 16" SPRECHKRAFT/Constants/DesignTokens.swift` exit 0
    - `grep -q "static let lg: CGFloat = 24" SPRECHKRAFT/Constants/DesignTokens.swift` exit 0
    - `grep -q "static let xl: CGFloat = 32" SPRECHKRAFT/Constants/DesignTokens.swift` exit 0
    - `xcodebuild build` exit code 0
  </acceptance_criteria>
  <done>DesignTokens.Spacing-Konstanten stehen projektweit zur Verfügung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppState → SwiftUI View | Keine — beides im Prozess-internen MainActor-Scope, kein IPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | T (Tampering) | RecordingState-Farbwerte | mitigate | Farbwerte als Literal-Konstanten im Enum, kein Zugriff von außen; UI-SPEC-Contract per RecordingStateTests verifiziert |
| T-01-07 | I (Information Disclosure) | AppState-Zustand | accept | AppState.recordingState ist ausschließlich UI-Zustand ohne PII; keine Mitigation nötig |
| T-01-08 | D (Denial of Service) | Pulse-Animation repeatForever | mitigate | Animation wird nur gestartet, wenn isPulsing true — Wechsel zu .idle beendet die Animation via `withAnimation(nil)`; keine unbegrenzten Task-Loops |
| T-01-09 | R (Repudiation) | Zustandswechsel toggleRecording | accept | Phase 1 Demo-Cycle loggt nichts; in Phase 2 kommt Audio-Event-Logging dazu |
</threat_model>

<verification>
- RecordingStateTests läuft grün (alle 8 Tests)
- AppStateTests läuft grün (2 Tests)
- `xcodebuild build` exit 0 ohne Warnungen zu Sendable/MainActor
- Alle drei neuen Dateien sind am Xcode-App-Target registriert
- SwiftUI-Previews in StatusBarIconView rendern alle 4 Zustände korrekt
</verification>

<success_criteria>
Plan 02 ist fertig, wenn:
1. RecordingState + AppState existieren und erfüllen alle Test-Contracts aus Plan 01 (GREEN)
2. StatusBarIconView rendert die 4 Zustände mit korrekter Farbe und Animation
3. DesignTokens.Spacing steht projektweit zur Verfügung
4. Projekt baut ohne Swift-6-Concurrency-Warnungen
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/01-app-shell/01-02-SUMMARY.md` mit:
- Test-Status: RecordingStateTests + AppStateTests grün
- Liste der öffentlichen API-Contracts (RecordingState-Cases, AppState-Methoden, StatusBarIconView-Parameter)
- Downstream-Konsumenten (Plan 03: AppDelegate integriert AppState + StatusBarIconView in NSStatusItem)
- Designentscheidungen, die bewusst vom Pattern-Vorschlag abweichen (falls zutreffend)
</output>
</output>
