---
phase: 07-parakeet-backend
plan: 03
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - SPRECHKRAFT.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - RECORD-04

must_haves:
  truths:
    - "WhisperKit SPM dependency is completely removed from the project"
    - "FluidAudio 0.12.4 SPM dependency is added to the project"
    - "Project builds without WhisperKit-related errors"
  artifacts:
    - path: "SPRECHKRAFT.xcodeproj/project.pbxproj"
      provides: "Cleaned pbxproj with FluidAudio added and WhisperKit removed at all 5 locations"
      contains: "FluidAudio"
  key_links:
    - from: "SPRECHKRAFT/Transcription/ParakeetBackend.swift"
      to: "SPRECHKRAFT.xcodeproj/project.pbxproj"
      via: "import FluidAudio resolved via SPM"
      pattern: "FluidAudio"
---

<objective>
Remove WhisperKit SPM dependency from the project and add FluidAudio 0.12.4. This is a prerequisite for Wave 2 (ParakeetBackend uses FluidAudio) and enables removing the WhisperKit import from TranscriptionService.

Purpose: Without this plan, Wave 2's `import FluidAudio` will fail to compile, and the leftover WhisperKit symbols will cause linker errors once TranscriptionService no longer references them.
Output: project.pbxproj modified (WhisperKit removed, FluidAudio added), Package.resolved regenerated by Xcode.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/phases/07-parakeet-backend/07-CONTEXT.md
@/Users/mbieling/claude/voice/.planning/phases/07-parakeet-backend/07-RESEARCH.md

<interfaces>
<!-- WhisperKit identifiers in project.pbxproj (RESEARCH.md Pitfall 7 — all 5 locations): -->

Line 41:  CAFE0028 /* WhisperKit in Frameworks */ = {isa = PBXBuildFile; productRef = BEEF0028 /* WhisperKit */; };
Line 89:  CAFE0028 /* WhisperKit in Frameworks */, (in PBXFrameworksBuildPhase)
Line 250: BEEF0028 /* WhisperKit */,  (in packageProductDependencies array)
Line 308: DEAD0103 /* XCRemoteSwiftPackageReference "argmax-oss-swift" */,  (in packageReferences array)
Lines 641-648: DEAD0103 /* XCRemoteSwiftPackageReference "argmax-oss-swift" */ = { ... } block
Lines 683-687: BEEF0028 /* WhiskerKit */ = { ... } block (XCSwiftPackageProductDependency)

<!-- FluidAudio SPM URL to add: -->
https://github.com/FluidInference/FluidAudio.git
Version: from "0.12.4"
Product name: FluidAudio
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove WhisperKit from project.pbxproj at all 5 locations</name>
  <read_first>
    - SPRECHKRAFT.xcodeproj/project.pbxproj lines 35-50 (CAFE0028 PBXBuildFile block)
    - SPRECHKRAFT.xcodeproj/project.pbxproj lines 82-100 (PBXFrameworksBuildPhase — CAFE0028 reference)
    - SPRECHKRAFT.xcodeproj/project.pbxproj lines 245-255 (packageProductDependencies — BEEF0028)
    - SPRECHKRAFT.xcodeproj/project.pbxproj lines 303-315 (packageReferences — DEAD0103)
    - SPRECHKRAFT.xcodeproj/project.pbxproj lines 635-695 (XCRemoteSwiftPackageReference + XCSwiftPackageProductDependency blocks)
    - .planning/phases/07-parakeet-backend/07-RESEARCH.md section "Pitfall 7" (all 5 removal points)
  </read_first>
  <files>SPRECHKRAFT.xcodeproj/project.pbxproj</files>
  <action>
Remove WhisperKit from SPRECHKRAFT.xcodeproj/project.pbxproj at exactly 5 locations. Partial removal causes an "unresolved package" build error — all 5 must be removed together.

Read the actual current line numbers first (they may differ slightly from research estimates). Use these grep commands to find exact locations before editing:

```bash
grep -n "CAFE0028\|BEEF0028\|DEAD0103\|WhisperKit\|argmax-oss-swift" SPRECHKRAFT.xcodeproj/project.pbxproj
```

**Location 1 — PBXBuildFile entry (single line):**
Remove the entire line matching: `CAFE0028 /* WhisperKit in Frameworks */ = {isa = PBXBuildFile; productRef = BEEF0028 /* WhisperKit */; };`

**Location 2 — PBXFrameworksBuildPhase reference (single line):**
Remove the line: `CAFE0028 /* WhisperKit in Frameworks */,`

**Location 3 — packageProductDependencies array entry (single line):**
Remove the line: `BEEF0028 /* WhiskerKit */,` (in the target's packageProductDependencies array)

**Location 4 — packageReferences array entry (single line):**
Remove the line: `DEAD0103 /* XCRemoteSwiftPackageReference "argmax-oss-swift" */,` (in the project's packageReferences array)

**Location 5 — Two object blocks to remove entirely:**
Remove the complete `XCRemoteSwiftPackageReference` block for DEAD0103 (approximately lines 641-648, looks like):
```
DEAD0103 /* XCRemoteSwiftPackageReference "argmax-oss-swift" */ = {
    isa = XCRemoteSwiftPackageReference;
    repositoryURL = "https://github.com/argmaxinc/argmax-oss-swift";
    requirement = {
        kind = upToNextMajorVersion;
        minimumVersion = ...;
    };
};
```

Remove the complete `XCSwiftPackageProductDependency` block for BEEF0028 (approximately lines 683-687, looks like):
```
BEEF0028 /* WhisperKit */ = {
    isa = XCSwiftPackageProductDependency;
    package = DEAD0103 /* XCRemoteSwiftPackageReference "argmax-oss-swift" */;
    productName = WhisperKit;
};
```

After removal, verify no WhisperKit, CAFE0028, BEEF0028, DEAD0103, or argmax references remain:
```bash
grep -c "WhiskerKit\|CAFE0028\|BEEF0028\|DEAD0103\|argmax" SPRECHKRAFT.xcodeproj/project.pbxproj
```
Expected output: 0

IMPORTANT: Do not modify any other content in project.pbxproj. Other SPM dependencies (KeyboardShortcuts, KeychainAccess, LaunchAtLogin, GRDB, Defaults) must remain untouched.
  </action>
  <verify>
    <automated>grep -c "WhisperKit\|argmax-oss-swift\|CAFE0028\|BEEF0028\|DEAD0103" /Users/mbieling/claude/voice/SPRECHKRAFT.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WhiskerKit\|argmax-oss-swift" SPRECHKRAFT.xcodeproj/project.pbxproj` outputs `0`
    - `grep -c "CAFE0028\|BEEF0028\|DEAD0103" SPRECHKRAFT.xcodeproj/project.pbxproj` outputs `0`
    - `grep "GRDB\|KeyboardShortcuts\|KeychainAccess\|LaunchAtLogin\|Defaults" SPRECHKRAFT.xcodeproj/project.pbxproj` still returns results (other SPM deps untouched)
  </acceptance_criteria>
  <done>All 5 WhisperKit references removed from project.pbxproj. No WhiskerKit, CAFE0028, BEEF0028, DEAD0103, or argmax-oss-swift references remain.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Add FluidAudio via Xcode — File > Add Package Dependencies</name>
  <what-built>Task 1 removed WhisperKit from pbxproj. Now FluidAudio must be added. This requires Xcode UI interaction because adding an SPM package via Xcode generates correct UUIDs and all required pbxproj sections automatically. Manual pbxproj editing for SPM addition is error-prone and fragile.</what-built>
  <how-to-verify>
    1. Open Xcode: `open /Users/mbieling/claude/voice/SPRECHKRAFT.xcodeproj`
    2. Wait for Xcode to finish loading and resolving packages (it will try to resolve, may show a warning about missing WhisperKit — that is expected)
    3. Go to: File > Add Package Dependencies...
    4. In the search field, paste: `https://github.com/FluidInference/FluidAudio.git`
    5. Press Enter and wait for Xcode to fetch package metadata
    6. In the version dropdown, select "Exact Version" and enter: `0.12.4`
       (Alternatively: "Up to Next Minor Version" with `0.12.4` as minimum — either is acceptable)
    7. Click "Add Package"
    8. In the "Add to Target" dialog that appears: check `SPRECHKRAFT` (not SPRECHKRAFTTests)
    9. Click "Add Package" to confirm
    10. Xcode updates project.pbxproj and Package.resolved automatically

    Verify success:
    - Xcode shows "FluidAudio" in the project navigator under "Package Dependencies"
    - Run: `grep "FluidAudio\|FluidInference" SPRECHKRAFT.xcodeproj/project.pbxproj | head -5`
      Expected: at least 4 lines (PBXBuildFile, PBXFrameworksBuildPhase, XCRemoteSwiftPackageReference, XCSwiftPackageProductDependency)
    - Run: `grep "FluidAudio" SPRECHKRAFT.xcodeproj/Package.resolved`
      Expected: package entry with "FluidInference/FluidAudio" and version 0.12.4
  </how-to-verify>
  <resume-signal>Type "FluidAudio added" when Xcode shows FluidAudio in Package Dependencies and the grep commands above return results. Or describe any error encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPM → Local build | Package resolution fetches from HuggingFace/GitHub at build time; no untrusted code execution at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04 | Tampering | Package.resolved FluidAudio checksum | accept | SPM verifies package integrity via checksum in Package.resolved; Xcode enforces this automatically |
| T-7-05 | Spoofing | FluidInference/FluidAudio GitHub repo | accept | Package URL locked to FluidInference org; checksum pinned after first resolution |
</threat_model>

<verification>
After checkpoint completes:
```bash
grep "FluidAudio\|FluidInference" /Users/mbieling/claude/voice/SPRECHKRAFT.xcodeproj/project.pbxproj | wc -l
```
Expected: at least 4 lines.

```bash
grep "FluidInference" /Users/mbieling/claude/voice/SPRECHKRAFT.xcodeproj/Package.resolved
```
Expected: entry with "FluidInference/FluidAudio" and version matching 0.12.4.
</verification>

<success_criteria>
- WhisperKit completely removed from project.pbxproj (0 references)
- FluidAudio 0.12.4 added as SPM dependency targeting SPRECHKRAFT target
- Package.resolved updated by Xcode with FluidAudio checksum
- Other SPM dependencies (GRDB, KeyboardShortcuts, etc.) untouched
</success_criteria>

<output>
After completion, create `.planning/phases/07-parakeet-backend/07-03-SUMMARY.md`
</output>
