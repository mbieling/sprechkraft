---
phase: 07-parakeet-backend
plan: 07
type: execute
wave: 5
depends_on:
  - "07-06"
files_modified: []
autonomous: false
requirements:
  - RECORD-04
  - RECORD-05

must_haves:
  truths:
    - "App transcribes speech via FluidAudio/Parakeet TDT v3 on first launch"
    - "Model download progress visible in menu bar title text during first-run download"
    - "Icon shows orange .modelLoading state during download, then .idle when ready"
    - "Warm-up inference completes before first recording is accepted"
    - "App builds and runs without WhiskerKit linker errors"
  artifacts:
    - path: "SPRECHKRAFT/Transcription/ParakeetBackend.swift"
      provides: "Active backend confirmed working end-to-end"
      contains: "actor ParakeetBackend"
    - path: "SPRECHKRAFT/Transcription/WhisperKitBackend.swift"
      provides: "Fallback file confirmed present and commented out"
      contains: "FALLBACK"
  key_links:
    - from: "AppDelegate (runtime)"
      to: "ParakeetBackend (runtime)"
      via: "transcriptionService.transcribeWithResampling"
      pattern: "FluidAudio ASRResult.text"
---

<objective>
Manual verification of the complete Phase 7 implementation. Confirm that FluidAudio/Parakeet replaces WhiskerKit end-to-end: download UI, warmup, live transcription, and icon states.

Purpose: Automated tests cannot cover the actual Parakeet inference quality, model download behavior, or icon animation. These checkpoints confirm the system works as a whole.
Output: Phase 7 verified and signed off.
</objective>

<execution_context>
@/Users/mbieling/claude/voice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/claude/voice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbieling/claude/voice/.planning/ROADMAP.md
@/Users/mbieling/claude/voice/.planning/phases/07-parakeet-backend/07-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full test suite green</name>
  <what-built>All Phase 7 production code is complete. Run automated tests to confirm no regressions.</what-built>
  <how-to-verify>
    Run the full test suite:
    ```
    xcodebuild test -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | tail -40
    ```
    Expected results:
    - RecordingStateTests: 9 tests pass (including 3 new Phase 7 tests)
    - AppStateTests: 6 tests pass (including isModelError test)
    - TranscriptionServiceTests: 5 tests pass (using MockTranscriptionBackend)
    - All other test suites: unchanged from Phase 6 baseline

    Also verify no WhiskerKit linker errors:
    ```
    xcodebuild build -scheme SPRECHKRAFT -destination 'platform=macOS' 2>&1 | grep -i "whisperkit\|argmax\|error:"
    ```
    Expected: no WhiskerKit or argmax references in build output.
  </how-to-verify>
  <resume-signal>Type "tests green" if all tests pass. Or paste the failing test names if any fail.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: First-launch model download UX</name>
  <what-built>ParakeetBackend.downloadAndLoad and AppDelegate.setupTranscription with cache check and D-06 title text.</what-built>
  <how-to-verify>
    To test first-launch behavior (model not yet cached):
    1. Delete the FluidAudio model cache if it exists:
       ```
       rm -rf ~/Library/Application\ Support/FluidAudio/Models
       ```
    2. Launch the app: `open -a SPRECHKRAFT` (or run from Xcode)
    3. Observe the menu bar icon immediately after launch:
       - Icon should show orange "arrow.down.circle" symbol (modelLoading state)
       - Menu bar button title should show "Parakeet-Modell wird geladen (~1.2 GB)…"
    4. Wait for download to complete (first time: may take several minutes on slow connection)
    5. After download: icon should transition to "hourglass" briefly (.warmingUp), then grey mic (.idle)
    6. Title text should clear to empty string

    If model was already cached (step 1 skipped):
    - App should launch directly to .idle state (no orange icon, no title text)
    - isModelReady should be true within a few seconds (warmup runs but no download)

    Confirm: Press the hotkey (⌥⌘R) during download — recording should NOT start (guard active).
    Confirm: Press the hotkey after .idle is reached — recording starts normally.
  </how-to-verify>
  <resume-signal>Type "download UX ok" if download → warmup → idle transition works correctly. Or describe what you observed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live transcription with Parakeet</name>
  <what-built>End-to-end: AudioController → TranscriptionService facade → ParakeetBackend → FluidAudio ASRResult.</what-built>
  <how-to-verify>
    1. Wait for the model to be ready (icon shows grey mic, isModelReady = true)
    2. Press ⌥⌘R to start recording
    3. Speak a short German sentence, e.g.: "Hallo, das ist ein Test für die Diktat-App."
    4. Press ⌥⌘R to stop recording
    5. Observe:
       - Icon transitions: idle → recording (red pulsing) → transcribing (blue) → idle
       - Text output appears in the active text field (or clipboard, depending on output mode setting)
       - Text should be a reasonable transcription of what was spoken

    Test short utterances (edge cases):
    - Speak for < 0.1 seconds — app should return to idle without output (minimum-sample guard)
    - Speak complete silence after starting — app should return to idle when silence auto-stop triggers

    Console check (open Console.app or Xcode debug console):
    - Should see NO "[ParakeetBackend] Download/Load error:" messages
    - Should see NO "[ParakeetBackend] Transkriptionsfehler:" messages for normal speech
    - Should NOT see any WhiskerKit references in output

    Quality note: First transcription after startup may have slightly higher latency if warmup was shorter than needed. Subsequent transcriptions should be fast.
  </how-to-verify>
  <resume-signal>Type "transcription ok" if Parakeet produces reasonable transcription output. Include a sample of what was said and what was transcribed if convenient. Or describe any issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Error state verification (optional — skip if inconvenient)</name>
  <what-built>AppDelegate.setupTranscription() error path: isModelError = true, recordingState = .modelError (D-08, D-09).</what-built>
  <how-to-verify>
    This test requires temporarily blocking the FluidAudio download. Skip if not convenient — the error path is unit-tested in AppStateTests.

    To test manually:
    1. Disconnect from the internet
    2. Delete the model cache: `rm -rf ~/Library/Application\ Support/FluidAudio/Models`
    3. Launch the app
    4. Observe: icon should show orange modelLoading briefly, then switch to red exclamationmark.triangle (.modelError)
    5. Reconnect internet — app will NOT auto-retry (Phase 8 adds retry button)
    6. Quit and relaunch the app to restore normal behavior

    Icon at .modelError:
    - Should show "exclamationmark.triangle.fill" SF Symbol in systemRed color
    - Hotkey should be blocked (isModelReady stays false)
  </how-to-verify>
  <resume-signal>Type "error state ok" if the .modelError icon appears correctly when download fails. Or type "skip" to skip this checkpoint.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual verification → production code | Human observation confirms behaviors that automated tests cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-12 | Denial of Service | First-launch download on slow connection | accept | User sees title text "~1.2 GB" as expectation-setting; app remains in .modelLoading with no recording capability; no timeout — download runs until complete or network failure |
</threat_model>

<verification>
Phase 7 complete when:
- Full test suite passes (automated gate)
- Model download + icon transitions confirmed (manual)
- Live Parakeet transcription produces text output (manual)
- No WhiskerKit build references remain (automated gate in Task 1)
</verification>

<success_criteria>
- All automated tests pass including 3 new RecordingStateTests, 1 new AppStateTests, 5 TranscriptionServiceTests
- Live transcription via FluidAudio/Parakeet TDT v3 confirmed working
- Download UX: orange icon + title text during download, idle after completion
- WhiskerKit completely absent from build output
- Phase 7 ROADMAP success criteria all met:
  1. App transcribes via FluidAudio/Parakeet TDT v3
  2. Model download with visible loading indicator
  3. Warmup inference after model load
  4. TranscriptionBackend protocol isolates backend from AppDelegate/AppState
  5. WhiskerKitBackend preserved as commented fallback
</success_criteria>

<output>
After completion, create `.planning/phases/07-parakeet-backend/07-07-SUMMARY.md`
</output>
